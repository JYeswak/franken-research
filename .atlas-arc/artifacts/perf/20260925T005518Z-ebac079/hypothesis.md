# Hypothesis ledger: 20260925T005518Z-ebac079

Each hypothesis gets a verdict of supports, rejects, or inconclusive, with evidence from this directory. Every claim is labelled with how it was established:
- [measured]: numbers from this run's artifacts.
- [diag]: separate diagnostic passes run after the timing runs.
- [INFERENCE]: reasoning from those numbers, not measured.

| # | Hypothesis | Verdict | Evidence |
|---:|---|---|---|
| H1 | Ranking every candidate (Array.from plus a full sort) is the largest engine cost | **supports** | [measured] Three methods agree. `selectTopK` is 59.8% of search time in the phone CDP profile, 58.2% in the Node sampler, and 59.6% under the Node stage timers (`cpu-attribution.json`, `span_summary-node-A-full.json`). The sort comparator alone is 19.5% of busy time in the Node sampler. |
| H2 | The posting walk and BM25 scoring is the second cost | **supports** | [measured] 33.4% on the phone, 39.0% in the Node sampler, 38.8% under the stage timers. `engine-a.mjs:61` recomputes `Math.log` idf per posting per field, and each document's score lives in a `Map` of objects. |
| H3 | Tokenizing and term lookup (binary search plus prefix range) matter | **rejects** | [measured] Lookup is 1.0% and tokenize 0.4% of engine time (`span_summary-node-A-full.json`). |
| H4 | Engine cost follows candidate-set size, which short prefixes and common words inflate | **supports** | [measured] The mean query gathers 926.9 candidates from 134.3 expanded terms, and the worst queries gather 1,330 to 3,747 (`span_summary-node-A-full.json`). Node p95 by query length: 1 char 3.12 ms, 3 chars 0.85 ms, 13+ chars 2.35 ms. Long phrases stay expensive because they contain "a", "of", "for" and "with" (`scaling-node.json` query_length_axis). |
| H5 | Engine latency grows faster than corpus size | **supports (weak)** | [measured] Doubling the corpus raises p95 2.51x (A) and 2.39x (C); quadrupling raises it 5.27x (A) and 5.49x (C) (`scaling-node.json`). This is consistent with a full n log n sort of candidates. Caveat: the corpus was replicated, which grows postings without growing the vocabulary. |
| H6 | A Web Worker (B) makes typing faster than the main thread (A) | **rejects** | [measured] Desktop quiet p95: B 6.2 ms, A 4.1 ms. Phone quiet p95: B 19.9 ms, and 22.9 ms once the worker's search is scaled by 4 [INFERENCE], against A's 18.0 ms (`contention.json`). The round trip adds a mean of 1.4 ms on desktop (1.89 ms search call against 0.49 ms of worker compute), and the search call's p99 is 34.7 ms (`contention.json` desktop.B.components_quiet). In Node, B p95 2.78 ms against A 2.39 ms (`bench-node-summary.json`). |
| H7 | B keeps long tasks off the main thread while typing | **supports** | [diag] Phone, 2 passes each: A 10 long tasks, B 0, I0 0 (`longtasks-browser-phone.json`). A's phone long tasks come from cold-JIT searches on the first keystrokes ("rust": 32.4 ms of search) and from frame work (55 to 99 ms after layout). [measured] Desktop quiet passes: A 6 long tasks, B 0 (`budgets.md`). |
| H8 | B pays a load-time penalty | **supports** | [measured] Phone usable p50: B 1,303 ms (budget FAIL), A 411 ms. B's core is hydrated 1,167 ms after DOMContentLoaded, but the hydrate itself takes 4.5 ms, so the time goes to getting the worker running and fetching (`baseline-browser-phone-B.json` load). [INFERENCE] The likely cause is the worker's module graph (worker.mjs, engine-a.mjs, tokenize.mjs) loading as sequential 165 ms round trips after boot. This run did not break it down per request. |
| H9 | MiniSearch (C) is competitive with the hand-written index | **rejects** | [measured] Node p95: C 16.0 ms, A 2.39 ms (6.7x). Phone quiet p95: C 59.1 ms. Node hydrate p95: C 356 ms, A 17.8 ms (`hydrate-node.json`). Main heap: C 9.96 MB, A 4.96 MB. Typing produced 111 long tasks on desktop and 3,206 on the phone (`budgets.md`). |
| H10 | Payload is the binding constraint (intake-FR2 §4.2 [I]) | **rejects** | [measured] Brotli sizes: core 102.0 KB (68% of 150 KB); full 444.4 KB (29% of 1.5 MB); full plus the quotes shard 569.0 KB (`budgets.md`, `sizes.json`). CPU per keystroke is what binds. |
| H11 | Indexing the evidence quotes is affordable | **inconclusive** | [measured] It adds 40 KB to the brotli full index (484.4 KB), and p95 moves from 2.39 to 2.50 ms. But p99.9 rises from 4.46 to 12.62 ms, and that candidate's run-to-run envelope is ESCALATE (112.9%), so the tail cannot be read (`bench-node-summary.json`, `variance-envelope.txt`). The quotes stay unindexed per DEC-009 until a rerun on a quiet host. |
| H12 | Host contention, not page work, explains most of the pooled phone numbers | **supports** | [measured] I0 does almost no work (44 names), yet its per-pass phone p95 ranged from 3.9 to 71.5 ms, and every candidate's per-pass input-delay p95 ranged from 3.7 to 131 ms (`contention.json`). [observed, not archived] `uptime` during the phone runs showed load averages of 57.23, 72.16 and 61.13 on 32 logical CPUs; `fingerprint.json` records 7.24, 14.00 and 26.32 at the start. Pooled numbers stay the primary verdict; quiet-pass numbers are post-hoc. |
| H13 | A's desktop long tasks are its own work | **rejects** | [diag] 3 desktop passes each of A, B and I0 at load average 23 to 25 produced 0 long tasks (`longtasks-browser-desktop.json`). [measured] Node A-full max is 13.8 ms over 23,300 queries (`bench-node-summary.json`). |
| H14 | Typo tolerance is cheap | **inconclusive** | [measured] MiniSearch fuzzy: p95 17.4 against 16.0 ms (+8.5%), p50 1.34 against 0.51 ms (2.6x) (`bench-node-summary.json`). A has no fuzzy matching, and the 17 queries with empty results in `golden-A-full-top10.json` are all typo variants. Fuzzy matching in A was not built. |
| H15 | Headless frame scheduling dominates `key2paint` | **supports** | [measured] Default headless rAF interval: p50 19.4 ms, p95 38.2 ms. With the frame rate unlocked, a DOM change reaches the next frame at p50 0.4 ms but max 68.2 ms (`frame-cadence.json`). This is why the primary metric stops at forced layout. |
| H16 | The lazy full index blocks typing while it hydrates | **supports (phone)** | [measured] Phone main-thread full hydrate: p50 49.7 ms, p95 78.2 ms. 13 of 20 phone runs logged a load-phase long task, with a maximum of 198 ms (`baseline-browser-phone-A.json` load). |

## New candidates raised this round
- H8 per-request breakdown: capture the worker's module and data fetches with Resource Timing inside the worker.
- H11 and the phone verdicts need a rerun on an idle host. The sentinel is `contention.json` inputDelay.
- The phone Commit p95 of 24.6 ms comes from a single contended trace pass. Re-trace on a quiet host before optimizing the frame.
