# Cross-Suite Brief — frankengraphdb

**Analyst:** cross-suite analyst (packet: frankengraphdb) · **Date:** 2026-09-22
**Method:** full read of `frankengraphdb-assessment.md`; skim of the other 43 packets in `~/workspace/franken-research/*-assessment.md` (TL;DR + claim inventory + verdict/TRL/NODUS + benchmark + license/governance sections), plus targeted greps for rider, bus-factor, and CI-de-automation evidence across the corpus.

**Tier legend (Rulebook §1, inherited):** **[Verified]** direct inspection — flavors **[Counted]**, **[Git-observed]**, **[Code-verified]**, **[CI-observed]**; **[Maintainer claim]** asserted, not independently executed; **[External]** independent sources; **[Inference]** analyst judgment. Confidence: **High** / **Medium** / **Low**.

---

## 1. OWN-PACKET DISTILLATION

**Verdict: NODUS ring Explore** [Inference, Medium] · **TRL 4** — "Durable embedded store + bounded GQL slice validated in lab (Sep-08 proof: 3,449 test outcomes, Miri green); server, execution engine, and perf gates unproven — lab-validated components, no operational environment" [Inference, Medium]. The packet explicitly considered Monitor and judged Explore correct "because the *machinery* (registries, reference oracle, simulation) is real and worth tracking even before the database ships." The rider is an advancement blocker (Explore-with-a-ceiling), not a ring qualifier.

**Strongest strength:** the claim-governance machinery is real and self-scrutinizing — machine-readable registries (`invariants.toml` 20 IDs [Counted, High], `laws.toml`, `claims_lint.toml`, `unsafe_boundary_ledger.toml`) enforced by a std-only `tools/registry-check` (84,136 src + 42,968 test lines [Counted, High]), a 19-row promise-vs-reality bridge table ("the single most load-bearing governance document in the repo" [Code-verified, High]), a 562-line negative-evidence ledger [Maintainer claim, High], and a 2026-09-04 changelog sequence documenting *two independent verifier-found overclaim residues* plus the mutations that now guard them [Maintainer claim, High].

**Strongest ceiling:** the license rider forbids OpenAI/Anthropic (plus affiliates and anyone acting for them) from *benchmarking, testing, analyzing, or indexing* the code [Code-verified (license text), High] — for a database whose §17 performance gates *are* the engineering discipline, "the honest-numbers story dies at the license, not at the lab" [Inference, Medium]. Compounded by bus factor 1 (Jeff Emanuel, sole human maintainer) [Git-observed, High], zero releases [External, High], and no CI verdict at any recent pin (de-automated 2026-09-03: "we do not use gh actions for any reason, ONLY /dsr") [CI-observed + Maintainer claim, High].

**Three most important pieces of evidence (with tiers):**
1. **The bridge table** (`docs/REALITY_CHECK_AND_BRIDGE_PLAN.md`, 19 rows): server (`fgdbd`), CLI, full GQL, Loom/GLA physical execution, Ripple subscriptions, full SSI, agent branches all "Partial" or "Not integrated" [Code-verified, High] — what ships is an embedded Rust composition crate with a bounded GQL read slice (1–2 hop, integer predicates, deterministic projection; aggregation, `ORDER BY`, mutation, typed params refused by the parser) [Maintainer claim + Code-verified, Medium].
2. **The overclaim-residue repair culture + its counterweight.** The 2026-09-04 changelog documents two verifier-found overclaims and the mutation controls guarding them [Maintainer claim, High] — raising credibility. The 2026-09-04-era README badge "No serde. Ever." is falsified by the repo's own lockfile (204 crates.io packages incl. `serde`, `serde_json` via pinned asupersync) [Counted, High] — lowering it.
3. **`unsafe_code = "forbid"` workspace-wide with exactly 6 `unsafe` block sites** confined to three `deny`+ledgered `fgdb-unsafe-*` crates [Counted, High] — plus the Sep-08 verification (9/9 core gates, 40/40 registered gates, 3,449 test outcomes in agreement, 9 Miri green [Maintainer claim, Medium]), **stale 14 days at the pin** — the executed-evidence currency problem in one line.

---

## 2. SHARED NEGATIVE PATTERNS

**P1 — The AI-lab rider (named-party benchmarking/analysis bar).** frankengraphdb carries the suite-standard MIT+OpenAI/Anthropic rider, shared with ~38 of the other 43 projects. Quoted packet evidence of the sharing:
- franken_whisper: "the MIT+OpenAI/Anthropic-rider license withholds all rights — including benchmarking and analysis — from the two leading AI labs" [Verified, High].
- franken_ocr: "a non-OSI MIT+OpenAI/Anthropic-rider license that excludes the frontier AI labs from even benchmarking it."
- asupersync: "the license rider names OpenAI and Anthropic and withholds even *benchmarking/analyzing* rights from them" [Code-verified (license text), High].
- frankenlibc: "the MIT+Anthropic/OpenAI rider makes independent evaluation by the best-equipped labs a license violation" [High].
- Exceptions proving the pattern: franken_agent_detection is "the only FrankenSuite repo with a plain-MIT, no-rider license" [Code-verified, High]; franken_native_capsule ships no LICENSE text so "the rider does not exist in this repo" [Verified, High]; the four website/dashboard repos (beads-for-frankentui, beads_for_franken_engine, frankensim_website, frankensqlite_website) have no license at all.
- Distinctive graphdb shading: its own §4.8 prices the rider as *benchmark-blocking* for a database — "no independent lab can ever validate a frankengraphdb number" — and notes the rider propagates through the whole Suite via `deny.toml`'s license-hash binding [Code-verified, High].

**P2 — Bus factor 1, often with an explicit no-outside-contributions policy.** Mentioned in 41 of 43 other packets; frankengraphdb's bus factor 1 (Jeff Emanuel) [Git-observed, High] is the norm, not the outlier. Quoted evidence:
- franken_lean: "one maintainer who refuses outside contributions" [Verified, High].
- franken_ocr: "bus factor 1 with an explicit no-outside-contributions policy" [Verified, High].
- frankenterm: "bus factor 1 by **explicit, deliberate policy** (no outside contributions accepted)."
- franken_nlp: "the bus factor is 1 *by stated policy* ('I do not accept outside contributions')."
- franken_networkx: "bus factor is 1 (single GitHub contributor, 8,808 contributions [External, High])."

**P3 — CI de-automated, disabled, or red at the pin; evidence resting on maintainer-local runs.** frankengraphdb's 2026-09-03 owner ruling (workflow `workflow_dispatch`-only; verdict contract moved to `scripts/local_proof.sh` + `/dsr`) is one instance of a suite-wide pattern:
- franken_overlap: "Disabled intentionally: this repository is validated on owner-controlled machines" — `ci/github-actions-disabled/ci.yml.disabled` [Code-verified, High].
- frankentui: "GitHub Actions was disabled 2026-09-06 so all verification runs on maintainer-private DSR infrastructure invisible to outsiders."
- franken_ocr: "the CI workflows were deleted from the tree at HEAD and the last runs (2026-09-03) failed" [Verified, High].
- frankenjax: "zero GitHub Actions workflows exist, so every 'green' claim executes on the maintainer's local RCH fleet" [External + Inference, High].
- franken_nlp: "GitHub Actions is disabled" (DSR build authority BLOCKED; workflow `on: {}`) [Code-verified, High].
- franken_code_browser: "no CI workflows (the `.github` directory contains only issue templates [Verified, High])".
- franken_whisper: "no CI config in the tree."
- franken_networkx: AGENTS.md declares "NEVER use GitHub Actions for this repository, for any reason" while the Actions API shows 7,723 runs and CI green 2026-09-09 — "the migration from Actions to DSR is documented as complete and observed as incomplete" [Code-verified + External, High].
- franken_surveillance: the qualification docs "explicitly demote GitHub CI to 'portable supplementary specification'" [Maintainer claim, High].
- Plus red-at-pin: franken_engine (native-runtime.yml "has never gone green" [CI-observed, High]), franken_node (8/8 push runs failed [CI-observed, High]), franken_numpy (G1 clippy fail), frankenredis (fmt-gate fail), franken_lean, frankentorch, frankensympy, frankenfs, franken_remote, asupersync (zero runs at pin).

**P4 — Present-tense-spec README and README-vs-code drift.** frankengraphdb's README is *deliberately* written in present tense as the 1.0 target-state spec, disclosed at L23 [Code-verified, High] — and it shares the form (and the drift) with:
- franken_manim: "the README is written in present tense as if the 1.0 design were fully realized; it is a spec of the target, not a description of the present" [Verified, High].
- franken_alignment: "written almost entirely in the present tense about a production system that does not exist yet" [Code-verified, High].
- The broader drift class: franken_networkx ("The README is a drift source" [Counted, High] — 13→12 tie-break variants, 33→34 fuzz targets, 3,823→4,129 coverage rows); franken_ocr (v0.8.0 badges vs v0.9.0 reality; "13–17 MB" binaries that are 20.6–28.5 MB [Verified, High]); franken_whisper (repo description still advertises "zero unsafe code" while HEAD's `src/` carries 33 annotated `unsafe` code lines); franken_threed (README honest-status table still says "Is there a compiler, renderer, or CLI? **No**" [Verified, High]); frankentorch ("the docs claim 'unsafe forbidden' while the tree holds **116** counted unsafe sites" [Counted, High]); frankensqlite_website ("0 unsafe blocks / pure safe Rust throughout" vs 41 counted unsafe blocks — "disproven as stated" [Verified, High]); franken_sympy ("the README's 'green workspace tests' line is stale-by-a-month" [Code-verified, High]); franken_node (README CLI drift failed CI [CI-observed, High]).

**P5 — Performance gates as targets, not results; self-speedups without incumbent ratios.** frankengraphdb's §17 gates (≥3× Neo4j SNB SF-100, ≥40M edges/s bulk load, …) are "provisional CI **gates** … **chosen from measured SOTA anchors with leapfrog margins**" — honest as gates, zero measured instances [Maintainer claim, High]. Shared with:
- franken_threed: README L41 — "**zero speedup has been measured**" [Maintainer claim, High].
- franken_manim: "performance at the promised scale is **unproven**."
- franken_lean: README states "0 bench targets."
- franken_ocr: "the headline speedups rest on maintainer hardware" [Maintainer claim, Medium].
- The incumbent-ratio gap: franken_networkx's self-audit found "579 of 591 KEEP rows carry no vs-incumbent ratio" [Code-verified, High]; frankenpandas's audit found performance claims "97% non-contract" [Maintainer claim, High]; franken_whisper's packet notes "the 2.99× flagship leans on an algorithmic lever (ToMe token merging) the incumbent structurally cannot express."

**P6 — Doctrine-vs-tree contradictions (the "lockfile contradicts the badge" class).** frankengraphdb's "No serde. Ever." vs 204 registry crates in its own `Cargo.lock` [Counted, High] is the sharpest instance of a shared pattern:
- franken_code_browser: "asupersync has **zero** Cargo edges anywhere in the tree" despite being a named foundation; franken_markdown is a local-path dev dep (`../../../franken_markdown`) "that the constitution itself bars from release" [Code-verified, High].
- franken_surveillance: "the declared sole async runtime (asupersync) is absent from `Cargo.lock` and the tree contains zero `async fn`" [Code-verified, High].
- franken_sympy: "the fresh clone does not build standalone (path deps on `../asupersync`, `../franken_numpy-pin`, `../frankenscipy-pin` are absent)" [Code-verified, High].
- franken_node: "no CI workflow has ever compiled the `asupersync-transport` feature" despite spec contracts marked "Delivered" [Code-verified, High].

---

## 3. CI/EVIDENCE BAR — what frankengraphdb demonstrates that other packets lack

*Honest scope note: the enforcement currency of several of these practices is unestablished at the pin — the gate chain's authority rests on CI runs up to 2026-09-03 and the Sep-08 local proof, 14 days stale at the pin [Maintainer claim, Medium]. What the other packets lack is the* mechanisms*, which graphdb demonstrates; whether the mechanisms bind the pin's tree is the packet's own open question 2.*

1. **Four machine-readable registries + a std-only enforcement tool + a ledger-control test.** `registries/invariants.toml` (exactly 20 IDs `FG-INV-01`–`FG-INV-20` [Counted, High]), `laws.toml`, `claims_lint.toml`, `unsafe_boundary_ledger.toml`, enforced by `tools/registry-check` (std-only, 84,136 src lines [Counted, High]), with a `claims_enforcement_ledger_control` test asserting the exact key set of enforced clauses — demoting a live clause fails loudly [Maintainer claim, High]. Closest others lack the stack: franken_engine has a 28-claim claim-to-proof matrix (13 OBSERVED / 8 TARGETED / 7 HYPOTHESIS) that "gates README wording in CI" [Counted, High] — prose-level, no machine registries; franken_markdown has `claims.tsv` + `check-claim-discipline.sh` (claim → capability-key → proof-script wiring); franken_node has an executable honesty manifest (`check_claims_manifest.py --check-honesty`: "9 checks ok, 0 drifted" [Verified, High]); franken_lean has a claim matrix with evidence states and a CI-enforced unsafe-site ledger ("the bidirectional unsafe-ledger census passed CI at the pin" [CI-observed, High]) but no enforced-clause ledger-control test. No other packet shows the four-registry + ledger-control-test combination.
2. **The 19-row promise-vs-reality bridge table** (`REALITY_CHECK_AND_BRIDGE_PLAN.md`) with per-row verdicts (Partial / Not integrated) and the changelog-disciplined bridge rows [Code-verified, High]. Closest: franken_manim's `docs/IMPLEMENTATION_STATUS.md` reality-check audit (2026-09-09) and franken_engine's TARGETED matrix — neither is a row-level promise→reality verdict table keyed to a plan's gates.
3. **Verifier-found overclaim residues → mutation guards, documented in the changelog** (2026-09-04 sequence: a test that "silently skipped its Timestamp arm," plus the `overlapping_prepared_batches_abort_the_second_committer` bound tests with mutation controls) [Maintainer claim, High]. Closest: franken_alignment's planted-mutation testing ("defects deliberately introduced, required to fail after controls pass" [Code-verified, High]) — planted defects vs *real verifier-found overclaims* is the distinguishing axis.
4. **Six standing benchmark laws as CI-binding discipline** (no benchmark-only semantics; distributions not averages; never hide compaction; memory is first-class; adaptive numbers disclose policy epoch; no unpriced protocol weight) plus complexity witnesses that "fail CI on op-count regression" and plan certificates with byte-for-byte `replay(certificate, seq, seed)` closure [Maintainer claim, Medium]. Closest: franken_whisper's campaign-win doctrine (same-invocation incumbent binary, A/A nulls in [0.98, 1.02]); frankenmermaid's incumbent-arm contract (mandatory A/A nulls, ELF-SHA-256 provenance). Neither has the named six-law discipline, the op-count regression witnesses, or the replay-closure certificate machinery.
5. **The de-automation → local-proof verdict contract.** After the 2026-09-03 ruling, the workflow comment names `scripts/local_proof.sh` on the exact tree as the verdict and `/dsr` as the release path, with `docs/LOCAL_PROOF_BUNDLE.md` as the bundle-verifier record [CI-observed + Maintainer claim, High]. Other de-automators lack the bundle record: franken_overlap validates on "owner-controlled machines" via `scripts/ci-local.sh` (no bundle verifier, no pin record [Code-verified, High]); franken_ocr's CI was simply deleted [Verified, High]; frankentui's DSR runs are "invisible to outsiders." GraphDB's packet goes furthest in *specifying* the replacement contract (its own next step 4: formalize "pin + local_proof.sh exit code + bundle verifier exit code" as shipped-state evidence).

---

## 4. SHARED HURDLES

- **Bus factor 1 with daily agent-scale velocity, unobservable review depth.** frankengraphdb: ~2 months from repo creation (2026-07-15) to 735,929 lines, 986 beads (709 closed, 264 open) [Git-observed + Counted, High]; "Commit velocity is high and daily; review depth is unobservable (solo)." Shared: franken_code_browser ("206 commits across 2026-09-17→22, peaking at 101 commits on 2026-09-17" [Git-observed, High]); frankenmermaid ("~3,282 commits in ~7 months" [Verified, High]); frankenpandas ("8,621 commits" [External, High]); frankentorch ("6,305 commits since 2026-02-13" [Git-observed, High]); franken_engine ("~20 commits/day Sep 1–20, 96 on Sep 15 alone" [Git-observed, High]). The packet's governance reading: "the machinery's maintenance cost exceeds one person's slack" [Inference, Medium].
- **The rider as advancement blocker** (see P1) — near-universal; the program's term is "Explore-with-a-ceiling" and it appears in the verdict of frankengraphdb, franken_engine, franken_code_browser, franken_node, franken_lean, frankentui, frankenmermaid, frankenpandas, franken_numpy, franken_whisper, frankenscipy, frankenjax, and frankensearch.
- **README drift** (see P4) — the class the Rulebook demands be reported as a governance signal; graphdb's lockfile contradiction is its instance.
- **Release artifacts lagging or absent.** frankengraphdb: 0 releases, 0 tagged commits [External, High]. Siblings show the adjacent failure mode — artifacts pinned to earlier commits than the assessed tree: franken_networkx ("newest release (`v0.2.2`, 2026-09-12, 23 signed assets) targets an earlier commit while PyPI still serves 0.2.1" [External, High]); frankenpandas ("the `v0.3.0` release tags … pointing at earlier commit `0ad22f91` … no release artifact for the assessed commit" [External, High]); franken_whisper ("the latest release is five weeks behind HEAD"); franken_tts ("CHANGELOG scope window 4 weeks stale at HEAD").
- **Heavyweight gate chains un-runnable by third parties.** frankengraphdb's full `check.sh` chain "historically ENOSPC'd a standard GitHub runner (~29 min into compilation; 18 red runs, zero completed verdicts per the workflow's own comment)" [Maintainer claim, High] — one structural cause of the de-automation. Shared with franken_alignment ("the gates require the maintainer's private RCH fleet so no independent party can reproduce them" [Inference, Medium]); asupersync ("every behavioral claim about the assessed tree rests on maintainer-run DSR gates, unobservable externally"); franken_nlp (DSR build authority BLOCKED, Actions disabled).

---

## 5. GENUINE UNIQUENESS — only things no other packet does

1. **The only property-graph database in the 44.** Corpus grep: "graph database" and Cypher/GQL appear in frankengraphdb alone; franken_networkx reimplements graph *algorithms* (NetworkX), not a database. The technical composition thesis — MVCC/time-travel/branches/replication unified in a single fountain-coded commit stream ("One Version Universe" / Chronicle), graph-structured LSM (Strata), factorized/WCOJ execution (Loom), DBSP-style incrementality (Ripple), deterministic certificate-carrying query results — is suite-unique.
2. **The four-registry claim-governance stack with enforced-clause ledger control.** (Overlaps honestly: franken_engine's prose claim matrix, franken_markdown's claims.tsv, franken_node's honesty manifest, franken_lean's unsafe census all do *parts*.) The combination — invariants + laws + claims-lint + unsafe-boundary registries, a std-only 84k-line checker, and a ledger-control test that fails loudly if a live clause is demoted — appears in no other packet.
3. **The 19-row promise→reality bridge table as the governance anchor** ("the single most load-bearing governance document in the repo" [Code-verified, High]). franken_manim's IMPLEMENTATION_STATUS audit and franken_engine's TARGETED matrix are adjacent, not equivalent.
4. **The self-negating-doctrine ledger entry.** `docs/NEGATIVE_EVIDENCE.md` records that the doctrine clause it enforces is keyed to an event the project has never performed: of 705 commits measured, exactly **1** was produced by `git revert`, **3** had revert semantics — and **0** reverted a doctrine violation, because AGENTS.md's Backwards Compatibility doctrine forbids the mechanism the clause requires. "The doctrine is self-negating, the ledger caught it, and the project published the finding rather than deleting the clause" [Maintainer claim, High — census is the maintainer's count].
5. **Six standing benchmark laws + complexity-witness op-count regression locks + plan certificates with byte-for-byte replay closure** [Maintainer claim, Medium]. (Overlaps: franken_whisper's campaign-win doctrine, frankenmermaid's incumbent-arm contract.) The named six-law discipline and the op-count witnesses are unique.
6. **`deny.toml` binding the whole FrankenSuite foundation chain to `LicenseRef-MIT-OpenAI-Anthropic-Rider` by license-text hash** [Code-verified, High] — the rider propagated as machine dependency policy, not prose. No other packet shows this mechanism.
7. **Test-to-code investment ratio.** 43% of the crates tree is tests (262,464 of 608,825 lines); the `fgdb` composition root's tests (117,382) are ~2× its source; the claim-governance *tool itself* is 84,136 src + 42,968 test lines [all Counted, High] — "the strongest test-to-code investment signal in the FrankenSuite" [Inference, Medium — the packet's comparative claim].
8. **The externally verified "why now."** Apple's October 2025 acquisition of Kùzu and the 2025-10-10 archival of the upstream repo — "the only open embedded engine with a comparable query architecture has been removed from the commons by an acquirer" [External, High] — is the strongest externally verified market-timing fact in the Suite, and it belongs to this packet alone.

---

## 6. CROSS-POLLINATION IN — concepts other packets should be applied to frankengraphdb

1. **Campaign-win measurement doctrine** → *origin: franken_whisper* → *why it fits:* frankengraphdb's next step 2 is "run the first honest benchmark of the *shipped* subset" (bounded GQL slice vs LDBC SNB SF-1) — exactly the campaign moment whisper's doctrine was built for: "the actual incumbent binary (SHA-256-pinned) ran side-by-side in the same invocation, with seven-round A/A nulls inside [0.98, 1.02], full host provenance … and WER-gated transcript equivalence" [Maintainer claim, High — whisper packet]. → *expected payoff:* the §17 provisional gates become falsifiable campaign measurements; prevents the self-speedup creep franken_networkx's own audit caught (579/591 KEEP rows with no incumbent ratio [Code-verified, High]).
2. **Claim-coverage audit (machine-readable KEEP-claim → live-evidence ratio)** → *origin: frankenmermaid* → *why it fits:* graphdb has 12 §17 gates, 20 registered invariants, 19 bridge rows, and a claims_lint registry — mermaid's audit "reports 96.4% of its own KEEP claims lack a same-invocation incumbent ratio" (225 KEEP, 8 with a live ratio) [Verified, High]. → *expected payoff:* the bridge table's Partial/Not-integrated verdicts become a countable, CI-gatable coverage metric — the tense-note disclaimer becomes a number, not a paragraph.
3. **Cargo-tree admissibility gate (build-failing forbidden-crate policy)** → *origin: franken_snowflake* → *why it fits:* snowflake enforces "a cargo-tree admissibility gate that fails the build if a forbidden crate appears in any of the ~21 feature lanes" [Verified, High]; graphdb's "No serde. Ever." badge is contradicted by its own lockfile because the policy is prose, not policy — serde arrives transitively via pinned asupersync [Counted, High]. → *expected payoff:* the closed-universe doctrine becomes machine-enforced on the *built graph* (direct + transitive), and the direct-vs-transitive distinction the packet defends becomes an explicit, gated carve-out rather than a badge-reader trap.
4. **Executable honesty manifest with live recounts** → *origin: franken_node* → *why it fits:* node "executed `scripts/check_claims_manifest.py --check-honesty` against the pinned tree — 9 checks ok, 0 drifted, with live recounts (3,756 → 4,106 integration tests; 21,621 → 22,421 inline; … 0 unsafe blocks) inside the manifest's tolerance bands" [Verified, High]. graphdb's own history includes a round-4 recount that corrected a 1.2M-line double-count [Maintainer claim, High] — the manifest pattern operationalizes graphdb's next step 4 ("pin + local_proof.sh exit code + bundle verifier exit code" as shipped-state evidence). → *expected payoff:* the pin-record verdict contract becomes independently re-runnable by any analyst, ending the trust-me fleet gap that is the packet's own open question 1 ("Does `scripts/local_proof.sh` exit 0 at the pin?").
5. **Sigstore-signed release attestation bundles** → *origin: frankenterm* → *why it fits:* term wraps releases in "content-addressed, sigstore-signed release attestation bundles" plus "explicit `skipped_not_proven` qualification gates" [Maintainer claim, High — term packet TL;DR]. graphdb's deepening question 1 asks for artifact provenance (signed tags or a checksum manifest) that "doesn't exist yet" — currently "artifact attestation today is 'the git hash the maintainer pushed'" [Maintainer claim + Code-verified, Medium]. → *expected payoff:* when graphdb cuts its first release, attestation bundles close the claim-provenance vs artifact-provenance gap and give the registries something to bind *to*.

---

## 7. CROSS-POLLINATION OUT — concepts from frankengraphdb to export to named projects

1. **registry-check + the four-registry pattern (invariants / laws / claims-lint / unsafe-boundary) + the `expected_enforced_clauses` ledger-control test** → *targets: frankenpandas, franken_engine, franken_lean* → *why it fits them:* frankenpandas's own KEEP-claim audit found only 22/751 claims contract-grade [Maintainer claim, High] — it needs a live-vs-stub claim registry with bound checkers; franken_engine's 28-claim matrix gates README wording in CI but is prose-level (no machine registries) [Counted, High]; franken_lean has the claim matrix and CI-enforced unsafe census but no clause-demotion tripwire. → *expected payoff:* claim demotion becomes machine-enforced across the Suite — the program's intake-time screen (graphdb's next step 1: "require every project's claim inventory to be machine-registered and check-bound").
2. **The 19-row promise→reality bridge table** → *targets: franken_manim, franken_nlp, franken_surveillance, franken_alignment* → *why it fits them:* franken_manim's README is a present-tense spec with convergence gates G2–G5 open [Maintainer claim]; franken_nlp's README states a "disclosed 1.0 target contract" [Maintainer claim, Medium] while nothing executes; franken_surveillance's README disclaims the product "does not yet exist as a product" [Maintainer claim, High]; franken_alignment's production broker/adapters land across gates G1–G6, all `not_implemented` at the pin [Code-verified, High]. → *expected payoff:* converts tense disclaimers into auditable verdict rows — exactly the discipline each packet's verdict already asks for.
3. **Negative-evidence ledger as doctrine-violation memorial** → *targets: franken_code_browser, franken_alignment, franken_node* → *why it fits them:* franken_code_browser's AGENTS.md already names "demo hardcoding," "proof laundering," "refusal farming," and "commit pumping" as reopenable defects [Code-verified, High] — a ledger would record actual violations the way graphdb's recorded the self-negating doctrine (705-commit census, 1 revert, 0 doctrine-violation reverts); franken_alignment's concordance-checked docs are the natural home for doctrine-violation memorials; franken_node's honesty machinery is the closest sibling. → *expected payoff:* constitutional doctrine becomes falsifiable in-repo — the ledger catches the clauses the doctrine cannot see.
4. **Six standing benchmark laws + complexity-witness op-count regression locks** → *targets: frankenmermaid, franken_whisper, franken_networkx, frankentorch* → *why it fits them:* frankenmermaid's perf ledger already uses mandatory A/A nulls + ELF-SHA-256 provenance [Verified, High]; franken_whisper has the campaign-win doctrine; franken_networkx runs the Gauntlet head-to-head campaign [Maintainer claim, High]; frankentorch's measurement-integrity gate is "name the worker, name the harness, replicate before quoting" [Maintainer claim, High] — all four have the *habit*, graphdb's laws give them the *code*. → *expected payoff:* benchmark governance graduates from ledger prose to CI-enforced law; the six laws (distributions not averages; never hide compaction; memory is first-class; …) close the specific gaps each packet's audit found (96.4% without incumbent ratios; 97% non-contract).
5. **deny.toml rider-propagation by license-text hash + the rider-cost intake rule** → *targets: the FrankenSuite program itself* → *why it fits:* graphdb's §4.8 shows the rider "propagates through the whole FrankenSuite dependency chain (asupersync, fnx-*, franken-* foundations all bound to `LicenseRef-MIT-OpenAI-Anthropic-Rider` by license hash)" [Code-verified, High]; graphdb's next step 5 proposes the intake rule verbatim: "any performance-claiming dependency candidate carrying a named-party benchmarking restriction is flagged as unvalidatable-by-design" [Inference, High]. → *expected payoff:* the rider's cost — ~38 projects whose performance claims can never be independently validated — becomes program machine-policy instead of per-packet §4.8 prose.

---

## Caveats and honest "also does X" notes

- **Claim-governance is a Suite habit, not a graphdb invention:** franken_engine (claim-to-proof matrix), franken_lean (claim matrix + CI unsafe census), franken_markdown (claims.tsv + claim-discipline script), franken_node (executable honesty manifest), frankenterm (claims ledger + `skipped_not_proven`), frankenpandas (KEEP-claim audit) all do parts. graphdb's distinctiveness is the *stack* (four registries + std-only checker + ledger-control test), not the idea.
- **Negative-evidence ledgers are the Suite's signature genre:** frankenredis (26,485 lines), franken_networkx (37,116 lines), franken_numpy (67,641 lines), franken_whisper (26,846 lines), franken_scipy (44,078 lines), franken_torch (42,708 lines), frankenmermaid (726 rows), franken_tts, frankenfs (20,556 lines), frankenlibc (41,205 lines), franken_overlap all maintain them. graphdb's 562-line ledger is small by comparison; its distinctiveness is the *doctrine-violation memorial* form, not the ledger.
- **Differential oracles are widespread:** frankenredis (5,041 probes vs vendored 7.2.4), franken_numpy (live NumPy oracle), franken_sqlite (bundled rusqlite), franken_sympy (230-fixture pinned 1.14.0 oracle), franken_threed (pinned three.js oracle, analyst re-executed 61/61 green). graphdb's `fgdb-reference` ("deliberately simple, single-threaded, obviously-correct") is *underused* — the packet's open question 3 asks whether it's used in differential testing today at all — so it is an IN candidate (import redis's differential-probe playbook), not an OUT one.
- **De-automated CI is a shared escape hatch:** graphdb's owner-ruling de-automation is matched or exceeded by franken_ocr (deleted CI), franken_overlap (intentionally disabled), franken_networkx (AGENTS.md vs Actions split), frankentui (DSR-only), frankenjax (zero workflows), franken_nlp (disabled), franken_whisper (no CI config), franken_code_browser (no workflows), frankensim_website / franken_markdown_website / frankentui_website (no CI). graphdb's edge is the *specified replacement contract* (local_proof.sh + bundle verifier), not the de-automation.
- **What this brief did not verify:** no packet claim was re-executed; all cross-packet quotes rest on the other packets' stated evidence tiers, which are reproduced faithfully but not independently audited here.
