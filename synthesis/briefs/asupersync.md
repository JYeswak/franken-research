# Cross-Suite Brief: asupersync

**Analyst role:** one of 44 cross-suite analysts · **Own packet:** `asupersync-assessment.md` (RULEBOOK v1.0, packet v6 — final production packet, 5 grade→revise rounds, QA 13/13, both graders PASS) · **Date:** 2026-09-22
**Method:** own packet read in full (all sections incl. claim inventory, benchmark audit, deepening questions); all 43 other `*-assessment.md` packets skimmed via TL;DR / verdict / TRL / NODUS ring / claim inventory / benchmark / limitations / license-governance sections, with targeted greps for mechanisms (benchmark gates, drift gates, unsafe ledgers, admissibility gates, honesty manifests). All facts below are packet-grounded; evidence tiers are the packets' own. Tier flavors: [Counted]/[Git-observed]/[Code-verified]/[CI-observed] (direct inspection), [Maintainer claim], [External], [Inference]; [Verified absence] for the grep-disproven kind.

---

## 1. OWN-PACKET DISTILLATION

**Verdict:** "the only FrankenSuite project with genuine registry-scale distribution and real downstream workloads — a cancel-correct async runtime whose honest-scope documentation culture is its most exportable asset — governed by one person, unobservable CI at the assessed commit, and a license that bars the AI labs from evaluating it." **NODUS ring: Pilot** [Inference, Medium].

**TRL: 6** — "Ships as 0.5.0 on crates.io with 20 GitHub releases and runs real downstream workloads (frankensqlite storage I/O, frankensearch core) — demonstrated in a relevant environment, but no independent qualification exists."

**Strongest strength:** the evidence/honesty machinery, not the runtime. The CI-blocking benchmark baseline gate (`artifacts/baseline.json`, 105 rows at the pin, fail above max(5%, ci95 envelope, +0.6ns), flamegraph artifacts, golden checksums, and "The infrastructure intentionally has no `[skip ci]`-style waiver" — README L2489 [Maintainer claim, High]); the honest-status table genre (✅/⚠️ with prose on exactly what is missing [Maintainer claim, High]); the reactor export contract ("Historical files such as `src/runtime/reactor/uring.rs` … are not part of the live export graph" — README L642–644 [Maintainer claim + Code-verified, High]); and the deterministic lab runtime with e-process anytime-valid oracles (`src/lab/oracle/eprocess.rs`, Ville's inequality [Code-verified, High]). The packet's own verdict: "The methodology machinery (claim-discipline, CI-blocking baseline gates, honest-status tables) is the exportable artifact even if the runtime itself is never adopted outside the constellation."

**Strongest ceiling:** the institutional triad, one mechanism not three problems — bus factor 1 with outside contributions explicitly refused (README L2620: "I do not accept outside contributions for any of my projects… it's my name on the thing" [Maintainer claim, High]); the non-OSI rider that withholds even *benchmarking/analyzing* rights from OpenAI and Anthropic [Code-verified (license text), High]; and CI that has never once run against the assessed commit (zero workflow runs for `head_sha=<pin>`; latest runs 2026-09-07 show the tokio-parity gate failing at drift-detection and the Benchmarks workflow 1-of-4 failed [CI-observed, High]). "For a project whose pitch is *evidence*, the evidence for this commit is take-my-word."

**The 3 most important pieces of evidence (with tiers):**

1. **Registry-scale distribution, third-party-measured:** crates.io `asupersync` 0.5.0 — 318,241 total downloads, 166,388 in the trailing 90 days, 78 reverse-dep crate versions returned by the endpoint [External, High — "the strongest third-party distribution signal in the FrankenSuite"]. 20 GitHub releases (v0.5.0, 2026-09-12, 11 signed multi-platform assets published via the maintainer's DSR pipeline, not GitHub Actions) [External, High on releases; Maintainer claim, High on DSR].
2. **Zero CI at the pin, red CI nearby:** Actions API returns zero runs for `head_sha` = pin `768595203e194573bb713158b5963f55861dfe2d`; the two latest runs (2026-09-07, older commits) fail — the tokio-parity gate at its drift-detection step, the Benchmarks workflow with 1 of 4 jobs failed ("Performance Benchmarks") [CI-observed, High]. "Every behavioral and benchmark claim about the assessed tree therefore rests on maintainer-run DSR gates whose execution is unobservable externally."
3. **Real, manifest-verified downstream workloads inside the constellation:** frankensqlite runs storage I/O on asupersync 0.5.0 with `Cx` in its public APIs (frankensqlite `Cargo.toml:213` + `Cargo.lock`, registry source with checksum [Code-verified, High]); frankensearch mandates 0.5.0 and forbids tokio ("Async runtime (structured concurrency, NO tokio)" — root Cargo.toml comment [Code-verified, High]); franken_networkx optionally depends (`fnx-runtime` behind `asupersync-integration` feature [Code-verified, High]). Counterweight inside the same evidence tier: frankenredis *evaluated and rejected* asupersync as an architectural rewrite (2026-04-21/22) [External, Medium].

---

## 2. SHARED NEGATIVE PATTERNS

Patterns asupersync shares with named other projects, with the packet evidence showing the sharing.

**Pattern A — The non-OSI OpenAI/Anthropic rider (benchmarking/analysis withheld from the AI labs).** asupersync's LICENSE (73 lines, read verbatim) is the FrankenSuite standard rider: "no rights are granted to any Restricted Party" (OpenAI, L.L.C.; Anthropic, PBC; affiliates; anyone acting for them), where "'use' includes, without limitation: copying, modifying… executing, benchmarking, testing, analyzing, indexing, or incorporating the Software… into any dataset, training corpus, evaluation harness, or pipeline" — breach "automatically and immediately terminates" the license [Code-verified (license text), High]; classified non-OSI, "non-open-source by the Open Source Definition" [Inference, High]. **Named sharers (rider mentioned in-packet; "rider" grep counts):** frankenredis (33 mentions — "bars OpenAI/Anthropic and anyone acting for them from using, benchmarking, or even analyzing the code"); frankenpandas (34 — "bars OpenAI/Anthropic (and anyone acting for them) from use/benchmarking/analysis and demands destroyed copies on breach"); franken_engine (32); frankensearch (30); franken_node (29); franken_numpy (29); frankentorch (28); franken_ocr ("excludes the frontier AI labs from even benchmarking it"); franken_lean ("bars the AI labs most able to validate it from even benchmarking it"); franken_manim ("withholds even benchmarking rights from the two leading AI labs"); franken_markdown ("withholds even *benchmarking and analysis* rights from the two AI labs the MCP server is built to serve"); franken_tts; franken_whisper; frankensqlite; frankenterm; frankentui; franken_snowflake; franken_threed ("the MIT+OpenAI/Anthropic rider is non-OSI and excludes the likeliest validators"); frankenscipy; frankensympy; frankenfs; frankengit; frankengraphdb; frankenjax; frankenlibc; frankenmermaid; franken_alignment; franken_code_browser; franken_drone_geometry_reconstruction; franken_remote; franken_surveillance_system; franken_overlap ("withholds all rights — including benchmarking and analysis — from the two leading AI labs"); franken_nlp. **Exceptions (do NOT share):** franken_agent_detection — "License (plain MIT, 21 lines, no rider)" [Code-verified, High]; franken_native_capsule — no license text in tree, one squashed commit, no CI ("A trust boundary with no trustor is a demo"); the beads/frankensim_website/frankensqlite_website repos — no LICENSE file at all (default copyright). asupersync's distinction within the pattern: its packet names the rider's strategic cost most explicitly — "it trades unmeasurable training-data protection for the certain loss of the most capable potential validators and distributors" [Inference, Medium].

**Pattern B — Bus factor 1 with an explicit, non-negotiable no-outside-contributions policy.** asupersync: README L2620 verbatim, "I do not accept outside contributions for any of my projects… it's my name on the thing" [Maintainer claim, High]; "all visible authorship is the maintainer" [Maintainer claim, High]. **Named sharers:** franken_agent_detection ("bus factor 1 with an explicit no-outside-contributions policy" [Maintainer claim, High]); franken_numpy ("explicit no-outside-contributions policy"); franken_manim ("bus factor 1 with outside contributions explicitly refused"); frankenjax ("One human maintainer with an explicit no-outside-contributions policy"); franken_nlp ("bus factor is 1 *by stated policy* ('I do not accept outside contributions')"); frankenpandas ("the README refuses outside contributions"); franken_ocr ("a single maintainer who explicitly accepts no outside contributions"); frankenpandas/frankenredis/frankentorch ("one human maintainer" + agent swarm); franken_code_browser ("Single human maintainer (Jeffrey Emanuel)"); franken_lean ("one maintainer who refuses outside contributions"). Effectively the entire 44 shares this. asupersync's distinction: the single-node cut set is *load-bearing* — "If the maintainer stops, 78 reverse deps lose their substrate's steward on the same day" [Inference, High] — and frankensympy makes the fragility concrete: "the fresh clone does not build standalone (path deps on `../asupersync`… are absent)" [Code-verified, High].

**Pattern C — CI red or absent at the pin (the "green suite" is maintainer-local or nonexistent).** asupersync: zero workflow runs at the pin; latest runs (2026-09-07) fail [CI-observed, High]. **Named sharers:** franken_agent_detection — "the main CI job is red at the pin — the `Format` step fails, so clippy/tests/packaging never executed on the assessed commit" [CI-observed, High]; franken_engine — "run #35550128941 (head = pin) failed all three jobs on test-execution steps, and all 46 recorded runs of the workflow (Sep 16–21) fail — it has never gone green" [CI-observed, High]; franken_node — "all 8 push-triggered runs observed against the pin's head SHA concluded failure" [CI-observed, High]; franken_numpy — "the latest run (head = pin) fails at the G1 `cargo clippy -- -D warnings` gate with G2–G9 all skipped… while the README badge still reads 'G1 green'" [CI-observed, High]; frankenredis — "CI is red at the pin — conformance run #7679 failed"; frankentorch — "the flagship `phase2c-reliability-gates` CI run against the pin failed in ~55 seconds" [CI-observed, High]; frankensympy — "the build-and-test gate has failed 786 consecutive CI runs (last green 2026-08-23; pin fails at the fmt gate, unit tests never run)" [CI-observed, High]; franken_drone_geometry_reconstruction — "the sole CI workflow is self-hosted and all 324 runs ever observed are queued/cancelled — including the run at the pin — so nothing has ever been shown green anywhere" [External, High]; franken_ocr — "the CI workflows were deleted from the tree at HEAD and the last runs (2026-09-03) failed, while the docs still describe CI as the release gate"; franken_remote — "the pin has zero registered check-runs; recent `rust-verification.yml` runs at earlier SHAs failed" [CI-observed, Medium]; frankenpandas — "the live-oracle conformance job and `--all-features` lint are red" [CI-observed, High]; franken_surveillance_system — "CI history across all 622 recorded runs shows zero successes… the pin's run sitting 'queued' against self-hosted runners" [CI-observed, High]. **Counter-cases (green at pin, which asupersync lacks):** frankenscipy — "CI Gates G1–G9 green at the pinned commit"; franken_threed — "one GitHub Actions workflow (green at the pin)" [CI-observed, High]; franken_lean — the unsafe-ledger census "passed CI at the pin" [CI-observed, High]. The pattern's asupersync flavor: GitHub Actions is the *display window* — "the gates that make the methodology credible run on the maintainer's DSR — the same hosts whose ambient noise the gate's +0.6ns floor is calibrated against — so the discipline is self-hosted end to end" [Inference, High].

**Pattern D — README/status drift (claims outrunning the tree).** asupersync's own drift audit found two instances: the ecosystem tagline disproven (claim 5) and "`artifacts/baseline.json`'s top-level `timestamp` field reads 2026-02-13 while its own `note` fields record re-recordings on 2026-07-27, 2026-08-02, and 2026-08-03 — a stale metadata field inside the project's most important evidence artifact" [Code-verified, High]. **Named sharers:** franken_agent_detection — README documents only the zero-feature detection API while "~90% of the code (the connector zoo) goes unmentioned, and its 'Limitations' line ('no session parsing or indexing') is flatly contradicted by the code" [Code-verified, High]; franken_networkx — "the README is a drift source (13-variant tie-break → 12 counted; 33 fuzz targets → 34 counted; delegation-ledger table 61/71/1113/92/338 → generated 31/150/1112/114/346…)" [Counted, High]; frankenjax — "The README contradicts itself and the code on the most basic facts (118 vs 162 primitives, 15 vs 17 crates, 162,733 vs 459,703 lines)" [Counted, High]; frankensympy — "the README's 'green workspace tests' line is stale-by-a-month" [Code-verified + CI-observed, High]; franken_drone_geometry_reconstruction — "README/IMPLEMENTATION_STATUS both claim '28 members' while the workspace has 30 (drift in the project's auditability pitch)" [Counted, High]; frankentui_website — hero-stat drift inventory ("60 fps via WASM + WebGPU" with "no measurement methodology, numbers, or trace"; "12 Workspace Crates" vs the kernel's 20 members; "'V0.1.1 Alive on Crates.io' is wrong twice over") [Verified stale, High]; frankensqlite_website — "26-crate workspace" vs 28 counted, "stale, and the staleness is enforced by the site's own tests (`expect(crates).toHaveLength(26)`)"; "0 unsafe blocks / pure safe Rust throughout" vs "41 counted unsafe blocks in `fsqlite-vfs`… — disproven as stated" [Verified, High]; franken_markdown_website — "the Lab Notes honesty strip and the README's deploy section are two engine versions stale (0.3.5 claimed vs 0.4.2 shipped vs 0.4.1 live)" [Verified, High]; franken_threed — "the public status surface is stale (README honest-status table still says 'Is there a compiler, renderer, or CLI? **No**' and lists 2 crates)" [Verified, High]. asupersync's distinction: it is one of the few packets that *ran its own drift audit* and published the two hits — the honesty about drift is the differentiator, not the absence of drift.

**Pattern E — Unmeasured or disavowed benchmark numbers (the suite's credibility wound).** asupersync's variant is gates-without-headlines: "the repo publishes almost no headline speedup claims — instead it publishes the *gate*" — but "the numbers it guards are maintainer-measured on maintainer hardware, and the README publishes almost no comparative figures (the ~27x timer-wheel cancel-path figure is dated 2026-06-01 and corpus-specific)" [Inference, Medium]. **Named sharers (stronger forms):** frankenredis — "The headline benchmark table is explicitly disavowed by its own README ('should be read as a contention-sandbox measurement, not a release claim' until a deferred re-baseline bead runs)" [Maintainer claim, High] — asupersync's packet calls this "the inverse of frankenredis's disavowed benchmark table: asupersync's numbers are gated but unpublished; frankenredis's are published but disavowed"; franken_networkx — "579 of 591 KEEP rows carry no vs-incumbent ratio — the headline speedups are overwhelmingly self-speedups, which the project's own ledger contract says 'must not support a competitive claim'" [Code-verified, High]; franken_numpy — "729 of 751 kept performance claims predate the evidence contract and lack same-invocation incumbent ratios" [Maintainer claim, High]; frankenpandas — "The 3.97x geomean covers *certified* lanes only — 143 of 359 (216 uncertified: 74 undecidable, 62 high-CV, 78 read-but-uncertified)" and "sub-1.5x ratios UNRESOLVED after measuring 2.6x same-source build variance" [Code-verified, High]; franken_threed — "no measured speedup anywhere — README L41: '**zero speedup has been measured**'" [Maintainer claim, High]; franken_markdown — "'ultra-fast' is an unmeasured word — zero published head-to-head numbers vs pulldown-cmark/comrak"; franken_nlp — a benchmark section that "refuses to contain benchmarks" by written policy ("no FrankenNLP performance number exists yet, because no kernel exists yet") [Maintainer claim, High]; franken_surveillance_system — "`docs/PERF_LEDGER.md` states verbatim 'No runtime exists yet, so this ledger contains no performance wins'". Within this pattern asupersync sits at the honest pole — but the pole's cost is that "the thesis's price tag [is] unknown": "the numbers load-bearing for the thesis — 'cancel-correctness costs X' — are never stated as such" [Inference, Medium].

**Pattern F — Private verification infrastructure (DSR / RCH / self-hosted) as the real gate.** asupersync: gates run on DSR — "the maintainer's private orchestration, never expanded in-repo, unobservable externally" — with releases "published via DSR, not GitHub Actions" [Maintainer claim, High; External, High on releases]. **Named sharers:** franken_alignment — "the gates require the maintainer's private RCH fleet so no independent party can reproduce them" [Inference, Medium]; frankenjax — "every 'green' claim executes on the maintainer's local RCH fleet and is attested only by checked-in artifact JSON" [External + Inference, High]; franken_manim — builds done "natively with DSR … without GitHub Actions" (v0.1.0 notes) [Verified on the releases page, High]; franken_markdown — release workflow "DISABLED — releases use DSR exclusively", `if: ${{ false }}` [Code-verified, High]; franken_nlp — "the DSR build authority is BLOCKED, GitHub Actions is disabled" [Code-verified + Maintainer claim, High]; franken_overlap — "validation is owner-local via `scripts/ci-local.sh`"; "GitHub Actions is deliberately disabled" [Code-verified, High]; franken_drone_geometry_reconstruction — "The sole CI workflow is self-hosted and all 324 runs ever observed are queued/cancelled"; franken_surveillance_system — the qualification docs "explicitly demote GitHub CI to 'portable supplementary specification'"; franken_ocr — CI workflows deleted while "the docs still describe CI as the release gate". This is arguably *the* FrankenSuite-wide structural pattern: GitHub Actions as display window, private fleet as authority.

---

## 3. CI/EVIDENCE BAR: WHAT ASUPERSYNC DEMONSTRATES THAT OTHERS LACK

Careful scope note: asupersync demonstrates *committed in-tree methodology machinery*, not executed CI at the pin (zero runs). The practices below are what no other packet documents in the same form.

1. **A statistically-specified, waiver-free benchmark-regression gate with committed baselines.** Mechanism: the `methodology_baselines` signoff contract + `artifacts/baseline.json` (105 rows at the pin, per-row environment tagging and layout-hypersensitivity notes — e.g. "the ovh-a host re-recording saga of 2026-08-03") + a failure threshold of max(5%, recorded ci95 envelope, +0.6ns absolute) + flamegraph artifacts required for scheduler-adjacent changes + golden checksums + "The infrastructure intentionally has no `[skip ci]`-style waiver" (README L2489) [Maintainer claim, High]. **Who lacks it:** franken_engine has a `perf_regression_gate.yml` with **"zero runs ever"** [CI-observed, High] — the workflow exists and has never fired; frankenredis has a `benchmark_gate` script but its headline table is disavowed [Maintainer claim, High]; franken_networkx's `run_benchmark_gate.sh` and its Gauntlet are a one-off campaign (2026-07-25), not a committed blocking gate; frankentorch has `benchmark_regression`/`check_perf_slos`/`run_perf_slos` scripts of unobserved CI status; frankenpandas's "143-lane certified benchmark gate" is a *vs-incumbent certification* contract, a different mechanism (certify vs. incumbent, not regress vs. committed baseline). No other packet documents a regression-vs-committed-baseline contract with a statistical envelope, environment-tagged rows, and a no-waiver rule.
2. **A drift gate that demonstrably fired — on drifted artifacts, which is the gate working as designed.** Mechanism: `tokio_parity_dashboard_drift.yml` — "latest run (2026-09-07, commit 87856556) failed at the 'Hard-fail if dashboard artifacts drift' step… downstream steps skipped" [CI-observed, High]. **Who lacks it:** frankensympy's README "green workspace tests" line went stale a month with no gate firing [Code-verified + CI-observed, High]; the three website packets (frankentui_website, frankensqlite_website, frankensim_website) each carry hero-stat drift inventories with no drift gate between engine manifests and site copy; franken_markdown_website's deploy docs drifted two engine versions with nothing catching it [Verified, High]. Exception to note honestly: franken_node *has* a `readme-cli-drift-gate.yml` (red at the pin) — the drift-gate *pattern* is shared; asupersync's instance is distinguished by the documented firing.
3. **Methodology machinery extracted and published as registry-distributed crates.** Mechanism: `franken_kernel` / `franken_evidence` / `franken_decision` workspace members published to crates.io — `franken-kernel` 0.5.0 with 339,863 downloads [Code-verified + External, High] — "the evidence/tracing kernel already extracted… the methodology half-extracted." **Who lacks it:** franken_engine's claim-to-proof matrix (README wording gated in CI), franken_lean's B8 claim matrix + anchor-bound contracts, franken_alignment's receipt-bound qualification + planted-mutation testing, franken_node's Ed25519-signed honesty manifest, franken_markdown's `claims.tsv` + `check-claim-discipline.sh`, franken_networkx's five auto-generated ledgers — all are in-tree-only; none is a versioned, registry-published artifact. (asupersync's own `frankenlab` is still in-tree-only — 3 files, 2,132 lines — so the extraction is half-done, as the packet admits.)
4. **An explicit reactor/export-surface contract that names historical files as non-live.** Mechanism: README L642–644 export contract — "Historical files such as `src/runtime/reactor/uring.rs` … are not part of the live export graph" [Maintainer claim + Code-verified, High] — a machine-checkable genre of "what is not shipped." **Who lacks it:** franken_code_browser has two layering inversions of its documented architecture (the "facade" crate is "really a 31,916-line feature-gated integration monolith," the native shell routes through the *application* crate [Code-verified, High]) with no export-surface contract to adjudicate them; franken_manim carries 2 G0 spikes in-tree with no stated boundary; franken_ocr's docs describe a CI that no longer exists in the tree with no "not part of the live graph" accounting.
5. **What asupersync does NOT demonstrate (honest counterweight):** CI-executed evidence at the pin — franken_lean's "bidirectional unsafe-ledger census passed CI at the pin" [CI-observed, High], frankenscipy's "Gates G1–G9 green at the pinned commit," and franken_threed's "green at the pin" CI plus analyst-executed differential-oracle parity (61/61 tests re-run by the assessor [Verified — executed by analyst, High]) are all *executed* evidence practices asupersync's gates lack at the pin. The bar asupersync sets is machinery-specified; the bar it fails is machinery-executed.

---

## 4. SHARED HURDLES: STRUCTURAL ISSUES ASUPERSYNC SHARES

1. **Velocity vs. review depth (agent-assisted commit firehoses).** asupersync's own packet flags the honest README as "a drift vector: 2,626 lines of carefully scoped claims that rot fast at this velocity" [Inference, Medium], and notes "agent-assisted development velocity" as what makes a from-scratch runtime feasible for one person [Inference, Medium]. **Sharers:** franken_code_browser — "206 commits across 2026-09-17→22, peaking at 101 commits on 2026-09-17" [Git-observed, High]; franken_engine — "~20 commits/day Sep 1–20, 96 on Sep 15 alone" [Git-observed, High]; frankentorch — "6,305 commits since 2026-02-13" [Git-observed, High]; frankenpandas — "8,621 commits" [External, High]; franken_surveillance_system — "~20 commits across 2026-09-21/22" [Git-observed, High]; franken_overlap — "347 commits in 21 days" [Git-observed + External, High]. The extreme form is franken_threed — "AI-agent-authored at scale with opaque provenance" [Git-observed, High] — and franken_code_browser's "named agent personas" in commit bodies. The hurdle is structural: review depth cannot scale with agent commit velocity under a bus-factor-1 policy, and the drift pattern (Pattern D) is its symptom.
2. **Scope sprawl (one crate, several projects).** asupersync: "the breadth is the review: this is several projects wearing one crate name" — scheduler, reactors, TCP/HTTP/1–3/TLS/WebSocket, QUIC, DB clients, Kafka, actor supervision, RaptorQ, formal methods, browser edition; "66 entries in root `[dependencies]`… notably broad for a runtime" [Counted, High]; "each sub-lane thinner than its standalone competitor" [Inference, Medium]. **Sharers:** frankensqlite — 28 workspace crates, "seven extension crates (FTS5, JSON1, R-tree, ICU, session, misc, FTS3-helpers), a SQLite-compatible C ABI shim, an experimental WASM target"; frankenterm — "a WezTerm-derived terminal runtime… plus an agent-swarm control plane — Robot Mode JSON/TOON API…, MCP surface, policy-gated actions, transactional multi-pane missions…, BOCPD…, FTS5 + Tantivy hybrid search, flight recorder/replay forensics"; franken_whisper — "a 10-stage pipeline… and then **replaced the backends it wraps**"; frankensim — "a 2.1-million-line monument." The hurdle: every sub-lane inherits the same bus-factor-1 maintainer and the same rider ceiling.
3. **The rider as a uniform advancement ceiling ("Explore-with-a-ceiling" / "Pilot-with-a-ceiling").** asupersync: "Pilot-with-a-ceiling: advancement past Pilot needs outside-constellation adoption and a governance story" — the rider "excludes exactly the two labs building the agent infrastructure this runtime is best suited for" [Inference, Medium]. **Sharers:** the packets repeatedly price the rider into the ring: franken_lean ("Explore-with-a-ceiling — the ceiling is… the rider is an advancement blocker"); franken_code_browser ("the rider is an advancement blocker (Explore-with-a-ceiling), not a ring qualifier"); frankenpandas ("un-advanceable while the rider stands"); franken_node, franken_numpy ("Explore-with-a-ceiling until the rider moves"), frankenredis ("currently un-advanceable while the rider stands"), franken_engine ("the runtime itself is currently un-advanceable while the rider stands"), frankensqlite/frankensearch (Explore-with-a-ceiling). asupersync's packet adds the sharpest strategic formulation: "As strategy it reads as defensive moat-building around training-data incorporation; as ecosystem strategy it is self-sabotage" [Inference, Medium] — and notes "every downstream constellation crate inherits the same ceiling."
4. **Governance decay as a synchronized, not gradual, risk.** asupersync: "The single-node-cut-set dependency graph means decay is synchronized, not gradual: one person's context switch freezes the substrate under 78 reverse deps" [Inference, Medium]. **Sharers:** franken_ocr shows the decay signal in the wild — "a governance decay signal — the CI workflows were deleted from the tree at HEAD and the last runs (2026-09-03) failed, while the docs still describe CI as the release gate"; franken_drone_geometry_reconstruction — "development stopped 2026-09-04 with `WP-018` open"; the beads dashboards — "a 2026-09-21 batch commit reformatted three sibling dashboards' *code* — data untouched since March everywhere" [External, High]; franken_native_capsule — "no commit to the default branch in 33 days… A trust boundary with no trustor is a demo." The hurdle is that maintenance promises in the suite are personal, and personal promises decay synchronously.
5. **Unsafe-posture marketing vs. counted census.** asupersync: "`deny(unsafe_code)` at the root is real, but 47 files carry `#[allow(unsafe_code)]` — platform probes via `libc::fallocate`, x86_64 `__cpuid`, and others sampled, not all audited in this pass" [Code-verified, High on the count; Medium on the audit] — "'Deny with 47 islands' is a weaker posture than the root attribute suggests at a glance." **Sharers:** franken_code_browser — "The unsafe-code constitution is already violated in-tree"; frankentorch — 'the docs claim "unsafe forbidden" while the tree holds **116**' unsafe sites; frankensqlite — "0 unsafe blocks / pure safe Rust throughout" marketed vs "41 counted unsafe blocks in `fsqlite-vfs` plus the `fsqlite-c-api` FFI boundary — disproven as stated" [Verified, High]; franken_numpy — "10 of 11 crates `#![forbid(unsafe_code)]` with zero counted unsafe sites, the 642 remaining sites confined to the PyO3 boundary" (the honest variant — confined and counted [Counted, High]); franken_engine — "zero `unsafe` in library code (the only `unsafe` blocks in the tree are adversarial negative tests trying to forge IFC labels via `transmute`" [Code-verified, High] — the strongest variant); franken_node — "zero real `unsafe` sites under `#![forbid(unsafe_code)]` in all three workspace members" [Counted, High]; frankenmermaid — "all 9 crate roots carry `#![forbid(unsafe_code)]` and a code-level census finds zero actual `unsafe` tokens" [Verified, High]. The hurdle is the same everywhere: the root attribute is cheap, the island census is the real posture — and only the censuses that were independently re-counted (asupersync's 47, engine's zero, node's zero, mermaid's zero) carry weight.

---

## 5. GENUINE UNIQUENESS: STANDALONE ASUPERSYNC

Only things no other packet does. Anything merely shared is labeled as such.

1. **The only project with registry-scale distribution and real downstream workloads.** 318,241 crates.io downloads / 78 reverse-dep crate versions [External, High]. Nearest comparables are an order of magnitude or more below: franken_agent_detection (21,189 downloads [External, High]); frankensearch family (3,284 / 4,148 / 751 downloads); franken_numpy (crates.io "downloads top out at 96" [External, High]); franken_markdown (151 downloads). (The 339,863-download `franken-kernel` is asupersync's *own* workspace member, not a separate project — it strengthens, not dilutes, the point.) No other packet can evidence "release artifacts plus a bounded, real workload fit" from a third-party registry.
2. **The only async runtime in the 44 — and the only cancel-correctness-by-construction substrate.** Region-owned tasks closing to quiescence, the explicit request → drain → finalize cancellation protocol with budgeted bounds, capability-gated effects (`Cx`), the three-lane (cancel > timed > ready) work-stealing scheduler [Code-verified, High on structure]. §4.6: "a *cancel-correct-by-construction* runtime with deterministic replay testing and anytime-valid oracles has no direct occupant — tokio documents cancellation hazards rather than structuring them away" [Inference, Medium]. (Shared in spirit, not in substance: franken_engine has capability algebra at IR2/hostcall edges and an e-process guardplane — but it is a JS extension runtime, not an async task runtime; the scheduling/cancellation semantics differ.)
3. **The only substrate with both manifest-verified adoption AND a documented sibling rejection.** Adopted: frankensqlite (0.5.0, `Cx` in public APIs), frankensearch (mandated, tokio forbidden), franken_networkx (optional feature) [all Code-verified in manifests, High]. Rejected: frankenredis "evaluated asupersync and rejected it as an architectural rewrite (2026-04-21/22), choosing a hand-rolled mio loop" [External, Medium] — "the most damning data point, from inside the family." franken_engine is also a substrate (franken_node: "FrankenEngine is the core substrate"), but no packet records a sibling evaluating and walking away from it. The adoption+rejection pair — including franken_whisper's inversion ("asupersync was evaluated and explicitly rejected — here it is load-bearing production infrastructure") — is unique to asupersync.
4. **The only packet whose sharpest finding is a *verified absence* against the ecosystem's own marketing.** The brief's tagline ("zero-copy DMA and RDMA support," "work-conserving packet-aware scheduler," "epoll/uring/netlink") "does not survive contact with the tree — RDMA appears nowhere in 3,864,420 lines of Rust, netlink nowhere in `src/`, `docs/`, or the README, 'packet-aware' never in the maintainer's words" [Verified absence, High]. No other packet disproves the constellation's *description of the project* by exhaustive grep; the finding is a governance signal, not a code finding.
5. **The only project whose evidence machinery is already a published, registry-distributed crate.** `franken_kernel`/`franken_evidence`/`franken_decision` as crates.io artifacts (`franken-kernel` 0.5.0, 339,863 downloads) [Code-verified + External, High]. Every sibling methodology (engine's claim-to-proof matrix, lean's B8, alignment's planted-mutation testing, node's honesty manifest, markdown's claim-discipline script, networkx's ledgers) is in-tree-only.
6. **The tokio-compat bridge as an adoption wedge** (`asupersync-tokio-compat`: hyper/tower bridges, blocking, io, cancel, runtime shims; 9 files, 0.6.0, path-dep on root [Code-verified, High]) — "an explicit admission that the ecosystem moat must be crossed, not ignored" [Inference, Medium]. No other runtime exists in the suite, so no other packet has a compat-bridge strategy; the nearest analogs (frankensqlite's C ABI shim, frankensympy's Python shell, franken_node's compat corpus) are compatibility *surfaces*, not migration *bridges* with a drift gate behind them.

**Explicitly NOT unique (to avoid overclaiming):** Pilot ring — shared with franken_agent_detection and franken_ocr (both Pilot, TRL 6); TRL 6 — shared with franken_agent_detection, franken_ocr, franken_networkx, franken_snowflake; honesty-status tables — shared with franken_manim's reality-check audit, franken_drone's does-not-claim list, franken_nlp's disclosed surface, franken_node's honesty manifest; deny-unsafe-with-islands — shared with franken_engine/franken_node/franken_numpy/frankenredis (see §4.5); e-process/anytime-valid machinery — shared with franken_engine's e-process guardplane ("testing by betting… applied to *runtime containment*"); deterministic replay — shared with franken_engine's "deterministic replay with sealed IR4 witnesses"; claim-gated README wording in CI — franken_engine has it ("a binding claim-to-proof matrix… that gates README wording in CI"); the single-maintainer/DSR/rider structure — the entire suite.

---

## 6. CROSS-POLLINATION IN: WHAT ASUPERSYNC SHOULD IMPORT

**1. Certified-lane benchmark protocol → from frankenpandas (+ franken_networkx's verdict contract).** The concept: frankenpandas's "generated vs-pandas scorecard with a documented certification gate (live incumbent same-invocation, per-arm A/A nulls, bootstrap median-CI, ELF SHA pinning)" [Code-verified, High], plus franken_networkx's enforced verdict contract that self-speedups "must not support a competitive claim" [Code-verified, High]. Why it fits asupersync: next step #4 demands an "epoll-vs-io_uring head-to-head on the checked-in TCP workload with the same A/A-null discipline the baseline gate demands, and let the loser change the README's 'primary path' sentence" — and franken_snowflake's packet independently fires the shot: its asupersync transport ships with "no performance or correctness comparison has ever been published, and the only throughput number in the repo (350 MB/s decoder) is a self-gated threshold on synthetic data with no A/A null." Expected payoff: the head-to-head becomes quotable instead of maintainer-internal; either the io_uring "primary path" claim survives with an incumbent-class gate behind it, or the README's narrowest sentence gets falsified — both outcomes are the honesty machinery working.

**2. Machine-checked claim discipline → from franken_markdown.** The concept: "`claims.tsv` + `check-claim-discipline.sh` wire each README claim to a `capabilities --json` key and a proof script — marketing hygiene as CI, a discipline no incumbent practices" [Code-verified, High]. Why it fits asupersync: the 2,626-line README is "a drift vector… that rot fast at this velocity" [Inference, Medium], and asupersync's own drift audit already caught the stale `baseline.json` timestamp [Code-verified, High] — exactly the class of drift a claim→key→proof-script check catches mechanically. Expected payoff: next step #1 ("Extract the honesty machinery as a standalone lint… claim text → code artifact → status row, machine-verified in CI") gets a concrete, already-working implementation to port rather than a greenfield design; the "done when: one other constellation project runs the check in CI and it catches one stale claim" criterion becomes testable immediately.

**3. Cargo-tree admissibility gate → from franken_snowflake.** The concept: an "admissibility gate that fails the build if a forbidden crate appears in any of the ~21 feature lanes, dev included" [Verified, High] — the mechanism behind snowflake's "no Tokio anywhere in its 589-package dependency graph." Why it fits asupersync: the packet's v1 correction establishes that "tokio is an optional root dependency… used in ATP benchmark adapters and benches, *not* in the default feature graph; the runtime proper is tokio-free" [Code-verified, High] — currently a prose claim guarding frankensearch's "NO tokio" mandate and snowflake's whole transport bet. Expected payoff: the tokio-free default graph becomes build-enforced instead of README-asserted; a future feature-unification accident (cf. frankensearch's `test-internals` leak, "HFDT gate RED… fixed as in frankensqlite #104" [Code-verified, High]) fails the build instead of shipping silently.

**4. Planted-mutation testing → from franken_alignment.** The concept: "planted-mutation testing (defects deliberately introduced, required to fail after controls pass)" [Code-verified/Counted, High] — "the required negative-evidence standard." Why it fits asupersync: the e-process oracles and deterministic lab are "the most novel statistical artifact in the tree" — and nothing validates the validators; the packet's own next step #3 proposes running the machinery "against a second system" with the falsification "a published negative result explains why not." Expected payoff: planted races/schedule-dependent defects give the oracles a negative control *before* the expensive extraction — if the e-process monitors can't catch deliberately planted violations, the "run it against a second system" experiment is premature; if they can, the extraction story (standalone `frankenlab` crate) starts with a validated detector.

**5. Signed, re-checkable claim census → from franken_node.** The concept: `docs/honesty_manifest.json` — "7 claims, Ed25519-signed" — plus `scripts/check_claims_manifest.py --check-honesty`, which the analyst executed against the pinned tree: "9 checks ok, 0 drifted, with live recounts… inside the manifest's tolerance bands" [Verified, High]; franken_engine adds the chained variant ("Ed25519-signed evidence entries chained by `prev_hash` into a transparency log with MMR inclusion/consistency proofs"). Why it fits asupersync: open question #1 — "Do the DSR gates actually execute at the pin, and would their receipts survive external audit?" — is the packet's highest-leverage unknown, and the deepening answer demands "portable attestation… publish the DSR runner (or its equivalent), the host specs, and the raw gate logs, so a verifier can re-execute rather than re-trust" [Inference, Medium]. Expected payoff: DSR gate receipts become signed, countable, re-checkable artifacts — the exact gap between "take-my-word" and auditable; even without publishing DSR itself, signed receipts make the "provenance chain terminates at trust the maintainer's hosts" problem legible claim-by-claim.

---

## 7. CROSS-POLLINATION OUT: WHAT ASUPERSYNC SHOULD EXPORT

**1. The statistical baseline-regression gate contract → to franken_engine, franken_ocr, franken_whisper/franken_tts.** The concept: `artifacts/baseline.json` (105 rows, per-row environment tagging, layout-hypersensitivity notes) + failure threshold max(5%, ci95 envelope, +0.6ns) + no-`[skip ci]` waiver + flamegraph artifacts [Maintainer claim, High]. Why it fits them: franken_engine has a `perf_regression_gate.yml` with "zero runs ever" [CI-observed, High] — the contract plus asupersync's fired example is the activation kit for a dead-letter workflow; franken_ocr has a "performance ledger that records losing rows" and a "three-pillar release-certification gauntlet" but its CI was deleted and its last runs failed — the blocking baseline is the missing enforcement half of its ledger culture; franken_whisper and franken_tts are shipped products with release pipelines and measured pipelines (whisper's 10-stage pipeline; tts's 11 releases) whose performance claims currently rest on maintainer measurement. Expected payoff: dead-letter perf workflows become firing gates; "records losing rows" cultures gain the gate that prevents the losses from shipping.

**2. The drift-failing status/dashboard gate → to frankensympy and the three website packets (frankentui_website, frankensqlite_website, frankensim_website).** The concept: `tokio_parity_dashboard_drift.yml`'s "Hard-fail if dashboard artifacts drift" step, which "failed… on drifted artifacts… i.e. the gate working as designed" [CI-observed, High]. Why it fits them: frankensympy's README "'green workspace tests' line is stale-by-a-month" [Code-verified + CI-observed, High] — a drift gate binding README badges to CI state would have fired; the website packets are drift inventories by their own packets' admission (frankentui_website's hero stats "wrong twice over" [Verified stale, High]; frankensqlite_website's "26-crate" claim "stale, and the staleness is enforced by the site's own tests"; frankensim_website's "CHANGELOG is a fossil from the sibling `asupersync_website` project" [Code-verified, High]) — a drift gate binding site stats to the engine repo's manifests is the exact mechanism. Expected payoff: the website-drift class dies at the source; the "museum piece" failure mode of the beads dashboards (data "untouched since March everywhere") gets a machine-readable tripwire.

**3. The deny-unsafe + named-island ledger → to frankentorch and frankensqlite.** The concept: `deny(unsafe_code)` at the crate root with 47 explicitly `#[allow(unsafe_code)]`-annotated island files, counted and sampled [Code-verified, High] — the posture as a census, not an attribute. Why it fits them: frankentorch's docs claim "unsafe forbidden" while "the tree holds **116**" sites — the island ledger is the forcing function that makes the marketed posture match the counted census; frankensqlite's website markets "0 unsafe blocks / pure safe Rust throughout" against "41 counted unsafe blocks in `fsqlite-vfs`" — "disproven as stated" [Verified, High] — and the ledger is the honesty fix that turns a disproven claim into a scoped one. Expected payoff: the unsafe-posture gap (Pattern §4.5) becomes a machine-checked census in the two packets where the gap is currently a marketing liability; franken_lean's precedent (unsafe-ledger census "passed CI at the pin" [CI-observed, High]) shows the census can be CI-enforced.

**4. Anytime-valid (e-process) invariant oracles → to franken_node and franken_whisper.** The concept: `src/lab/oracle/eprocess.rs` — e-processes with Ville's inequality for "anytime-valid invariant monitoring… re-runnable mid-execution" [Code-verified, High on existence]. Why it fits them: franken_node runs "a close-condition oracle with conjunctive L1/L2/release-policy gates that currently reads RED on its own corpus numbers" [Maintainer claim, High] — sequential anytime-valid monitoring is the natural upgrade from batch-gate verdicts for an oracle that must be watched while RED; franken_whisper runs a 10-stage pipeline with "asupersync =0… load-bearing production infrastructure" (Bayesian backend routing, NDJSON robot streaming, SQLite run persistence) — stage invariants monitored under schedule variation fit a pipeline whose whole thesis is orchestration correctness. (frankentui already wires e-processes into resize coalescing — noted as "also does this," not a target; franken_engine's e-process guardplane is the sibling precedent that the concept ports.) Expected payoff: runtime invariant monitoring instead of batch-only gates — the oracle watches continuously rather than verdicting periodically.

**5. Deterministic-schedule lab replay for orchestration stacks → to franken_whisper and franken_remote.** The concept: the lab runtime's "virtual time, deterministic scheduling, trace replay" (`src/lab/`, 82 files, 105,646 lines [Counted, High]) — the packet's own next step #3 proposes extracting `frankenlab` and applying "its deterministic-schedule + oracle pipeline to a *different* concurrent system… *Done when:* the machinery finds one real race in a system it wasn't built for." Why it fits them: franken_whisper's 10-stage pipeline (Bayesian routing across whisper.cpp/insanely-fast-whisper/diarization backends, now a pure-Rust default engine) is exactly the schedule-sensitive orchestration the lab was built to de-Heisenbug; franken_remote's implemented-and-tested slices are "slice-scoped, with every important integration boundary explicitly unqualified (no live Tailscale/QUIC/H3…)" [Maintainer claim, High] — deterministic replay of the timing-sensitive slices is cheaper than live qualification. Expected payoff: the lab's first external customer (the falsification experiment the packet demands) — either "one real race in a system it wasn't built for" or the published negative result, both of which the packet counts as success.

---

**Analyst's closing note for the parent:** asupersync is the suite's load-bearing paradox — the only packet with third-party distribution evidence, and the packet whose evidence bar is simultaneously the highest-specified and the least executed at the assessed commit. Its genuine export is not the runtime but the *evidence contract genre*: committed baselines with statistical thresholds, drift gates that fire, export-surface contracts, and methodology as published crates. Every cross-pollination item above is chosen so the payoff is falsifiable in the packet's own terms — a fired gate, a caught stale claim, a signed receipt, or a published negative result.
