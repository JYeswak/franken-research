# Cross-Suite Brief: frankenredis

**Packet read in full:** `~/workspace/franken-research/frankenredis-assessment.md` (v6, pinned commit `f78f64e4eea5d0ae7848d917e40aafaef1d60ce6`, assessed 2026-09-22).
**Skim basis:** TL;DR, verdict/TRL/NODUS ring, benchmark sections, limitations, and license/governance of the other 43 packets in `~/workspace/franken-research/*-assessment.md`.
**Tier labels are the packets' own** (Rulebook §1): [Verified]/[Counted]/[Git-observed]/[Code-verified]/[CI-observed]/[Maintainer claim]/[External]/[Inference], each with High/Medium/Low confidence.

---

## 1. OWN-PACKET DISTILLATION

- **Verdict / TRL / NODUS ring:** "the most rigorously self-audited codebase assessed in this program to date" — making production-grade claims it simultaneously disavows, under a license that forbids the most likely evaluators from touching it. **TRL 4–5** ("the range, not a point, is honest: lab validation holds, but the red pin blocks any claim of system-level validation in a relevant environment") [Inference, Medium]. **NODUS: Explore — "Explore-with-a-ceiling"** — "substantive-but-unproven is the textbook case… the rider is an advancement blocker (Explore-with-a-ceiling), not a ring qualifier" [Inference, Medium]. The rider is priced into feasibility (2/5) and collaboration (1/5); revisit trigger 3 (rider narrowed/removed) is the only gate that moves the ring conversation.
- **Strongest strength:** Self-scrutiny infrastructure — 26,485-line negative-evidence ledger, SHA-pinned proof bundles per optimization PR, 5,041 differential probes vs a vendored Redis 7.2.4 oracle, full upstream Tcl suite executing in CI, 499 MB fuzz corpus, independently re-counted 61-site unsafe census [all Counted/CI-observed, High]. Packet: "Self-scrutiny infrastructure is the best in the FrankenSuite."
- **Strongest ceiling:** The license rider — names OpenAI and Anthropic (plus affiliates and anyone acting for them) as forbidden parties, bars even *benchmarking, testing, analyzing, indexing* [Code-verified (license text), High]. The packet prices it as "an advancement blocker (Explore-with-a-ceiling), not a ring qualifier," and notes the sharpest irony: the pinned commit itself is Co-Authored-By: Claude Opus 4.8 — "a named Restricted Party under the rider" [Inference, High].

**The 3 most important pieces of evidence (packet tiers kept):**

1. **Negative-evidence ledger: 26,485 lines of recorded losses with verdicts** — "NEGATIVE_EVIDENCE.md: 26,485 lines of recorded losses with verdicts … demonstrated … `wc -l` at pin" [Counted, High]. This is the load-bearing artifact behind the "most self-audited" judgment; process claims verify at High while outcome claims are disavowed.
2. **CI is red at the pin — established, not unknown** — conformance run #7679 "failed at the G1 `cargo fmt` gate with all substantive gates skipped"; Tcl-lane scheduled run #284 "executed the suite (~21 min) but the 'Verify complete upstream suite verdict' step failed," both "established 2026-09-22 via the Actions REST `runs/{run_id}/jobs` endpoint" [CI-observed, High]. This downgrades claims 6–7 to "partially demonstrated" and hardens the "unproven" half of the Explore verdict.
3. **The rider's "use" definition, quoted verbatim** — "'use' includes, without limitation: copying, modifying, merging, publishing, distributing, sublicensing, selling, transferring, making available, hosting, deploying, executing, **benchmarking, testing, analyzing, indexing**, or incorporating the Software or any Derivative Works into any dataset, training corpus, evaluation harness, or pipeline for machine learning or other automated systems" [Code-verified (license text), High]; classification "non-OSI, source-available… fails Open Source Definition §5" [Inference, High]. This is the text behind the ceiling — and it "arguably covers this very assessment" [Inference, Medium].

---

## 2. SHARED NEGATIVE PATTERNS

Patterns frankenredis shares with named other projects, with the packet evidence showing the sharing:

**P1. The license rider is suite-wide, not a frankenredis quirk.** Every one of the 44 packets' Quick Links lists a "License (MIT + OpenAI/Anthropic rider)" link and records the same named-party bar. frankenredis shares the ceiling with e.g.:
- franken_node: "the license rider bars OpenAI/Anthropic and anyone acting for them from use including benchmarking, testing, and analyzing" [Code-verified (license text), High]; its TL;DR uses identical "Explore-with-a-ceiling" language.
- franken_networkx: "the license rider bars OpenAI/Anthropic and their affiliates from use, benchmarking, or analysis" [Code-verified, High]; "the rider is an advancement blocker."
- frankentorch: "the rider bars the likeliest evaluators (AI labs and their agents) from using, benchmarking, or analyzing the code" [Code-verified (license text), High].
- frankenpandas: the rider "bars OpenAI/Anthropic (and anyone acting for them) from use/benchmarking/analysis and demands destroyed copies on breach" [Code-verified, High] — the frankenpandas packet adjudicates the rider as "durable" (adopted at inception, survived review in bead `frankenpandas-dio8`).
- frankentui: the rider "withholds all rights — including benchmarking and analysis — from the two leading AI labs."
frankenredis's packet calls the rider *its* advancement blocker; in fact it is the suite's common ceiling, and frankenredis merely has the sharpest internal contradiction (pin co-authored by a Restricted Party's model).

**P2. CI red at the pin — a shared structural condition.** frankenredis is not alone in having per-job-verified red CI at the pin; the failure *shape* recurs (a lint/format G1 gate failing while all substantive gates are skipped):
- franken_numpy: "CI is **red at the pin** — the latest run (head = pin) fails at the G1 `cargo clippy -- -D warnings` gate with G2–G9 all skipped, established 2026-09-22 via the Actions API" [CI-observed, High] — the same G1-blocked/substantive-skipped shape as frankenredis's run #7679.
- franken_node: "CI is red at the pin — all 8 push-triggered runs observed against the pin's head SHA concluded failure" [CI-observed, High].
- franken_engine: "The core CI lane is red at the pin — `native-runtime.yml` run #35550128941 (head = pin) failed all three jobs on test-execution steps, and all 46 recorded runs of the workflow (Sep 16–21) fail — it has never gone green" [CI-observed, High].
- franken_sympy: "the build-and-test gate has failed 786 consecutive CI runs (last green 2026-08-23; pin fails at the fmt gate, unit tests never run)" [CI-observed, High].
- frankentorch: "the flagship `phase2c-reliability-gates` CI run against the pin failed in ~55 seconds" [CI-observed, High].
- frankenlibc: "main CI is red at the pin"; frankenpandas: "the live-oracle conformance job and `--all-features` lint are red" [CI-observed, High]; frankenfs: "CI red on the main workflow at HEAD"; franken_lean: "CI red at the pin."
Counter-cases that prove the bar is reachable: frankenscipy "CI Gates G1–G9 green at the pinned commit"; franken_threed "one GitHub Actions workflow (green at the pin)" [CI-observed, High].

**P3. README drift — velocity outrunning documentation.** frankenredis's §4.7 weakness 4 (README counting 13 crates vs 16 at pin, "every component is forbid" while 2 are deny and 2 ungated, fr-fec "planned (not implemented)" while implemented) is one instance of a suite-wide failure mode:
- frankenjax: "The README contradicts itself and the code on the most basic facts (118 vs 162 primitives, 15 vs 17 crates, 162,733 vs 459,703 lines)" [Counted, High].
- franken_networkx: "the README is a drift source (13-variant tie-break → 12 counted; 33 fuzz targets → 34 counted; delegation-ledger table 61/71/1113/92/338 → generated 31/150/1112/114/346)" [Counted, High].
- franken_threed: "README honest-status table still says 'Is there a compiler, renderer, or CLI? **No**'" [Verified, High].
- franken_drone_geometry_reconstruction: "README/IMPLEMENTATION_STATUS both claim '28 members' while the workspace has 30" [Counted, High].
- franken_sympy: "the README's 'green workspace tests' line is stale-by-a-month" [Code-verified + CI-observed, High].
The deliberate counter-design exists: franken_code_browser is "the only assessed repo that cannot be caught in README drift *by construction*, because it claims nothing is built," and frankenterm ships "an auto-stamping count script that documents its own drift" (`scripts/stamp-readme-counts.sh`).

**P4. Self-disavowed benchmarks — the honesty culture is suite-wide.** frankenredis's disavowal ("should be read as a contention-sandbox measurement, not a release claim") is the most formalized instance, but the posture is shared:
- frankenfs: "a parity report that explicitly disavows its own 97/97 number as 'not an executed-test result or a readiness score,' and mounted scorecards that open with all-caps anti-hype warnings, including one that proves the project's best btrfs 'win' was a transport illusion."
- frankensqlite: "a README that refuses numeric performance claims for current main."
- franken_engine: "a README that *refuses its own performance claims* — the Node/Bun denominator stays TARGETED."
- frankenscipy: "a scorecard that disavows its own numbers."
- franken_whisper: "a maintainer who records his losses (the failed A/A-null campaign is published as 'no admissible verdict,' not buried)."
- frankenpandas: "the maintainer's own ledger declares sub-1.5x ratios UNRESOLVED after measuring 2.6x same-source build variance" [Code-verified, High].
- frankenmermaid's claim-coverage audit self-reports "225 KEEP claims, 8 with a live incumbent ratio — 96.4% without"; franken_networkx's: "579 of 591 KEEP rows carry no vs-incumbent ratio."

**P5. Agent co-authorship murking "clean-room" labels.** frankenredis's §4.8 (36% of the 449 most recent commits carry agent Co-Authored-By trailers, 158 Claude-family) is shared:
- frankenjax: the rider "bars OpenAI/Anthropic and their agents from even *analyzing* the code — the same labs whose models co-authored 53% of recent commits" [External, High].
- franken_threed: "AI-agent-authored at scale with opaque provenance" [Git-observed, High] (Claude-Session links and named agent personas in commit bodies, no Co-Authored-By trailers).
- frankenscipy: "bus factor 1 (6,395 commits by a `claude` contributor, 158 by the human owner)."
- frankenpandas and franken_engine both record "agent co-authors"/agent personas.

**P6. No release artifact at the assessed pin.** frankenredis's v0.1.0 "targets the earlier tag commit `c577c0ae` — a clean fork point for the released tree, but not for the assessed pin" is mirrored by franken_networkx ("the newest release (`v0.2.2`, 2026-09-12, 23 signed assets) targets an earlier commit while PyPI still serves 0.2.1 — `pip install` gets the older build" [External, High]); frankentui goes further with a phantom v0.9.0 that "exists on no surface GitHub tags, GitHub Releases, or crates.io can see."

---

## 3. CI/EVIDENCE BAR

What frankenredis demonstrates that named others lack (and where others "also do X"):

- **Per-job CI conclusions via the Actions REST `runs/{run_id}/jobs` endpoint.** The v5 round established greenness *red* at the pin per job for both lanes (run #7679 G1 fmt failure with G2–G7 skipped; run #284 verdict-step failure) [CI-observed, High]. frankenscipy asserts "CI Gates G1–G9 green at the pinned commit" but the packet evidences no per-job verification method; franken_threed's "green at the pin" is a single-workflow claim [CI-observed, High] without the per-job audit. *Also does X:* franken_numpy's packet used the same Actions-API method ("established 2026-09-22 via the Actions API") — the technique is shared between these two packets, not unique to frankenredis.
- **Proof bundles per optimization PR** (`artifacts/optimization/*/PROOF.md`): "every optimization ships with a SHA-pinned golden RESP transcript and paired/reversed hyperfine runs under a keep/reject score gate" [Maintainer claim, Medium; artifacts verified to exist]. No other packet evidences a per-PR proof-bundle artifact with golden transcripts + paired/reversed runs + score gate. (franken_whisper has the campaign-win doctrine and a 5,567-line performance ledger; frankenmermaid has head-to-head TOML evidence; neither has per-optimization-PR PROOF.md bundles.)
- **Executing the incumbent's own full upstream test suite in CI** (284 observed runs of `upstream-redis-7.2.4-full.yml`) [CI-observed, High]. Differential-vs-oracle harnesses are shared (frankenjax 861 JAX-oracle fixtures, frankensympy 230-fixture corpus vs pinned SymPy, franken_threed differential harness vs pinned upstream oracle, frankenpandas 1,387 conformance packets), but running the *incumbent's own test suite* as a CI lane is not evidenced elsewhere.
- **Fuzz corpus scale:** 33 targets / 11,015 lines, corpus of 127,571 files / 499 MB (127,559 nonzero) [Counted, High]. Others count *targets* (frankenlibc: 66 fuzz targets; frankenscipy: 96 fuzz targets; franken_graphdb: 9 Miri tests) but no other packet evidences a corpus of this measured size. franken_numpy's corpus size is not evidenced in its TL;DR.
- **BENCH_METHODOLOGY.md discipline** (same host, verified-idle cores, A/A nulls 0.979–0.999, core pinning, ELF SHA-256 pinning) [Maintainer claim, Medium]. *Also does X:* frankenmermaid has "mandatory A/A nulls and ELF-SHA-256 provenance"; frankenpandas has "live incumbent same-invocation, per-arm A/A nulls, bootstrap median-CI, ELF SHA pinning" [Code-verified, High]; franken_networkx has "same-invocation A/A nulls, byte-identity-before-timing, and ELF-hash pinning" [Maintainer claim, High]; franken_whisper gates on A/A nulls in [0.98, 1.02]. The discipline is a shared suite pattern; frankenredis's distinguishing add is the published methodology doc plus the disavowal sentence that operationalizes it.
- **Publicly observable CI at scale** (7,679 conformance-gate runs; 284 Tcl runs — both lanes inspectable on GitHub Actions). Contrast with the **private-CI pattern** many siblings use, which no outsider can audit: frankenjax "zero GitHub Actions workflows exist, so every 'green' claim executes on the maintainer's local RCH fleet and is attested only by checked-in artifact JSON" [External + Inference, High]; frankentui "GitHub Actions was disabled 2026-09-06 so all verification runs on maintainer-private DSR infrastructure invisible to outsiders"; frankenterm "no public CI (verification runs on maintainer-controlled RCH hosts that are not independently inspectable)"; frankengraphdb "GitHub Actions was de-automated by owner ruling on 2026-09-03 ('we do not use gh actions for any reason, ONLY /dsr')"; franken_overlap "GitHub Actions is deliberately disabled"; franken_whisper "no CI config in the tree." frankenredis's CI is publicly inspectable even when red — that observability is itself the bar.

---

## 4. SHARED HURDLES

Structural issues frankenredis shares, with evidence:

- **Bus factor 1 (suite-universal).** frankenredis: "Bus factor: 1. One human maintainer (Jeffrey Emanuel); all 449 commits in the deepened window are authored by Dicklesworthstone/Jeffrey Emanuel (the same person)" [Git-observed, High]. Every packet records the same: franken_engine "bus factor 1" [Git-observed, High]; franken_numpy "bus factor 1"; frankenmermaid "**bus factor 1 at extreme velocity** — one human, ~3,282 commits in ~7 months"; frankenpandas "One human maintainer (Jeffrey Emanuel) plus agent co-authors; 8,621 commits" [External, High]. The no-contributions policy hardens it in several: asupersync "bus factor 1 with outside contributions explicitly refused (README L2620)" [Maintainer claim, High]; frankenterm "bus factor 1 by **explicit, deliberate policy**"; frankenfs/frankentorch/frankensqlite all record "explicit no-contributions policy." frankenredis shares the bus factor but its packet does *not* evidence a stated no-contributions policy — the governance ceiling is the rider, not a contributions ban.
- **Velocity vs review depth.** frankenredis: 8,115 non-merge commits in ~6 months decelerating to ~16/day; "Review depth is unassessed: PRs-vs-direct-to-main and who reviews bead closures were not examined [Not verified]" (open question 6). Same tension elsewhere: frankenmermaid's ceiling is stated as "**bus factor 1 at extreme velocity** — one human, ~3,282 commits in ~7 months… everything else (license, benchmarks, roadmap) becomes moot if the maintainer stops"; franken_code_browser peaked at "101 commits on 2026-09-17 and already decelerating to 4 on pin day" [Git-observed, High]; franken_surveillance_system: "25 of the last 40 commits carry 'NOT RUN' disclaimers (~63% of the recent window)" [Git-observed, High].
- **README drift** — documented in §2/P3 above; frankenredis's packet uniquely frames it as "a structural liability, not a typo" for a project "whose pitch is *auditability*" [Code-verified, High].
- **License rider** — §2/P1 above; frankenredis's §4.8 adjudicates it as "a licensee-facing contract term — it binds users, not the author," with the rider as "advancement blocker" wired to trajectory trigger 3.
- **Unobservability of CI (DSR/RCH private lanes)** — §3 above. frankenredis is on the *good* side of this hurdle (public Actions CI); it shares the *other* side's consequence in miniature only where its own CI is red.
- **Parity-tail grind / asymptotic parity.** frankenredis §4.7 weakness 6: "Parity is asymptotic and the tail is where drop-in replacement dies" (three keyspace gates, WAITAOF, StreamSkeleton, INFO AOF fields, RESP3 doubles, cjson key-order WONTFIX) [Maintainer claim, Medium]. Shared: frankenmermaid's "manimlib compatibility long-tail (93 placeholders)"; frankenscipy "behavioral parity that is name-census rather than assertion coverage (the maintainer admits 'referenced is weaker than compared')"; frankenjax's differential results "maintainer-asserted"; franken_node's "L1 product oracle sits at 69.82% (391/560) against the charter's ≥95% ship floor."

---

## 5. GENUINE UNIQUENESS

Only things no other packet evidences (checked against all 43; "also does X" called out explicitly):

1. **Per-optimization-PR proof bundles** — `artifacts/optimization/*/PROOF.md` with SHA-pinned golden RESP transcripts, paired/reversed hyperfine runs, and a keep/reject score gate [Maintainer claim, Medium; artifacts verified to exist]. No other packet evidences a per-PR artifact of this form. (Adjacent: franken_whisper's campaign-win doctrine is a *policy*, not a per-PR artifact; frankenmermaid's head-to-head TOML is campaign-level.)
2. **A runtime threat ledger** — 8 `ThreatClass` variants, a JSON-lines ledger (`fr_threat_ledger_v1`) flushed to disk from the server loop in hardened mode [Code-verified, High on existence]. franken_node has a *static* "22-row threat model with explicit out-of-scope non-goals and per-threat regression anchors" [Code-verified, High]; franken_engine has a signed evidence ledger with MMR; neither evidences a *runtime-persisted* threat ledger flushed from the server hot path.
3. **Executing the incumbent's own full test suite as a CI lane** — `upstream-redis-7.2.4-full.yml`, 284 runs observed [CI-observed, High]. Other packets do differential-against-oracle (frankenjax, frankensympy, franken_threed, frankenpandas, frankensqlite's "differential oracle against bundled rusqlite"), but none evidences running the *incumbent's native test suite* in CI.
4. **A measured fuzz corpus of 499 MB / 127,571 files** (33 targets, 11,015 lines) [Counted, High]. Other packets evidence target *counts*, not corpus scale.
5. **A hand-rolled Lua 5.1 parser + bytecode VM (31,799 lines in one file) for byte-exact parity with the incumbent's modified interpreter** [Counted, High]. No other packet evidences a hand-rolled interpreter substituted for an off-the-shelf one for parity reasons; cf. frankenjax where `jit` is "a verified no-op" (the opposite choice).
6. **A maintainer-written disavowal of its own headline benchmark table inside the README's Performance section** with a named deferred bead (`vibu6`) as the un-block condition [Maintainer claim, High]. Others disavow *numbers* (frankenfs, frankenscipy, franken_whisper) or *refuse* claims (frankensqlite, franken_engine); none evidences a *named, deferred, bead-tracked re-baseline condition* embedded in the README as the gate for quoting the table.
7. **Sentinel failover with quorum-2 end-to-end test** (`failover_step` driving a 7-variant `FailoverState`; `three_fr_sentinels_with_quorum_two_agree_elect_and_fail_over`) [Code-verified, High]. No other packet evidences HA/failover machinery.

**Contested non-uniqueness (frankenredis's packet claims it; other packets rebut or share):**
- "The most rigorously self-audited codebase assessed in this program to date" [Inference, Medium] vs franken_networkx's packet: "the most honest self-scrutiny culture in the FrankenSuite — stronger, on paper, than FrankenRedis's"; frankenlibc's packet: "the strongest measurement discipline observed in the program so far" [Inference, High]; franken_lean's packet: "the most self-auditing repo in this assessment program." This superlative is genuinely contested across packets — do not repeat it unqualified.
- The negative-evidence ledger itself is *not* unique: franken_numpy (67,641 lines), frankenlibc (41,205), frankenpandas (44,086), frankenscipy (44,078), frankentorch (42,708), franken_networkx (37,116), franken_whisper (26,846 — within ~1.4% of frankenredis's 26,485), frankenfs (20,556), frankensearch (19,201) all evidence one. Frankenredis's ledger is distinguished by *how losses are recorded* (verdict + artifact per loss), not by having one.
- RaptorQ FEC sidecars are *not* unique: frankensqlite ("RaptorQ erasure-coded durability" as a signature bet) and frankensearch ("RaptorQ sidecars … in code" [Code-verified, High]) both evidence the same maintainer-pattern.
- The differential-against-vendored-oracle harness is *not* unique (see §3): frankenjax, frankensympy, franken_threed, frankenpandas, frankensqlite, franken_networkx (structural-equivalence oracle).

---

## 6. CROSS-POLLINATION IN

Specific concepts from other packets that should be applied to frankenredis:

1. **CI-enforced unsafe-site ledger** → from **franken_lean** → Frankenredis's 61-site census is a README table plus an analyst's independent recount — a snapshot auditors must re-verify, which is exactly how its README drifted on the forbid/deny posture before. Franken_lean's packet evidences "a CI-enforced unsafe-site ledger" whose "bidirectional unsafe-ledger census passed CI at the pin" [CI-observed, High] — the ledger fails the build if the code and the census disagree. → **Expected payoff:** the unsafe census becomes a living gate instead of a snapshot; a new unsafe site in fr-simd can never silently land the way the README's "every component is forbid" line silently rotted.

2. **README claim-wiring (claims.tsv + check-claim-discipline.sh)** → from **franken_markdown** → frankenredis's §4.7 weakness 4 documents README drift as "a structural liability, not a typo" in a project "whose pitch is *auditability*" [Code-verified, High] — the README miscounted crates (13 vs 16), misstated the safety posture, called fr-fec "planned (not implemented)," and printed a stale 5,076 argv count. Franken_markdown's packet evidences "claims.tsv + check-claim-discipline.sh wire each README claim to a `capabilities --json` key and a proof script — marketing hygiene as CI, a discipline no incumbent practices" [Verified, High]. → **Expected payoff:** every countable README assertion (crate count, argv-probe count, forbid/deny posture, feature status) fails CI when it drifts; the auditability pitch becomes mechanically true.

3. **Measurement-integrity gate (name the worker, name the harness, replicate before quoting)** → from **frankentorch** → frankenredis's entire benchmark posture hinges on the deferred `vibu6` quiet-host re-baseline and the contention-sandbox vocabulary. Frankentorch's packet evidences "a standing measurement-integrity gate (name the worker, name the harness, replicate before quoting — adopted 2026-08-15)" [Counted + Maintainer claim, High], and its ledger's verdict that "exactly one of fourteen vs-PyTorch lanes is quotable" [Maintainer claim, High]. Frankenfs's packet independently shows why this matters: its August audit admitted "166 published benchmark rows cannot prove which machine they ran on." → **Expected payoff:** when `vibu6` ever un-defers, the re-baseline runs under a rule that names the worker and harness, so the numbers can't inherit the "contention-sandbox" ambiguity the README currently confesses.

4. **Planted-mutation testing** → from **franken_alignment** → frankenredis's parity thesis rests on 5,041 differential probes and the Tcl lane catching regressions — but nothing evidences the harness *can* catch a regression. Franken_alignment's packet evidences "planted-mutation testing (defects deliberately introduced, required to fail after controls pass)" [Code-verified/Counted, High] as part of its honesty apparatus. → **Expected payoff:** deliberately introduce parity breaks (e.g., a wrong `%.17g` rendering, a dropped RESP3 double) and require the 5,041-probe harness and Tcl lane to flag them; converts "the harness would catch it" from [Inference] to [Verified].

5. **Claim-coverage audit (self-reported unmeasured-claim share)** → from **frankenmermaid** → frankenredis's README mixes internally-controlled wins (pipelined table with A/A nulls) with ambiguous rows (XADD rows under a separate header, disavowal scope unclear — §4.5). Frankenmermaid's packet evidences "a claim-coverage audit that self-reports '225 KEEP claims, 8 with a live incumbent ratio — 96.4% without'" — the audit of its own unmeasured claims as a first-class artifact. → **Expected payoff:** a machine-generated audit of every perf claim in the frankenredis README (certified vs self-speedup vs disavowed vs unmeasured) would resolve the §4.5 XADD ambiguity by construction instead of by inference.

---

## 7. CROSS-POLLINATION OUT

Specific frankenredis concepts to export to named other projects:

1. **PROOF.md per-optimization-PR proof bundles** (SHA-pinned golden transcripts + paired/reversed hyperfine + keep/reject score gate) → **frankenpandas, frankentorch, frankenmermaid** → frankenpandas's packet: "729 of 751 kept performance claims predate the evidence contract and lack same-invocation incumbent ratios" [Maintainer claim, High] — the proof-bundle schema would retrofit exactly the missing same-invocation discipline claim-by-claim; frankentorch's performance thesis "rests on a single quotable lane" and its own SDPA retraction (a 2x win exposed as a 3-D-vs-4-D layout artifact [Maintainer claim, High]) is the canonical case paired/reversed runs exist to catch; frankenmermaid's audit shows "225 KEEP claims, 8 with a live incumbent ratio — 96.4% without." → **Expected payoff:** their perf claims become individually falsifiable artifacts instead of ledger rows; frankentorch gets a second quotable lane only when it survives the score gate.

2. **Runtime threat ledger** (8 `ThreatClass` variants, JSON-lines ledger flushed from the server loop, hardened mode) → **franken_engine, franken_node** → both are adversarial-workload runtimes: franken_engine is "a from-scratch Rust execution substrate for adversarial JS/TS extension workloads" with "IFC lattice + capability algebra" but evidences only a signed evidence ledger with MMR (design-time provenance, not runtime decisions); franken_node has "a 22-row threat model with explicit out-of-scope non-goals and per-threat regression anchors" [Code-verified, High] but no persisted runtime log of the decisions its policy gates make. → **Expected payoff:** per-decision, on-disk security telemetry for adversarial-extension workloads — the audit trail their threat models describe but don't produce at runtime.

3. **BENCH_METHODOLOGY.md + "contention-sandbox" vocabulary** (same-host, verified-idle cores, A/A nulls, core pinning, ELF SHA-256 pinning, plus a README-embedded label for non-release numbers) → **frankenfs, frankensympy** → frankenfs's own packet admits "166 published benchmark rows cannot prove which machine they ran on" (August worker-scope audit) — ELF-SHA pinning + named-worker methodology fixes exactly that finding; frankensympy's packet shows "PERF-001's 'materially outperforms' status sits above a paired report whose geometric mean favors the oracle 0.616" [Code-verified, High] — the "contention-sandbox measurement, not a release claim" label is the honest status PERF-001 needs. → **Expected payoff:** frankensympy's perf claims get an honest, machine-checkable label; frankenfs's rows become attributable to a machine.

4. **Negative-evidence ledger discipline** (every loss recorded with verdict + artifact; 26,485 lines at the pin) → **franken_native_capsule, franken_drone_geometry_reconstruction** → franken_native_capsule's packet evidences "adversarial honesty as an engineering discipline — the README enumerates what the code is *not* (nine named non-capabilities)" but no ledger of failed experiments or losses; franken_drone's packet is tracked explicitly for "methodology (claim inventory discipline, maturity vocabulary, admission auditing)" and records self-audit drift (28-vs-30 members) that a loss ledger is designed to memorialize. → **Expected payoff:** their honesty cultures gain the one artifact that makes honesty cumulative — a ledger where a disproven claim retires *into* the record instead of out of it.

5. **Unsafe-census methodology** (per-crate `forbid`/`deny` gate table + independent recount excluding comments/lints, published as a README census table) → **franken_remote, franken_native_capsule** → franken_remote's packet evidences "unsafe code confined to `fr-native` (40 files) and `fr-ffi` (2 files), with `unsafe_code = "forbid"` inherited workspace-wide" [Code-verified, High] — the confinement is stated but not *counted*; franken_native_capsule's packet counts "13 `unsafe {}` blocks confined to two explicitly allow-listed modules" [Counted/Code-verified, High] but publishes no gate-posture table. → **Expected payoff:** a census table with the frankenredis arithmetic (gates 12-forbid/2-deny/2-ungated = 16; sites 46/5/7/3) turns "confined" from an architectural claim into a checkable number.

---

*Grounding note:* Every claim above is drawn from the 44 assessment packets' own evidence tiers; where frankenredis's packet and a sibling packet disagree on a superlative (see §5, "Contested non-uniqueness"), both positions are recorded rather than adjudicated. No packet was compiled, executed, or benchmarked in this brief — the packets' own "not done" sections stand.
