# Cross-suite brief: franken_snowflake

**Method:** franken_snowflake-assessment.md read in full (all 232 lines, §§4.1–4.12 + deepening questions); the other 43 packets skimmed via TL;DR + TRL/NODUS/license/bus-factor/CI/drift keyword extraction, with targeted greps for mechanism-level detail (MCP, DPOR, admissibility, typed-skip, approval, redaction, SWAR). Every claim below is grounded in packet evidence with the packet's own evidence-tier labels preserved.

---

## 1. OWN-PACKET DISTILLATION

**Verdict:** Genuine, working, 14-crate / 62,062-line Rust workspace speaking Snowflake's SQL API v2 over HTTPS — clean-room, Tokio-free, built for coding agents. Real releases (v0.0.1→v0.0.4, 16 checksummed multi-arch binaries, crates.io 0.0.4). But the README's "read + write live-success" badge is ahead of the repo's own proof tracking.

**TRL:** 6. **NODUS ring:** Explore (a "genuine near-miss on Pilot" — release artifacts and a bounded workload exist on paper, but the maintainer's own e2e live-proof bead is still open, the flagship `cli live,mcp` lane is red in the project's own CI with no run on HEAD, and the license bars the labs most likely to pilot it; "when in doubt, ring down, not up").

**Strongest strength:** The dependency-hygiene and evidence machinery is real and machine-checked, not aspirational — "the no-Tokio policy is proven by the lock file, not promised in a README" [Verified, High]. The packet calls it "the strongest 'dependency diet' evidence found in any of the 44 assessed repos [Verified, High]": zero forbidden crates in all 589 `Cargo.lock` packages *including dev lanes*, enforced by a cargo-tree admissibility gate across ~21 feature lanes.

**Strongest ceiling:** A compound ceiling — bus factor 1 *with contributions explicitly refused by policy*, zero adoption signal (6 stars, 2 forks), zero independent validation, and the MIT+OpenAI/Anthropic-rider license ("NOT OSI open source" [Verified, High]) that withholds even benchmarking rights from the two frontier AI labs — "a hard ceiling for a project whose entire thesis is agent infrastructure."

**The 3 most important pieces of evidence (with tiers):**

1. **The lock-file grep + the executed gate** [Verified, High]: zero occurrences of tokio/reqwest/hyper/axum/tower/sqlx in all 589 lock packages; and the Actions REST API shows the "Assert dependency admissibility" CI job **passed on all three OS jobs** [Verified via Actions REST API, High] — the policy is machine-executed, not just machine-checked locally.
2. **The bead-status vs badge tension** [Verified, High]: bead `fsnow-agent-ergonomic-cli-cli-live-e2e-and-receipts-bvf` (end-to-end live-account proof) is `in_progress` at HEAD and the empirical jsonv2 golden bead is `open`, while `docs/live_proof.md` states "only the credentialed evidence run … remains" — against a README badge claiming "read + write live-success" [Maintainer claim, Medium, with Verified-High counter-evidence].
3. **The CI run that shouldn't exist** [Verified via Actions REST API + clone, High]: a new `.github/workflows/ci.yml` executing by 2026-09-13, 140 runs, latest (#140) **red** — the `cli live,mcp` lane failed on ubuntu-latest and windows-latest, macos-latest failed on the "installer shell UI safety" assertion, no run on the HEAD commit — all against AGENTS.md's explicit prohibition ("This repository never uses GitHub Actions") that was already enforced once (previous workflow deleted at v0.0.3 after 52 parse-failed runs).

---

## 2. SHARED NEGATIVE PATTERNS

Patterns franken_snowflake shares with other packets, with evidence of the sharing:

**P1. Red or failing CI at the pin / CI-vs-docs contradiction.** Shared with a large bloc: frankenredis ("CI is red at the pin — conformance run #7679 failed at the `cargo fmt` gate … and the Tcl lane's scheduled run #284 failed at the verdict-verification step" [CI-observed, High]); franken_lean ("CI at the pin is red on the main gate — 11 of 30 steps failing" [CI-observed, High]); franken_engine ("`native-runtime.yml` run #35550128941 (head = pin) failed all three jobs … all 46 recorded runs … have never gone green" [CI-observed, High]); franken_node ("all 8 push-triggered runs observed against the pin's head SHA concluded failure" [CI-observed, High]); franken_numpy ("latest run (head = pin) fails at the G1 `cargo clippy -- -D warnings` gate … while the README badge still reads 'G1 green'" [CI-observed, High]); frankenfs ("main workflow … failing at HEAD" [Verified, High]); frankenlibc (pin's main CI run "observed red"); frankensympy ("the build-and-test gate has failed 786 consecutive runs" [CI-observed, High]); frankentorch ("flagship `phase2c-reliability-gates` CI run against the pin failed in ~55 seconds" [CI-observed, High]); franken_agent_detection ("main CI job is red at the pin — the `Format` step fails, so clippy/tests/packaging never executed on the assessed commit"); frankenpandas ("latest run: conformance job red, 15/19 jobs green" [CI-observed, High]). Franken_snowflake's twist — a CI that runs 140 times against an enforced prohibition — is unique, but red-at-pin membership is the program norm.

**P2. Badge/marketing ahead of the evidence.** Shared with: franken_numpy ("the README badge still reads 'G1 green'" while CI fails at the G1 clippy gate at the pin [CI-observed, High]); frankensympy ("the README's 'green workspace tests' line is stale-by-a-month" [Code-verified + CI-observed, High]); franken_ocr ("v0.8.0 badges vs v0.9.0 reality, '13–17 MB' binaries that are 20.6–28.5 MB" [Verified, High]); frankentui ("the phantom v0.9.0 (CHANGELOG claims release + crates.io 0.9.0; reality is v0.8.0 / crates.io 0.7.0) is the largest drift found in any packet so far" [External, High]); franken_markdown ("README says 'CI fails if it drops' … while the only GitHub Actions workflow is disabled" [Verified, High]); franken_manim (README "written in present tense as if the 1.0 design were fully realized; it is a spec of the target, not a description of the present" [Verified, High]). Franken_snowflake's "read + write live-success" badge against an `in_progress` proof bead is one instance of a program-wide class.

**P3. Zero independent validation.** Universal: franken_whisper ("no independent benchmark, review, or production deployment found"), frankentui ("zero independent validation found (no third-party review, benchmark, or production deployment)"), frankenfs ("Zero independent validation of any kind"), frankengit ("Zero independent validation of any kind"), franken_networkx, franken_numpy, franken_node ("No independent reproduction is known"), franken_ocr, franken_tts, franken_torch, franken_sim, franken_lean ("zero independent validation, red CI"), franken_manim ("Independent validation: zero"), franken_remote, franken_surveillance ("No independent coverage found"), franken_drone ("No independent rerun of anything is known"), franken_pandas, frankenscipy ("zero independent validation, trivial crates.io downloads"), franken_threed, franken_overlap ("zero independent coverage"), franken_graphdb, franken_code_browser ("no independent coverage of the repo beyond the repository itself"), frankensqlite ("Zero independent validation of any kind"), franken_native_capsule, franken_nlp, franken_alignment ("no independent party can reproduce them"), asupersync ("No independent reproduction is known to exist"). Only two packets deviate: asupersync (318,241 crates.io downloads, 78 reverse-dep crate versions [External, High] — real distribution, though adoption is constellation-internal) and franken_agent_detection (21,189 downloads + real downstream workload: CASS "discovers sessions from 26 agent harnesses *through this crate*" [Maintainer claim, Medium]).

**P4. README/docs drift as a governance signal.** Program-wide; franken_snowflake's own instances (420 vs 576 tests, 18 vs 21 lanes, stale CHANGELOG scope header, v0.0.4 one-day date drift) sit beside: franken_networkx ("README drifts on every hand-typed number the auto-generated ledgers already know correctly" [Counted, High]); franken_ocr (v0.8.0-vs-0.9.0 badges, binary-size claims); franken_node ("CHANGELOG's documented window ends 2026-08-19, over a month before the pin"); franken_whisper ("CHANGELOG scope window 4 weeks stale at HEAD"); frankenfs ("CHANGELOG's scope window ends 2026-08-19 while HEAD is 2026-09-22"); franken_tts ("version badge reads 0.1.9 while `Cargo.toml` is 0.1.10"); franken_remote ("two sections say the workspace has 6 crates (it has 13)"); franken_threed ("README honest-status table still says 'Is there a compiler, renderer, or CLI? **No**'" [Verified, High]); franken_pandas ("README drifts in at least four places"); franken_torch ("README says 12 crates (13 at pin)"); franken_mermaid (line counts "every crate 1.7–7× stale"); franken_engine ("stale on every count one month after its verification date"); franken_drone ("28-member" in docs vs 30 in Cargo.toml); franken_lean (badge vs K2's non-existence); franken_markdown (test counts, line counts, CI language); franken_sim; franken_sympy; franken_tui; franken_graphdb ("README-vs-lockfile drift of exactly the class the Rulebook demands be reported"); franken_libc; franken_manim; frankensqlite ("crate LOC tree listing, CI badge, CHANGELOG unreleased section"); franken_redis; franken_nlp; franken_surveillance. The packet's own finding — "docs chronically lag the tree … each minor, but the pattern is the governance signal" — is the program's shared governance signature.

**P5. The MIT+OpenAI/Anthropic rider as the program's default license.** Shared with ~30 of 44: franken_lean, franken_engine, franken_node, franken_numpy, franken_ocr, franken_tts, franken_whisper, frankenfs, frankengit, frankengraphdb, frankenjax, frankenlibc, frankenmermaid, frankenpandas, frankenredis, frankenscipy, frankensearch, frankensim, frankensqlite, frankensympy, frankenterm, frankentorch, frankentui, franken_markdown, franken_markdown_website, franken_markdown_website (frankentui_website), franken_overlap, franken_remote, franken_surveillance, franken_drone, asupersync, franken_code_browser, franken_threed, franken_nlp, franken_alignment, franken_manim — all "MIT + OpenAI/Anthropic rider — NOT OSI open source," with the rider's "use" definition covering "benchmarking, testing, analyzing, indexing" [Code-verified (license text), High] in each. The structural irony is also shared: frankenjax ("52 of the 100 most recent commits were co-authored by Anthropic's models … license names OpenAI and Anthropic as forbidden parties" [External, High]), frankenlibc ("forbids Anthropic's models from analyzing code that Anthropic's models helped write"), frankenscipy (6,395 `claude` commits vs 158 human), frankensnowflake's own (README: "I'll have Claude or Codex review submissions" vs a LICENSE barring Anthropic from all rights [Verified verbatim, High]). Deviations: franken_agent_detection ("the only FrankenSuite repo with a plain-MIT, no-rider license"), franken_native_capsule (no LICENSE file — metadata-only MIT claim), the websites repos (no LICENSE / null / "Unspecified").

**P6. Self-gated/disavowed performance numbers instead of benchmarks.** franken_snowflake's 350 MB/s tripwire ("a self-gated `assert!` … no A/A null, no core pinning … Do not quote 350 MB/s as a result" [Verified, High]) sits with: frankenredis (headline benchmark table "explicitly disavowed by its own README" [Maintainer claim, High]); franken_torch ("the ledger's own verdict that exactly one of fourteen vs-PyTorch lanes is quotable" [Maintainer claim, High]); frankensqlite (README benchmark honesty = "no published parity scorecard," and "A packet must never cite a FrankenSQLite performance number as a result, because none is offered"); franken_networkx (only 2.0% of 591 performance claims carry incumbent comparisons, and its own ledger contract says self-speedups "must not support a competitive claim" [Code-verified, High]); franken_threed (README: "**zero speedup has been measured**" [Maintainer claim, High]); franken_ocr (performance ledger "records losing rows"); franken_manim (93 placeholder symbols; performance "unproven").

**P7. Zero-`unsafe` posture.** franken_snowflake (`unsafe_code = "forbid"` workspace-wide, zero unsafe blocks [Verified, High]) shares the stance with: franken_mermaid ("the strongest in this program — all 9 crate roots carry `#![forbid(unsafe_code)]` and a code-level census finds zero actual `unsafe` tokens" [Verified, High]); franken_overlap ("Zero unsafe code under a workspace-level `forbid(unsafe_code)`" [Counted, High]); franken_scipy ("zero `unsafe` blocks (workspace `#![forbid(unsafe_code)]`)"); franken_lean ("a `forbid(unsafe)` kernel with zero project-authored unsafe blocks" [Code-verified, High]); franken_markdown ("`#![forbid(unsafe_code)]` at the crate root plus workspace-wide `[lints.rust] unsafe_code = "forbid"`" [Verified, High]); franken_node ("zero real `unsafe` sites under `#![forbid(unsafe_code)]` in all three workspace members" [Counted, High]); franken_drone (zero unsafe blocks re-counted); franken_sympy (zero unsafe blocks). Franken_snowflake's posture is solid but commodity inside this program.

**P8. Extreme single-maintainer velocity (~10 commits/day, 99 commits in 10 days).** Shared with: frankengit ("~25–40 commits/day"), franken_engine ("~20 commits/day Sep 1–20, 96 on Sep 15 alone" [Git-observed, High]), franken_node ("~20 commits/day"), franken_surveillance ("~20 commits across 2026-09-21/22"), franken_overlap ("347 commits in 21 days … ~16.5/day"), franken_threed ("~29 commits/day"), franken_tui ("~3,282 commits in ~7 months"), franken_torch (6,305 commits), franken_pandas (8,621 commits), franken_whisper (3,778 commits), franken_code_browser ("206 commits across 2026-09-17→22, peaking at 101 commits on 2026-09-17"). Velocity-vs-review-depth is the program's structural tax.

---

## 3. CI/EVIDENCE BAR: what franken_snowflake demonstrates that others lack

**B1. Machine-checked dependency diet across every feature lane, executed in third-party CI.** `scripts/check-dependency-admissibility.py` runs `cargo tree` across ~21 feature lanes (default, no-default, each production feature, all combined, *and dev/test lanes*) and fails on any forbidden crate; at run #140 the "Assert dependency admissibility" job passed on all three OSes [Verified via Actions REST API, High]. Who lacks it: **frankensearch** — its no-tokio stance is a *comment* ("Async runtime (structured concurrency, NO tokio)" — root Cargo.toml comment [Code-verified in manifests, High]), i.e., policy-as-prose with no gate; **franken_drone_geometry_reconstruction** and **franken_surveillance_system** achieve their zero-external-crate posture by construction (Cargo.lock = std only [Counted, High]) but have no gate that would catch a future dependency introduction — and their CI is unobservable (324 queued/cancelled self-hosted runs) or permanently red (570 cancelled, 47 failed); **franken_code_browser** has a `DEPENDENCY_CONSTITUTION.md` but its unsafe constitution is "already violated in-tree" and it has **no CI workflows at all** [Verified, High]; **franken_networkx** has auto-generated ledgers that "fail CI on drift" [Code-verified, High] but they audit *coverage*, not the dependency graph; no other packet contains any cargo-tree or lock-file admission gate. Franken_snowflake's own packet claims this is "the strongest 'dependency diet' evidence found in any of the 44 assessed repos [Verified, High]" — the cross-read corroborates it.

**B2. The typed-skip live-proof contract as a credential-gated-CI mechanism.** Credential-less runs emit a *structured skip event* (`franken_snowflake.live_gate.v1`) instead of silently passing, with opt-in env, spawned-process env sanitization, and a secret scan [Verified structure, High]. Near-misses that lack the contract: **franken_ocr** has an "L0–L5 parity ladder with skip-honest no-weights mode" but it is about absent model weights, not credential-gated live accounts; **franken_tts** has the `GREEN WITH SKIPS` receipt doctrine [Verified/External, High] but it is a prose doctrine, not a machine-emitted typed event with env sanitization; **frankenterm** has `skipped_not_proven` qualification gates [Verified, High] — the same refusal-to-claim spirit, but gated at qualification scope, not per-run in CI. No other packet has a typed skip-event contract for credential-gated testing.

**B3. Compile-time secret-leak gate.** The auth crate's `build.rs` scans its own `src/` for credential markers and fails the build if a credential-shaped field derives `Debug` [Verified, High], backed by planted-canary redaction tests. Partial overlaps that lack it: **franken_node** has manual `Debug` impls that redact (`card_hash`/`registry_signature` → `"[REDACTED]"` — "a small, telling operational-hygiene" measure) — runtime-level, handwritten, and ungated; **frankenterm** has a `redactor` crate and "secret redaction" in its policy engine — also runtime; **frankengraphdb** has "redacted `Debug`" for prepared-query plans [Maintainer claim, Medium] — manual, not compile-time. A build-time scanner that fails the build is unique to franken_snowflake.

**B4. Live-receipt evidence discipline.** Every live execution writes a BLAKE3 content-addressed receipt plus partition evidence and an append-only audit event, with per-envelope `data_source` provenance and "the live path never substitutes fixture or empty data" [Verified in code/docs, High]. The honest-tracking discipline is also demonstrated: bead `fsnow-agent-ergonomic-cli-gap-docs-truth-79s` records the maintainer *reopening a falsely-closed golden bead* [Verified from `.beads/issues.jsonl`, High] — the tracker corrects its own proof accounting.

**Caveat:** franken_snowflake's evidence bar is lane-specific, not a general lead. **frankenscipy** has CI Gates G1–G9 *green at the pinned commit* [CI-observed, High] — full CI greenness franken_snowflake does not have. **franken_engine**, **franken_lean**, **franken_networkx**, **franken_node**, **franken_markdown**, **franken_term** all have claim-ledger or claim-manifest machinery that machine-checks *marketing wording* (see §6) — franken_snowflake's 18-vs-21-lane and 420-vs-576-test drift shows it lacks exactly that class of gate.

---

## 4. SHARED HURDLES

**H1. Bus factor 1, by explicit policy.** Universal. Franken_snowflake's README "About Contributions" refuses outside contributions [Verified, High] — the same verbatim refusal appears in franken_agent_detection, franken_code_browser, franken_lean, franken_ocr, franken_overlap, franken_pandas, franken_tts, frankenfs ("I do not accept outside contributions for any of my projects"), frankenterm ("no outside contributions accepted … explicit, deliberate policy"), frankentui, franken_markdown, franken_networkx ("contributions accepted"? — no: asupersync's README L2620 "I do not accept outside contributions for any of my projects… it's my name on the thing" [Maintainer claim, High]), frankenfs, frankensqlite, franken_torch ("bus factor 1 [External + Git-observed, High]"), franken_whisper, franken_sim, franken_graphdb, franken_remote, franken_surveillance, franken_drone ("bus factor 1 (sole author Jeffrey Emanuel; 31 stars, 3 forks)" [External, High]), franken_threed, franken_nlp ("bus factor 1 by policy"), franken_alignment, franken_manim, franken_native_capsule, franken_sympy, franken_libc, franken_markdown_website, franken_tui_website, franken_sim_website, beads dashboards, franken_engine, franken_node. Succession is a program-wide unsolved problem; franken_snowflake's 62,062-line tree on a pinned nightly (`nightly-2026-08-31`) with "no contributor on-ramp" is the standard configuration.

**H2. The license rider as adoption/validation ceiling.** See P5 above — franken_snowflake shares the non-OSI MIT+rider with ~30 of 44. The rider withholds *benchmarking and analysis*, which "chills the independent validation the project most needs" [Inference, Medium] — consistent with P3's program-wide zero-independent-validation finding. The two packets that escape (franken_agent_detection's plain MIT, franken_native_capsule's unlicensed state) are the only governance experiments in the program, and franken_agent_detection's is the telling one: it is "also the only one with a documented downstream integration (CASS) and 21k downloads" — the natural experiment suggests the rider correlates with the adoption ceiling.

**H3. Asupersync version risk.** Franken_snowflake is pinned at asupersync `=0.3.5` with an open migration bead (`fsnow-agent-ergonomic-cli-ufb`: "Migrate the native runtime and optional integration graph to Asupersync 0.5") [Verified, High], while **frankensqlite** "runs its storage I/O on asupersync 0.5.0, a real registry-published async runtime dependency" [Verified in Cargo.lock with checksum, High], **franken_remote** consumes "asupersync 0.5.0 … as *published* crates" [Code-verified, High], and **frankensearch** mandates asupersync [Code-verified in manifests, High]. Franken_snowflake is the laggard of its own constellation's runtime upgrade, and its packet flags this as "a known liability, not a hidden one" [Verified, High].

**H4. No independent benchmark against the incumbent.** Franken_snowflake has "no published benchmark against any competitor or baseline" and no comparison of the asupersync transport vs hyper/reqwest/rustls [Verified, High]. Shared with: franken_markdown ("zero published head-to-head numbers vs pulldown-cmark/comrak"), franken_torch ("exactly one of fourteen vs-PyTorch lanes is quotable"), franken_networkx ("the headline speedups are overwhelmingly self-speedups"), franken_threed ("**zero speedup has been measured**"), franken_sympy, franken_ocr, franken_tts. Counter-examples that clear the bar: franken_whisper (incumbent binary "running side-by-side in the same invocation, SHA-256 identities for both ELFs, A/A nulls in [0.98, 1.02]" [Maintainer claim/Verified, High]), franken_libc ("same-invocation incumbent benchmarks"), franken_pandas ("143-lane certified benchmark gate … live incumbent same-invocation, per-arm A/A nulls" [Code-verified, High]). Franken_snowflake is in the majority bloc that has not done this.

**H5. Velocity vs review depth.** ~10 commits/day with a 62k-line tree only one human understands [Verified, High] — see P8. The packet's own forecast: "a slowdown without a successor doesn't stabilize the project — it strands 62,062 lines on aging pins … bit-rot sets in within quarters" [Inference, High].

---

## 5. GENUINE UNIQUENESS

Only things no other packet does — "also does X" items are labeled as such:

**U1. The dependency-admissibility gate (machine-checked dependency diet).** `scripts/check-dependency-admissibility.py` + `scripts/check-asupersync-single-version.sh`, cargo-tree across ~21 lanes incl. dev, failing on any forbidden crate, executed green in third-party CI on three OSes [Verified via Actions REST API, High]. No other packet has a dependency-diet gate. (Related but different: frankensearch's "NO tokio" Cargo.toml *comment* is prose policy; franken_drone/franken_surveillance have zero-external-crate *construction*; franken_networkx's CI-on-drift ledgers audit *coverage*.)

**U2. The compile-time secret-leak gate.** A `build.rs` that fails the build on credential-shaped fields deriving `Debug`, plus planted-canary redaction tests [Verified, High]. No other packet has a build-time secret-leak scanner. (Also-does-X: franken_node's manual `Debug` redaction impls, frankenterm's `redactor` crate, frankengraphdb's redacted `Debug` plans — all runtime-level and ungated.)

**U3. The explicit-arming write-safety ladder for agent-driven warehouse mutation.** `WRITE_ENABLED` opt-in → bare writes execute directly once armed → dry-run/confirm ceremony re-armable via `WRITE_REQUIRE_CONFIRM` → DDL behind `WRITE_ALLOW_DDL`, with deterministic confirmation tokens (`confirm:{kind}:{request_id}`) and typed refusals with exact repair commands [Verified in code, High]. No other packet implements an explicit-arming ladder for *data mutation*. (Also-does-X: frankenterm's policy engine has "approval tokens" and "policy-gated actions" — for terminal-mission control, not warehouse writes; franken_engine has capability algebras and declassification rules — substrate-level, not an ops ladder.)

**U4. The agent-contract layer as an integrated package for a data warehouse.** Deterministic versioned JSON envelopes with `data_source` provenance + self-describing capability registry with per-command JSON Schemas + MCP server sharing the CLI handlers + BLAKE3 content-addressed receipts + append-only audit log [all Verified in code, High]. Pieces exist elsewhere (franken_engine: signed evidence ledger + MMR; franken_alignment: receipt-bound execution evidence; franken_markdown: MCP stdio server; franken_node: threat model + validators; franken_whisper: run persistence), but the *packaged combination* — deterministic envelopes, capability discovery, explicit-arming writes, content-addressed execution receipts — has no equivalent in any other packet, and the packet's Wardley analysis found "no equivalent … in any incumbent connector or MCP database server" [Inference, Medium]. The domain itself is unoccupied: no other packet is a SQL-API connector or touches Snowflake.

**U5. The typed-skip live-proof contract (credential-gated CI as a formal mechanism).** Opt-in env + structured skip events (`franken_snowflake.live_gate.v1`) + spawned-process env sanitization + secret scan, so "no credentials" is never confused with "green" [Verified structure, High]. Near-misses exist (franken_ocr's skip-honest no-weights mode, franken_tts's GREEN WITH SKIPS doctrine, frankenterm's `skipped_not_proven` gates) but none is a typed per-run event contract for credential-gated testing.

**U6. The sustained policy-vs-tree CI contradiction.** AGENTS.md's explicit "never uses GitHub Actions" prohibition — already enforced once (workflow deleted at v0.0.3 after 52 parse-failed runs) — coexisting with a `.github/workflows/ci.yml` executing 140 runs by 2026-09-13 [Verified via Actions REST API + clone, High]. Others disable CI *consistently* (franken_overlap: "GitHub Actions is deliberately disabled" by policy [Code-verified, High]; franken_markdown: disabled workflow named "DISABLED"; frankenjax: zero workflows). A prohibition enforced once and then ignored for 140 runs is unique.

**U7. Tokio-free via asupersync as a machine-proven fact, not a stance.** The *stance* is shared (frankensearch mandates asupersync; frankensqlite and franken_remote consume 0.5.0). What is unique is the *proof*: a lock-file-verified, CI-executed demonstration that the whole 589-package graph — dev lanes included — contains zero forbidden crates. Frankensqlite/frankensearch/franken_remote assert the stance in manifests; franken_snowflake *proves* it.

---

## 6. CROSS-POLLINATION IN (apply to franken_snowflake)

**IN-1. Claim-discipline gate → from franken_markdown → fixes franken_snowflake's doc drift.**
*Concept:* `claims.tsv` + `check-claim-discipline.sh` "map README patterns to `capabilities --json` keys and proof scripts, failing when marketing drifts from code" [Verified, High — "the most rigorous marketing-hygiene mechanism observed in this program"].
*Why it fits:* franken_snowflake's drift is exactly the class this gate kills: README "420 tests" vs 576 counted [Verified, High], "18 lanes" vs 21 in the same document, and the "read + write live-success" badge vs an `in_progress` bead. Its `capabilities` command already emits machine-readable output, so the mapping target exists.
*Expected payoff:* badge-ahead-of-proof and count drift become mechanically impossible; the README stays revision-bound to the tree.

**IN-2. Live-recount claims manifest → from franken_node → fixes franken_snowflake's stale figures.**
*Concept:* `scripts/check_claims_manifest.py --check-honesty` runs live recounts against the tree (e.g., "3,756 → 4,106 integration tests … inside the manifest's tolerance bands") [Verified, High].
*Why it fits:* franken_snowflake's two flagship drift instances (test count, lane count) are *countable* — a manifest with tolerance bands catches 420→576 automatically, where prose review missed it for weeks.
*Expected payoff:* the numbers in the README/CHANGELOG are continuously re-verified; the "each minor, but the pattern is the governance signal" weakness shrinks to zero.

**IN-3. Result-class doctrine (self-speedups are maintenance) → from franken_whisper → disciplines franken_snowflake's decoder numbers.**
*Concept:* the PERF_LEDGER's formal result-class doctrine — "self-speedups are *maintenance*, not wins; a campaign win requires the actual incumbent binary running side-by-side in the same invocation" with "A/A nulls in [0.98, 1.02]" [Maintainer claim/Verified, High].
*Why it fits:* franken_snowflake's 350 MB/s figure is currently "a self-gated `assert!` … no A/A null, no core pinning" that its own packet says "Do not quote … as a result" [Verified, High]; and its next step 4 proposes an asupersync-vs-mainstream benchmark with no method. The doctrine supplies the method: reclassify 350 MB/s as a maintenance tripwire today, require A/A nulls + pinned hardware for the transport comparison.
*Expected payoff:* the honest-benchmark next step becomes executable with a pre-committed protocol; no future number can be quoted as a result without the null.

**IN-4. Sigstore-signed release attestation bundles → from frankenterm → hardens franken_snowflake's release story.**
*Concept:* "content-addressed, sigstore-signed release attestation bundles" with per-bead provenance and a claim registry (`docs/attestations/claim-registry.json`) [Code-verified, High] — "a claim-governance apparatus with no peer in this program."
*Why it fits:* franken_snowflake ships 16 multi-arch assets with checksums + manifest [Verified, High] but per-asset download counts of 2–4 and zero independent validation — its releases are *disciplined but unattested*. It already has the content-addressing habit (BLAKE3 receipts) and the bead tracker for provenance.
*Expected payoff:* releases become verifiable by third parties without the maintainer in the room; supports the receipt-provenance narrative (next step 1's tamper-evident ledger).

**IN-5. Documentation CI that rejects wording stronger than the claim matrix permits → from franken_lean → prevents franken_snowflake's badge tension.**
*Concept:* a "claim matrix with evidence states and documentation CI that rejects wording stronger than the matrix permits" [Verified, High] — franken_lean's B8 evidence-governance machinery.
*Why it fits:* franken_snowflake already has the raw material — the §4.3 claim inventory with demonstrated/partial/aspirational/stale statuses. Encoding that inventory as a machine-checked matrix means the README's "read + write live-success" wording would be auto-rejected while the e2e bead reads `in_progress` — the exact in-repo tension the packet found.
*Expected payoff:* the badge-vs-bead contradiction becomes unstateable in CI; the tracker is the single source of truth, which is what the honest-bead behavior (`fsnow-agent-ergonomic-cli-gap-docs-truth-79s` reopening the falsely-closed golden) already points toward.

---

## 7. CROSS-POLLINATION OUT (export from franken_snowflake)

**OUT-1. Dependency-admissibility gate → to frankenredis (named by the packet itself), frankensearch, franken_code_browser.**
*Concept:* `scripts/check-dependency-admissibility.py` — cargo-tree across every feature lane incl. dev, failing on any forbidden crate [Verified, High].
*Why it fits:* frankenredis is the packet's own named falsification target (next step 3: "apply the script to a second FrankenSuite repo with its own policy list (e.g. frankenredis)"); frankensearch's no-tokio stance is currently a *Cargo.toml comment* with no enforcement [Code-verified in manifests, High] — the comment is the policy that deserves the gate; franken_code_browser has a `DEPENDENCY_CONSTITUTION.md` whose constitution is "already violated in-tree" with zero CI to catch it [Verified, High] — it needs machine-checking most.
*Expected payoff:* frankenredis gets a machine-checked dependency story to match its measurement story; frankensearch's manifesto stance becomes provable; franken_code_browser gets its first real gate.

**OUT-2. Typed-skip live-proof contract → to frankengraphdb, franken_node, franken_remote.**
*Concept:* opt-in env + structured skip events instead of silent passes + spawned-process env sanitization + secret scan [Verified structure, High].
*Why it fits:* frankengraphdb has replication/chronicle durability lanes that will eventually need network/credential-gated integration tests; franken_node's CI is red at the pin partly on its "connector conformance" lane — structured skip events would separate "no credentials" from "failed"; franken_remote has "real QUIC/TLS integration tests against localhost" [Maintainer claim, Medium] whose unavailable-network case currently has no typed representation.
*Expected payoff:* credential-gated lanes stop conflating "skipped" with "green" program-wide; CI failures become attributable (missing credentials vs real regression).

**OUT-3. Explicit-arming write ladder → to frankenterm, franken_remote.**
*Concept:* `WRITE_ENABLED` opt-in arming → writes execute directly once armed → dry-run/confirm ceremony re-armable via `WRITE_REQUIRE_CONFIRM` → destructive-class ops behind a separate opt-in — "explicit arming, not always-on ceremony" [Verified in code, High].
*Why it fits:* frankenterm's policy engine already has "approval tokens" and "policy-gated actions" for agent missions [Code-verified, High] — the write ladder's key design insight (frictionless-by-default *once armed*, ceremony re-armable rather than always-on) is the missing UX semantic for its approval flow; franken_remote's `frd` daemon executes commands on remote hosts — destructive remote operations map directly onto the arming ladder (arm the profile, not the command).
*Expected payoff:* frankenterm's policy engine gains an explicit-arming semantic distinct from per-action approval; franken_remote gets a governed-destructive-ops story for agent-driven remote execution.

**OUT-4. Compile-time secret-leak gate → to frankensqlite, franken_node.**
*Concept:* `build.rs` credential-marker scan failing the build on credential-shaped `Debug` derives + planted-canary redaction tests [Verified, High].
*Why it fits:* frankensqlite's headline honesty exhibit is its encryption story — "an XChaCha20-Poly1305 DEK/KEK design lives in `fsqlite-pager` but `PRAGMA key`/`rekey` are not dispatched" [Verified, High] — key material in-tree is exactly what this gate guards; franken_node hand-writes `Debug` redaction impls today [Verified, High] and the gate would machine-check that no future field silently derives a leaking `Debug`.
*Expected payoff:* key/secret handling becomes compile-time enforced in the two repos where secrets are load-bearing; planted canaries give the redaction a falsifiable test.

**OUT-5. BLAKE3 content-addressed receipts + append-only audit log → to frankenterm, franken_remote.**
*Concept:* per-execution content-addressed receipts plus partition evidence and an append-only audit event, with `data_source` provenance on every envelope [Verified in code/docs, High].
*Why it fits:* frankenterm's "flight recorder/replay forensics" is the closest existing analog [Code-verified, High] — content-addressed receipts would make its forensics tamper-evident rather than merely recorded; franken_remote executes commands on remote hosts with no evidenced audit story — receipts give it one.
*Expected payoff:* frankenterm's forensics gain the hash-chaining starting point for the tamper-evident ledger (franken_snowflake's own next step 1); franken_remote gets an audit trail for remote agent execution it currently lacks.

---

## Caveats

- Cross-packet claims rest on keyword extraction + targeted greps over the 43 other assessments, not full reads; mechanism details were verified by quote but I did not re-open the underlying repos. Anything load-bearing for a decision should be re-checked against the named packet's §4.3/§4.7/§4.8.
- Evidence tiers are the originating packet's own labels, preserved as quoted; [External, High] findings carry the usual recall caveats (single web search, 2026-09-22).
- One asymmetry to note: franken_snowflake is the only packet whose assessed artifact is a *connector to an external vendor service* rather than a self-contained runtime/library — its live-proof burden (credentialed account, vendor dependency) has no direct analog in the other 43, which bounds how far cross-pollination of proof machinery can go.
