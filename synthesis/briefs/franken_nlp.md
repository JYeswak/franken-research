# Cross-Suite Brief: franken_nlp

**Analyst role:** cross-suite analyst (1 of 44). Own packet read in full: `~/workspace/franken-research/franken_nlp-assessment.md` (packet v5, pinned commit `30373ca0c7911784dd04865636eaf82f2f2dfab8`, assessed 2026-09-22). The other 43 packets skimmed via TL;DR, claim-inventory/benchmark/4.8-license/4.12-limitations extraction, plus targeted greps for mechanism keywords (claim matrices, check_claims, SUITE.lock, truth packs, admissibility gates, DSR).

**Evidence-tier legend (inherited from the own packet, Rulebook §1):** [Verified] direct inspection by the analyst · [Counted] analyst-ran count · [Code-verified] source read · [CI-observed] seen executing on CI pages · [Maintainer claim] asserted in repo materials, not independently executed · [External] independent sources · [Inference] analyst judgment, always labeled.

---

## 1. OWN-PACKET DISTILLATION

- **Verdict:** "The most rigorously self-annotated design-stage repository assessed in this program, whose engine has never executed and whose honesty machinery has." [Inference, Medium — comparative judgment over packets completed at write time]
- **TRL:** 2–3 — "Concept + analytical design complete (TRL 2 fully); experimental evidence limited to arithmetic/dependency probes, never a model-execution path (TRL 3 partial)" [Inference, Medium]
- **NODUS ring:** Monitor — "The Rulebook is explicit: Monitor is for plan-stage work. A 116K-line scaffold does not promote a design review to Explore" [Inference, Medium]
- **Strongest strength:** the evidence-governance machinery — inline evidence-state vocabulary ([OBSERVED@pin], [REPORTED], [TARGETED], etc.) across the 324 KB design plan, machine-checkable `fnlp-claim` annotations, `CLAIMS.json` (6 claims, all `targeted`, zero evidence digests), ledger schemas with a pre-registered rejection, a claims linter that currently reports FAIL on the pin's own tree, and a tensor census independently re-verified byte-for-byte against the live HuggingFace revision [all Verified/Code-verified, High]
- **Strongest ceiling:** "nothing executes end to end. No kernel has run, no model weights are present, the `production` feature graph does not exist, the DSR build authority is BLOCKED, GitHub Actions is disabled, there are no tags or releases, and the bus factor is 1 *by stated policy*." [Code-verified + Maintainer claim, High]

**The 3 most important pieces of evidence (with tiers):**

1. `check_claims.py --check` **executed 2026-09-22: runs, reports FAIL** on the pin's own tree (`src/cli.rs:26` — unannotated numeric/superlative wording) with exit 0 [Verified, High]. The honesty machinery bites its owner — and observes without enforcing ("documentation with a shebang").
2. The 201-tensor census (4,169,800,704 params) was **independently reproduced**: `gen_tensor_census.py --check-artifact` PASS, plus a live replay against the pinned HF revision (`f56ec5a9…`) — downloaded `config.json` (1,019 B) and `model.safetensors.index.json` (16,519 B), both SHA-256 hashes matching the script's hardcoded pins, `--check` PASS with missing=0/mismatched=0/extra=0 [Verified, High]. The strongest independent verification the assessment produced.
3. The zero-execution core: the README Performance section contains zero *measured* numbers by written policy — *"Every number below is a provisional gate, TARGETED rather than OBSERVED: no FrankenNLP performance number exists yet, because no kernel exists yet"* [Maintainer claim, High] — and the pin commit's own message states: "Compilation, Rust tests, model inference and physical crash/DSR qualification were not run" [Git-observed, High].

---

## 2. SHARED NEGATIVE PATTERNS

Patterns franken_nlp demonstrably shares with named sibling packets, with quoted packet evidence.

### Pattern A — The MIT + OpenAI/Anthropic rider (non-OSI, bars benchmarking/analyzing)
franken_nlp's LICENSE is "MIT License (with OpenAI/Anthropic Rider)"; "use" is defined to include "copying, modifying, merging, publishing, distributing, sublicensing, selling, transferring, making available, hosting, deploying, executing, **benchmarking, testing, analyzing, indexing**, or incorporating the Software or any Derivative Works into any dataset, training corpus, evaluation harness, or pipeline" [Code-verified (license text), High]. The own packet notes: "the rider bars as licensee conduct what the project practiced as method" — adversarial review round 3 ran on "GPT Pro deep reasoning," an OpenAI model, i.e., output of a Restricted Party under the project's own rider [Maintainer claim, High on the rounds' occurrence; the independence assessment is Inference, Medium]. The packet further flags the Suite-wide shape: "(The sibling FrankenRedis assessment in this program found the same pattern — Anthropic-model co-authorship under an anti-Anthropic rider — suggesting a Suite-wide posture: build with the labs' tools, bar the labs from the artifact.)"

**Shared with (41 of 44 packets carry the rider per keyword count; named here with their packet evidence):**
- frankenredis: "the license rider bars OpenAI/Anthropic and anyone acting for them from using, benchmarking, or even analyzing the code — killing any AI-company use case" [Maintainer claim/Inference]
- franken_ocr: "a non-OSI MIT+OpenAI/Anthropic-rider license that excludes the frontier AI labs from even benchmarking it"
- franken_tts: "a non-OSI MIT+OpenAI/Anthropic-rider license that bars exactly the two AI labs building agent-voice infrastructure from even benchmarking it"
- franken_whisper: "the MIT+OpenAI/Anthropic-rider license withholds all rights — including benchmarking and analysis — from the two leading AI labs"
- franken_numpy: "the license rider bars OpenAI, Anthropic, affiliates, and anyone acting for them from use *including benchmarking, testing, analyzing, and indexing* — killing independent validation and the agent-era evaluation story" [Code-verified (license text), High]
- franken_snowflake: "The MIT+OpenAI/Anthropic-rider license is **not OSI open source** and withholds even benchmarking rights from the two leading AI labs" [Verified, High] — "a hard ceiling for a project whose entire thesis is agent infrastructure"
- frankenpandas: "the license rider bars OpenAI/Anthropic (and anyone acting for them) from use/benchmarking/analysis and demands destroyed copies on breach" [Code-verified, High]
- frankenjax: "the license rider bars OpenAI/Anthropic and their agents from even *analyzing* the code — the same labs whose models co-authored 53% of recent commits"
- franken_term: "a custom MIT+OpenAI/Anthropic-rider license that denies all rights to the two leading AI labs — the precise demographic an agent-orchestration tool needs"
- asupersync: "the license rider names OpenAI and Anthropic and withholds even *benchmarking/analyzing* rights from them" [Code-verified (license text), High]
- Also attested in the packets for: franken_manim, franken_markdown, franken_threed, franken_remote, franken_overlap, franken_networkx, frankenlibc, frankenscipy, frankenmermaid, frankentorch, frankenfs, frankengit, frankentui, franken_node, franken_alignment, frankensympy, franken_sim, frankensearch, franken_surveillance_system.

**The pattern's second-order shape** (rider bars what the method used): franken_nlp's GPT-Pro review round; frankenredis's "Anthropic-model co-authorship under an anti-Anthropic rider"; frankenjax's "53% of recent commits" co-authored by the barred labs' models; frankenscipy (6,395 commits by a `claude` contributor, per its packet).

### Pattern B — Bus factor 1 by stated policy ("I do not accept outside contributions")
franken_nlp's README: *"I do not accept outside contributions for any of my projects. I simply don't have the mental bandwidth to review anything… I'll have Claude or Codex review submissions via `gh` and independently decide whether and how to address them."* [Maintainer claim, High]. All 827 commits are authored by Jeff Emanuel/Dicklesworthstone; 8 of the last 200 carry `Co-Authored-By` trailers and 1 commit is authored by "YellowRaven (Claude)" [Git-observed, High]. No succession plan, no second committer, no foundation [Inference, Medium].

**Shared with (packet-quoted):** franken_ocr ("a single maintainer who explicitly accepts no outside contributions"); franken_tts ("the single-maintainer / no-outside-contributions governance"); franken_whisper ("bus factor 1"); frankenpandas ("the README refuses outside contributions"); frankenfs ("bus factor 1 with an explicit no-contributions policy"); frankenterm ("bus factor 1 by **explicit, deliberate policy** (no outside contributions accepted)"); frankentui ("bus factor 1"); frankentorch ("bus factor 1"); frankenscipy ("bus factor 1"); frankenmermaid ("**bus factor 1 at extreme velocity** — one human, ~3,282 commits in ~7 months"); franken_markdown ("bus factor 1"); franken_manim ("bus factor 1 with outside contributions explicitly refused"); franken_networkx ("bus factor is 1 (single GitHub contributor)"); franken_overlap ("One human maintainer"); franken_snowflake; franken_numpy ("bus factor 1"); franken_node; frankenjax ("One human maintainer with an explicit no-outside-contributions policy"); franken_remote; frankensim ("Bus factor 1 + contributions-not-accepted policy"); frankenlibc ("the bus factor is one"); franken_alignment ("one maintainer, no release, no second committer"); franken_threed ("bus factor 1"); franken_drone; franken_surveillance_system ("Single human maintainer"); asupersync ("bus factor 1 with outside contributions explicitly refused"); franken_agent_detection ("bus factor 1 with an explicit no-outside-contributions policy").

### Pattern C — CI cannot certify the pin (red / disabled / deleted / never-green / unobservable)
franken_nlp: DSR build authority BLOCKED; GitHub Actions disabled (repo Actions API returned `enabled=false` in the project's own 2026-07-31 audit); CI workflow inert (`on: {}`); the claims linter FAILS on the pin's own tree [Code-verified/Maintainer claim, High].

**Shared with (packet-quoted):**
- frankenredis: "CI is red at the pin — conformance run #7679 failed at the `cargo fmt` gate (all substantive gates skipped) and the Tcl lane's scheduled run #284 failed at the verdict-verification step" [CI-observed, High]
- franken_ocr: "a governance decay signal — the CI workflows were deleted from the tree at HEAD and the last runs (2026-09-03) failed, while the docs still describe CI as the release gate"
- franken_agent_detection: "the main CI job is red at the pin — the `Format` step fails, so clippy/tests/packaging never executed on the assessed commit" [CI-observed, High]
- franken_node: "CI is red at the pin — all 8 push-triggered runs observed against the pin's head SHA concluded failure" [CI-observed, High]
- franken_numpy: "CI is **red at the pin** — the latest run (head = pin) fails at the G1 `cargo clippy -- -D warnings` gate with G2–G9 all skipped" [CI-observed, High]
- franken_engine: "The core CI lane is red at the pin… and all 46 recorded runs of the workflow (Sep 16–21) fail — it has never gone green" [CI-observed, High]
- franken_lean: "CI red at the pin, so the project's own gates cannot currently certify its own claims"
- frankensympy: "the build-and-test gate has failed 786 consecutive CI runs (last green 2026-08-23; pin fails at the fmt gate, unit tests never run)" [CI-observed, High]
- franken_surveillance_system: "CI history across all 622 recorded runs shows zero successes on the current 'portable qualification specification' workflow (570 cancelled, 47 failed)" [CI-observed, High]
- frankentorch: "the flagship `phase2c-reliability-gates` CI run against the pin failed in ~55 seconds" [CI-observed, High]
- frankentui: "GitHub Actions was disabled 2026-09-06 so all verification runs on maintainer-private DSR infrastructure invisible to outsiders"
- franken_overlap: "GitHub Actions is deliberately disabled, so no third party can observe test greenness; validation is owner-local via `scripts/ci-local.sh`"
- franken_drone: "the sole CI workflow is self-hosted and all 324 runs ever observed are queued/cancelled — including the run at the pin — so nothing has ever been shown green anywhere" [External, High]
- frankenfs: "CI red on the main workflow at HEAD"
- frankenpandas: "the live-oracle conformance job and `--all-features` lint are red" [CI-observed, High]
- franken_remote: "the pin has zero registered check-runs" [CI-observed, Medium]
- franken_alignment: "the pin commit's own message documents its newest tests as unexecuted ('rch not found (exit 127)')" [Git-observed, High]
- asupersync: "zero workflow runs exist at the pinned commit and the most recent runs (2026-09-07, older commits) show failures… so every behavioral claim about the assessed tree rests on maintainer-run DSR gates, unobservable externally" [CI-observed, High]

### Pattern D — README/status-surface drift and the present-tense problem
franken_nlp: the README's "note on tense" is honest, but the 55 KB of present-tense target description above it is quotable, and at the pin exactly **one** `fnlp-claim` annotation exists in the README (line 1) [Counted, High]; "the honesty relies on readers reading the note first" [Inference, Medium].

**Shared with (packet-quoted):**
- franken_lean: same "note on tense" README device (verified README text) — the disavowal device itself is shared
- franken_manim: "**the README is written in present tense as if the 1.0 design were fully realized; it is a spec of the target, not a description of the present**" [Verified, High]
- franken_networkx: "The README contradicts itself and the code on the most basic facts (118 vs 162 primitives, 15 vs 17 crates, 162,733 vs 459,703 lines)" [Counted, High]
- frankensympy: "the README's 'green workspace tests' line is stale-by-a-month" [Code-verified + CI-observed, High]
- franken_threed: "the public status surface is stale (README honest-status table still says 'Is there a compiler, renderer, or CLI? **No**' and lists 2 crates… AGENTS.md L17 still says 'zero implementation')" [Verified, High]
- franken_markdown_website: "the Lab Notes honesty strip and the README's deploy section are two engine versions stale (0.3.5 claimed vs 0.4.2 shipped vs 0.4.1 live), and the live page simultaneously claims both 0.4.1 and 0.3.5" [Verified, High]
- frankensqlite_website: "Four site claims are contradicted by the engine it markets" — including "'0 unsafe blocks / pure safe Rust throughout' vs 41 counted unsafe blocks… self-healing storage and page-level encryption marketed with copy-paste code examples, while the engine README says both are 'not yet reachable from the public API'" [Verified/Counted, High]
- frankentui: "the CHANGELOG describes a v0.9.0 release (2026-09-18) that exists on no surface GitHub tags, GitHub Releases, or crates.io can see"
- franken_drone: "README/IMPLEMENTATION_STATUS both claim '28 members' while the workspace has 30 (drift in the project's auditability pitch)" [Counted, High]
- franken_whisper: "CHANGELOG scope window 4 weeks stale at HEAD"
- franken_code_browser is the deliberate *counter-example*: "the only assessed repo that cannot be caught in README drift *by construction*, because it claims nothing is built"

### Pattern E — Campaign-pulse velocity, not cadence (burst commits, then quiet)
franken_nlp: 827 commits, of which **583 landed on a single day (2026-07-31)** [Git-observed, High] — "a campaign burst, not sustained velocity" — then sparse commits through the September code-first campaign; the August window nearly empty.

**Shared with (packet-quoted):** franken_code_browser ("206 commits across 2026-09-17→22, peaking at 101 commits on 2026-09-17 and already decelerating to 4 on pin day, with a zero-commit gap on 09-20" [Git-observed, High]); franken_node/franken_engine ("~20 commits/day in the sampled window," "96 on Sep 15 alone" [Git-observed, High]); frankenmermaid ("~3,282 commits in ~7 months"); franken_overlap ("347 commits in 21 days" then "18 days quiet at assessment" [Git-observed + External, High]); franken_surveillance_system ("~20 commits across 2026-09-21/22" [Git-observed, High]); franken_drone ("development stopped 2026-09-04"); frankenterm ("16,269 commits since 2026-01-18"); franken_scipy (6,395 commits by a `claude` contributor).

### Pattern F — Zero independent validation of any kind
franken_nlp: "there are no users, no releases, no independent coverage of franken_nlp beyond the repository itself" [External, High within recall caveats]. Shared with: frankenredis ("zero independent validation of any kind"); franken_term ("zero independent benchmarks, reviews, or production deployments found"); franken_tui ("zero independent validation, no crates.io/npm publication"); franken_tts ("zero independent validation of any kind"); franken_snowflake ("zero independent benchmarks, reviews, or production deployments found"); frankenscipy ("zero independent validation"); frankengit ("zero independent validation found"); franken_whisper ("no independent benchmark, review, or production deployment found").

---

## 3. CI/EVIDENCE BAR: what franken_nlp demonstrates that others lack

Mechanisms in franken_nlp with named peers and honest caveats about who is close.

1. **The self-biting claims linter.** `check_claims.py --check` was *executed by the analyst* and reports FAIL on the pin's own tree (`src/cli.rs:26`) with exit 0 [Verified, High]. Peers with claim machinery: franken_engine ("a binding claim-to-proof matrix (28 claims: 13 OBSERVED / 8 TARGETED / 7 HYPOTHESIS… that gates README wording in CI"), franken_lean ("machine-checked claim matrix… documentation CI that rejects wording stronger than the matrix permits"), franken_node (`scripts/check_claims_manifest.py --check-honesty` — executed by the analyst: "9 checks ok, 0 drifted" [Verified, High]), franken_markdown ("`claims.tsv` + `check-claim-discipline.sh` wire each README claim to a `capabilities --json` key"), frankengit ("a machine-checked claims registry whose verified rows cover narrow artifact-identity binding… auto-demoting claims registry"). **What they lack:** none of these packets shows a claim tool *failing on its own tree and the failure being the audited finding* — franken_node's check passes clean. franken_nlp is the only packet where the governance tool is verified self-indicting. (The flip side: it reports with exit 0 — it observes, it does not gate — so this is evidence discipline as honesty, not enforcement.)

2. **Analyst-executed byte-identical model-dossier replay.** `gen_tensor_census.py --check-artifact` PASS plus a live replay against the pinned HuggingFace revision: the 16,519-byte index + 1,019-byte config downloaded, SHA-256 hashes matching the script's hardcoded pins, `--check` PASS with missing=0/mismatched=0/extra=0, re-deriving the full 201-tensor, 4,169,800,704-parameter census from public data in seconds [Verified, High]. Peers with truth packs: franken_ocr ("a Phase −1 'truth pack' pinning the exact model source commit and fixture hashes") and franken_tts ("a pinned truth pack (HF weights @ `5d83992`, upstream source @ `022e286`)"). **What they lack:** neither packet records the analyst independently re-executing a model-dossier replay against the live upstream revision; franken_nlp's one-command reproducibility recipe was the one actually re-run.

3. **The numerics-profile claim vocabulary — the anti-benchmark-lie construction.** Plan §1.1.1 defines `hf-bf16-eager` (owns all "matches HF" claims; the pinned HF implementation is "a bf16 program with explicit cast points"), `diagnostic-f32` (token flips vs bf16 are *named fixtures, not build failures*), and versioned strict-quantized profiles with preregistered logit/argmax/token/task budgets that are "never inherited exactness" [Code-verified, High on existence; Maintainer claim, Medium on the semantics]. The packet: "it forbids the single most common ML-benchmark lie (comparing numbers across different numerical programs) by construction." The plan's §10 also carries a "nearest format peers" rule forbidding the phrase "matched quantization" — the rule "makes the PG-3/PG-4 'meet/beat' gates *harder to pass*, not easier." **No other packet shows this.** Nearest peers are mechanical, not conceptual: franken_ocr's "model-specific int8 kernels with bit-identical scalar oracles" and franken_whisper's "WER-gated" speedup claims. Neither encodes cast-point vocabulary into the claim system itself.

4. **Sentence-level inline evidence-state vocabulary across the design plan.** The 324 KB plan grades its own sentences with [OBSERVED@pin] / [REPORTED] / [EVIDENCED] / [PARTIAL] / [TARGETED] / [HYPOTHESIS], machine-checked via `fnlp-claim` annotations against `CLAIMS.json` [Code-verified, High]. Peers with claim matrices: franken_engine, franken_lean, franken_node, franken_markdown, frankengit (above). **What they lack:** all of those operate at the claim-row level (28 rows, TSV rows, manifest keys); none grades *every sentence of the design document itself* with a machine-checkable inline vocabulary. The plan-as-graded-artifact is unique to franken_nlp.

5. **Ledgers seeded with pre-registered reserved entries before any data exists.** PERF_LEDGER/NEGATIVE_EVIDENCE/DISCREPANCIES exist as schemas plus a NEGATIVE_EVIDENCE seed: `NE-AVX2-RAW-VPMADDUBSW-001`, a reserved "rejected by construction" entry, not a recorded loss [Code-verified, High]. The Suite-wide negative-evidence-ledger pattern is shared (franken_ocr, franken_tts, franken_whisper, frankenredis, frankenpandas, franken_numpy, franken_networkx, frankenmermaid, frankenscipy, frankentorch, frankenfs, frankenlibc, frankensqlite, frankengit, frankengraphdb, frankenjax, franken_sympy, franken_code_browser, franken_remote, franken_surveillance_system, frankensearch, frankenterm — all carry some ledger). **What they lack:** pre-registration of rejections in *empty* ledgers — every other ledger records losses after the fact; only franken_nlp reserves the entry before the fact.

**Correction to the own packet's comparative claim:** the packet's §4.4 states the `SUITE.lock` is "a machine-checked foundation-pin ledger no sibling packet has shown." Cross-suite reading falsifies that: franken_lean's packet shows `SUITE.lock` pinning "the Reference, Corpus, toolchain, and suite commits in one ceremony" [Verified, High], and franken_manim's packet shows `SUITE.lock` plus `governed_closure.rs` "enforcing rev == `SUITE.lock`… CI fails on unlisted packages" [Verified in manifest, High] — arguably the stronger enforcement. What remains distinctive about franken_nlp's variant is only the format-2 flavor (plan-audited vs selected revs with reason strings). Do not cite the uniqueness claim as written.

---

## 4. SHARED HURDLES (structural, with evidence)

1. **Bus factor 1 by policy, no succession.** Own packet §4.8: "no succession plan, no second committer, no foundation" [Inference, Medium]. Suite-wide (Pattern B above). The franken_lean packet names the mechanism precisely: "institutional un-adoptability — one maintainer who refuses outside contributions, a license rider that bars the AI labs most able to validate it from even benchmarking it, and CI red at the pin… These three are one mechanism, not three problems." That sentence applies to franken_nlp almost verbatim.

2. **The license rider as advancement blocker.** Own packet §4.13.6: for the stated mission the exclusion "costs little direct adoption but sabotages the *validation* mission: the two labs best equipped to benchmark or analyze the engine may not touch it." Shared with franken_numpy ("killing independent validation and the agent-era evaluation story"), franken_term ("the precise demographic an agent-orchestration tool needs"), franken_snowflake ("a hard ceiling for a project whose entire thesis is agent infrastructure"). frankenpandas' packet adjudicates the rider as a *durable* ceiling ("adopted at inception, survived review"). FrankenGitHub's classifier reports the tree as `other` / `NOASSERTION` [External, High] — "every downstream license scanner will flag or misclassify the tree."

3. **Velocity vs review depth.** The own packet's §4.8: "Review depth is unassessed: with no merged outside PRs possible, 'review' means the maintainer plus Claude/Codex via `gh`" [Maintainer claim, Medium]. The Suite's review layer is uniformly the maintainer plus agent personas: franken_nlp (8 of last 200 commits carry `Co-Authored-By` trailers; 1 commit authored by "YellowRaven (Claude)"); franken_numpy ("Single human maintainer plus an agent swarm"); frankenscipy ("6,395 commits by a `claude` contributor, 158 by the human owner"); franken_threed ("AI-agent-authored at scale with opaque provenance"); frankenredis ("plus agent personas"); franken_code_browser ("named agent personas in commit bodies"). franken_nlp's differentiator here is only that the 57 KB AGENTS.md swarm-operating system (bead-assignee locking so two agents never work the same issue, claim-annotation grammar, DSR receipt workflow) is written down — "the most complete solo-maintainer knowledge-transfer system in this program's packets" [Inference, Medium].

4. **Toolchain-pin bit-rot.** franken_nlp pins `nightly-2026-08-31` in `rust-toolchain.toml`; the packet's §4.11: the pin "will not stay buildable forever," and one decay trigger is that "nightly-2026-08-31 bit-rots before first execution." Shared with: frankenterm ("a pinned nightly toolchain"); frankenmermaid ("on a pinned nightly"); franken_node/franken_engine ("nightly-only Rust"); franken_lean (a "SUITE.lock/toolchain pin inconsistency (preflight expects nightly-2026-07-13 vs. functional run on nightly-2026-08-31)" is self-reported [Maintainer claim, High]).

5. **Private verification infrastructure invisible to outsiders.** franken_nlp: DSR BLOCKED, no `production` graph, maintainer-remote gates, GitHub Actions disabled. Shared with: frankenterm ("verification runs on maintainer-controlled RCH hosts that are not independently inspectable"); frankentui ("maintainer-private DSR infrastructure invisible to outsiders"); franken_alignment ("the gates require the maintainer's private RCH fleet so no independent party can reproduce them"); frankenjax ("every 'green' claim executes on the maintainer's local RCH fleet and is attested only by checked-in artifact JSON"); franken_node (all "green suite" claims rest on the maintainer's "strict remote" runs); asupersync ("every behavioral claim about the assessed tree rests on maintainer-run DSR gates, unobservable externally"); franken_remote ("remote-executed RCH gates at an earlier revision ran 236 tests… not independently re-run"). The Suite's CI story is uniformly: public CI red or absent, real qualification on private metal.

6. **The honest baseline improves monthly while the project is pre-execution.** franken_nlp: "upstream llama.cpp improves monthly and the Nanbeige authors ship their own `-b nanbeige42` serving branches — the 'real, maintained baseline' the README names gets faster and more convenient while `fnlp` is pre-execution" [Inference, Medium]. The structural analog across the Suite is the incumbent's convenience moat: frankengit vs "an incumbent (GitHub/git) whose moat is ecosystem, trust, and network effects, not code"; frankensearch vs search incumbents ("relevance quality is unproven"); franken_snowflake ("read + write live-success" bead still in_progress). The race-against-the-incumbent-while-unexecuted shape is shared; franken_nlp's version is the most acute because its baseline literally merged upstream support eight weeks before assessment.

---

## 5. GENUINE UNIQUENESS

Only claims that survive a check against all 43 other packets. Each is falsifiable against the named near-peer.

1. **A claims linter verified to fail on its own tree.** `check_claims.py --check` was executed and reports FAIL on the pin's own tree with exit 0 [Verified, High]. franken_node's `check_claims_manifest.py` passes (9 ok, 0 drifted); franken_engine's matrix gates in CI (presumed green); franken_markdown's discipline script has no recorded self-failure. No other packet shows a governance tool biting its owner. This is uniqueness as *observed self-indictment*, not as machinery.

2. **A README Performance section that is a gates table with zero measured numbers — by written policy.** *"no FrankenNLP performance number exists yet, because no kernel exists yet"* [Maintainer claim, High]. Nearest peer: frankensqlite's README, which "refuses numeric performance claims for current main" — a refusal, not a structured gates table. franken_threed states "zero speedup has been measured" as a line; franken_overlap has "an honest disavowal where the benchmark numbers would be"; franken_surveillance_system's PERF_LEDGER states "No runtime exists yet, so this ledger contains no performance wins." None combines a *named gate series (PG-0–PG-8) + explicitly hypothetical roofline table labeled "context, not promises"* in the Performance section itself. The form is unique.

3. **An analyst-executed, byte-identical, one-command model-dossier replay.** The tensor census re-derived from 17.5 KB of public downloads with hash-pinned verification [Verified, High] (see §1, evidence 2). franken_ocr and franken_tts both maintain pinned truth packs, but in neither packet did the analyst re-execute the replay against the live upstream revision. Unique as an *executed* reproducibility artifact.

4. **The numerics-profile claim vocabulary (cast-point contracts).** `hf-bf16-eager` owns "matches HF" claims; `diagnostic-f32` token flips are named fixtures, not build failures; quantized profiles carry preregistered budgets "never inherited exactness"; the "nearest format peers" rule forbids the phrase "matched quantization" [Code-verified, High on existence]. No other packet encodes numerical-program identity into its claim grammar. The nearest peers (franken_ocr's bit-identical scalar oracles, franken_whisper's WER gating) are measurement practices, not claim-system vocabulary.

5. **Per-sentence evidence-state grading of the design plan itself.** The 324 KB plan carries [OBSERVED@pin]/[REPORTED]/[EVIDENCED]/[PARTIAL]/[TARGETED]/[HYPOTHESIS] inline, machine-checkable via `fnlp-claim` [Code-verified, High]. franken_engine's claim-to-proof matrix (28 claims, CI-gated), franken_lean's claim matrix, franken_node's claims manifest, franken_markdown's claims.tsv, and frankengit's auto-demoting registry all operate at claim-row granularity. None grades every sentence of the governing design document.

6. **Pre-registered reserved entries in empty ledgers.** NEGATIVE_EVIDENCE seeded with `NE-AVX2-RAW-VPMADDUBSW-001`, a reserved "rejected by construction" entry [Code-verified, High]. Every other ledger in the Suite (20+ packets) records losses after the fact; none shows a pre-registered rejection awaiting its first loss.

**Explicitly not unique (shared, do not cite as distinctive):** the note-on-tense README (shared with franken_lean, franken_alignment's IMPLEMENTATION_STATUS.md, franken_manim's disclaimer); SUITE.lock as a mechanism (shared with franken_lean and franken_manim — see §3 correction); the negative-evidence ledger pattern itself (Suite-wide); the "measured-faster wins" ISA dispatch doctrine (inherited *from* franken_ocr's kernel campaign per the plan's own §4.1); the 57 KB AGENTS.md swarm OS (franken_code_browser's AGENTS.md names anti-fabrication defects with comparable specificity).

---

## 6. CROSS-POLLINATION IN (other packets → franken_nlp)

1. **CI-gated claim matrix → franken_engine → franken_nlp.** franken_engine's "binding claim-to-proof matrix (28 claims: 13 OBSERVED / 8 TARGETED / 7 HYPOTHESIS… that gates README wording in CI." franken_nlp already runs the linter and has the verified self-FAIL — this is its own "Franken-worthy next step 3" (wire `check_claims.py` into `scripts/check.sh` with nonzero exit on FAIL). **Why it fits:** the tool, the failure, and the fix location (`src/cli.rs:26`) are all known; franken_engine proves the CI-gating pattern works. **Expected payoff:** converts weakness §4.7.5 ("machinery that observes without enforcing is documentation with a shebang") into an enforced gate — the one change that makes the honesty machinery load-bearing.

2. **L0–L5 parity ladder with skip-honest no-weights mode → franken_ocr → franken_nlp.** franken_ocr's "L0–L5 parity ladder with skip-honest no-weights mode" plus its "measured parity receipts." **Why it fits:** franken_nlp's fidelity thesis (B1/B2, the `hf-bf16-eager` oracle-parity kill test of §4.13.8) is currently a plan sentence; the ladder is the receipt structure it needs, and the skip-honest mode matches franken_nlp's exact current state (probes exist, weights absent). **Expected payoff:** the Monitor→Explore ring-transition trigger ("first executed oracle-parity receipt… with a retained PASS DSR receipt") becomes a mechanically retainable artifact instead of an anecdote.

3. **Certified-lane benchmark discipline (mandatory A/A nulls, ELF-SHA-256 provenance, claim-coverage audit) → frankenmermaid → franken_nlp.** frankenmermaid's "726-row negative-evidence perf ledger with mandatory A/A nulls and ELF-SHA-256 provenance, a claim-coverage audit" and "a machine-readable contract for what counts as a vs-incumbent claim"; franken_whisper's parallel "campaign-win doctrine (self-speedups are *maintenance*, not wins; a campaign win requires the actual incumbent binary running side-by-side in the same invocation with A/A nulls in [0.98, 1.02])." **Why it fits:** franken_nlp's §10 measurement discipline (randomized paired A/B, per-regime distributions, the pinned `b77d646…` baseline commit) is specified but unexecuted; these are the enforcement patterns for it. **Expected payoff:** when PG-3/PG-4 finally run against the pinned llama.cpp support commit, the numbers arrive with provenance instead of needing retroactive justification — the "measured PG-0/PG-1 miss the roofline by >2×" downgrade trigger becomes adjudicable on day one.

4. **Marketing-hygiene-as-CI (`claims.tsv` + `check-claim-discipline.sh`) → franken_markdown → franken_nlp.** franken_markdown wires "each README claim to a `capabilities --json` key and a proof script — marketing hygiene as CI." **Why it fits:** franken_nlp's weakness §4.7.6 — 55 KB of present-tense target description with exactly one `fnlp-claim` annotation in the README, "quotable out of context," honesty "relies on readers reading the note first." **Expected payoff:** machine-checked README honesty closes the out-of-context quotation risk without relying on reader diligence; also forces the annotation coverage the packet admits is missing.

5. **Cargo-tree admissibility gate → franken_snowflake → franken_nlp.** franken_snowflake's "cargo-tree admissibility gate that fails the build if a forbidden crate appears in any of the ~21 feature lanes, dev included" [Verified, High], which proves a Tokio-free graph mechanically. **Why it fits:** franken_nlp's claim 11 — the CHANGELOG asserts the Rayon edge was audited as dev-only and "the release graph excludes Rayon by dependency-policy gate" — is explicitly "a changelog assertion, not a verified build fact" [Maintainer claim, Medium], and there is no `production` graph to check it against. **Expected payoff:** when the production graph lands, the no-tokio/no-rayon posture becomes a build fact rather than a design rule — the cheapest way to close the claim-11 gap.

(Alternate candidate not chosen: franken_alignment's "receipt-bound execution evidence (source-hash-bound JSON receipts per qualification batch)" — relevant to unblocking the DSR authority chain, but the DSR unblock is primarily an infrastructure problem, not a receipt-format problem, so it was deprioritized.)

---

## 7. CROSS-POLLINATION OUT (franken_nlp → other projects)

1. **The evidence-state vocabulary + `fnlp-claim` annotation scheme + `CLAIMS.json` + `check_claims.py` → franken_alignment, franken_drone.** franken_alignment is "a design-first research program (251 KB plan, 257 docs, 143 machine-readable work packets…)" whose README already "disclaims its own tense" and whose SLOs "self-label `all_values_are_unmeasured_proposals: true`" — the annotation scheme would formalize exactly that posture into machine-checkable form. franken_drone has a "maturity vocabulary (source-present → production-admitted) enforced in docs" plus a live README/IMPLEMENTATION_STATUS drift bug (28 claimed vs 30 counted members) — the linter would have caught that mechanically. **Why it fits them:** both are design-heavy, pre-execution, and already practicing the honesty informally; the grammar makes it enforceable. **Expected payoff:** franken_drone's drift class disappears; franken_alignment's 257 docs become claim-graded instead of prose-graded. (franken_nlp's own next step 1 proposes exactly this lift-and-adopt experiment.)

2. **The tensor-census replay recipe (`gen_tensor_census.py`) → franken_tts, franken_whisper, franken_ocr.** All three pin HuggingFace model revisions and maintain truth packs (franken_tts: "a pinned truth pack (HF weights @ `5d83992`, upstream source @ `022e286`)"; franken_ocr: "a Phase −1 'truth pack' pinning the exact model source commit and fixture hashes"). **Why it fits them:** each already does model-dossier diligence by hand; the hash-pinned one-command census replay makes the footprint re-derivable by a third party in seconds. **Expected payoff:** independent re-verification of each runtime's model footprint becomes a one-command act, and future upstream-revision drift breaks the replay mechanically ("no argument required").

3. **The SUITE.lock format-2 pattern (plan-audited vs selected revs with reason strings) → franken_whisper, franken_node, franken_sympy.** franken_whisper carries "heavy intra-suite coupling (Frankensqlite, FrankenTorch, FrankenTTS, FrankenTUI, FrankenJAX)"; franken_node's "bare clone does not compile" without the sibling `franken_engine` checkout; franken_sympy's "fresh clone does not build standalone (path deps on `../asupersync`, `../franken_numpy-pin`, `../frankenscipy-pin` are absent)." **Why it fits them:** each has undocumented or implicit sibling-revision coupling; the pin ledger with reason strings makes the foundation contract explicit and machine-checked. **Expected payoff:** the coupling becomes auditable and the "which rev of the sibling did you build against" question gets a one-file answer. (franken_lean and franken_manim already have SUITE.lock and are excluded as targets.)

4. **The numerics-profile claim vocabulary (cast-point contracts, named fixtures vs failures, preregistered budgets) → frankentorch, frankenscipy.** frankentorch is organized around a "Deterministic Autograd Contract: replayable, seeded, provenance-logged gradient computation" — the cast-point vocabulary would discipline which determinism claims each profile owns (its packet notes only "one of fourteen vs-PyTorch lanes is quotable" by its own rules). frankenscipy's CASP does "runtime algorithm selection by Bayesian expected-loss minimization against calibrated loss matrices" — the preregistered-budget idea maps directly onto calibrated solver claims. **Why it fits them:** both make numerical claims across precision/algorithm variants; both currently lack a vocabulary that distinguishes which profile owns which claim. **Expected payoff:** the "comparing numbers across different numerical programs" failure mode becomes structurally unstateable in their docs.

5. **The zero-measured-numbers Performance-section pattern + pre-registered reserved ledger entries → franken_overlap, franken_threed.** franken_overlap's central thesis "has no checked-in evidence run: no `proof.json`, `suite-status.json`, or `RESULTS.md` exists in the tree" — pre-registering the `fo-claim-gate` verdict rows now (supported/inconclusive/unsupported) and refusing performance numbers until they land would formalize its already-honest disavowal. franken_threed already states "**zero speedup has been measured**" — the PG-style gates table would give that honesty a structure that survives future measured results. **Why it fits them:** both are pre-evidence on their headline claims and already disclaiming informally; the pattern converts the disclaimer into machinery. **Expected payoff:** when their first real numbers arrive, they land in a schema that already distinguishes TARGETED from OBSERVED — no retroactive re-labeling.

---

## Caveats and open questions for the parent

- **Skim depth:** the 43 other packets were skimmed via extracted TL;DRs, NODUS factsheets, license/governance and limitation sections, plus keyword greps — not read in full. Cross-packet mechanism attributions (e.g., "no other packet shows X") rest on the extraction + greps; a mechanism buried in a packet's §4.4 or claim inventory that I did not extract could exist. The uniqueness claims in §5 are therefore "no evidence found in the skim," not logical proof.
- **The SUITE.lock correction (§3) is the one place this brief contradicts the own packet.** The own packet's "no sibling packet has shown" was a comparative judgment made before the full suite was assessed; the cross-read falsifies it (franken_lean, franken_manim). Flag to the parent in case the own packet gets revised.
- **Evidence tiers for other packets:** where I quote another packet, I preserved its tier labels when present in the extracted text; where the extraction truncated the tier, the quote is labeled with the packet's own wording. No facts about any project were invented — every project-level claim above traces to quoted packet text.
- **No live-browser work was required or performed;** all links in the own packet's Quick Links were verified resolving by that packet's analyst, not re-verified here.
