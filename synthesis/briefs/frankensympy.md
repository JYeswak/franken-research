# Cross-Suite Brief — frankensympy

Analyst packet: `frankensympy-assessment.md` (pin `55d176b8625a2ad8d4fc995bd076e0f582098d2f`, assessed 2026-09-22). Cross-read: all 43 sibling packets, focusing on TL;DR, claim inventory, verdict/TRL/NODUS, benchmark sections, limitations, license/governance. Every claim below is grounded in packet evidence; evidence tiers follow the Rulebook legend ([Counted], [Code-verified], [CI-observed], [Maintainer claim], [External], [Inference]; confidence High/Medium/Low). Quotes are from the cited packet.

---

## 1. OWN-PACKET DISTILLATION

- **Verdict:** "the most rigorously self-honest pre-certification codebase assessed in this program — a real Zassenhaus factorization pipeline, a real independent proof verifier, and a differential-oracle harness, all wrapped in a machine-readable claims registry that refuses to certify any of it — under a CI that has not gone green in a month and a license that forbids the most likely evaluators from touching it."
- **TRL:** 3–4 [Inference, Medium]. **NODUS ring:** Explore [Inference, Medium] — "the assignment rules are mechanical here: *Invest* requires independent validation plus governance — there is no independent validation; *Pilot* requires a release artifact plus a bounded real workload fit — there is no release and no workload; *Monitor* is for websites and plan-stage work — this is far past plan-stage."
- **Strongest strength:** the claim-governance and adversarial self-audit machinery — a machine-readable `registries/claims.toml` with 27 claims (1 documented, 15 implemented_uncertified, 11 planned; **zero validated, zero certified** [Counted, High]), `present_tense_allowed = false` defaults, a repo `AGENTS.md` that binds contributing agents to the registry with an explicit forbidden-claims list [Code-verified, High], and the revised Sept-8 reality-check audit that reads as a red-team report against its own author — whose P0 finding (fails-open corpus wrapper) is verifiably fixed at the pin [Code-verified, High].
- **Strongest ceiling:** the license rider — "bars OpenAI, Anthropic, affiliates, and anyone acting for them from use incl. benchmarking/analysis/training; breach = automatic termination" [Code-verified (license text), High], flagged **[FATAL]** in the skeptic's take: "a project whose entire value proposition is *independently verifiable evidence* forbids independent verification by the two organizations most capable of performing it." Compounded by bus factor 1 [Git-observed/External, High], no succession plan [Inference, Medium], and a build-and-test CI gate red for 786 consecutive runs [CI-observed, High].
- **Three most important pieces of evidence:**
  1. Full Zassenhaus factorization pipeline — Cantor–Zassenhaus equal-degree splitting, classical Hensel pair lifting, Zassenhaus recombination, multivariate polynomials supported — read across `crates/fsym-polys/src/factorization.rs` (1,700+ lines) [Code-verified, High].
  2. Independent proof kernel — `verify_derivation_independent` entry points, rule-application checkers, RealBall certificate checker; generator crates depend on the kernel, never vice versa (constitutional separation holds) [Code-verified, High]; certificate families gated by mutation tests (WS06/WS08/WS11 closures: 16/16 directed-rounding mutants killed [Maintainer claim, Medium — locally observed only, CI has not executed them in a month]).
  3. Pinned SymPy 1.14.0 differential oracle with a 230-fixture construction admission corpus and an exact-surface diagnostic — admission is construction-only; the Sep-8 audit's own diagnostic found **229/230 fixtures differing on MRO/pickle** [Code-verified, High]. The packet's verdict on "full compatibility": "aspirational."

## 2. SHARED NEGATIVE PATTERNS

**Pattern A — The AI-lab license rider.** Frankensympy's rider wording is the "standard FrankenSuite wording" per its own packet [Inference, Medium], and the restriction is documented in virtually every packet: frankenredis ("the license rider bars OpenAI/Anthropic and anyone acting for them from using, benchmarking, or even analyzing the code" [Inference, Medium]); franken_numpy ("the license rider bars OpenAI, Anthropic, affiliates, and anyone acting for them from use *including benchmarking, testing, analyzing, and indexing*" [Code-verified (license text), High]); frankenpandas ("the license rider bars OpenAI/Anthropic (and anyone acting for them) from use/benchmarking/analysis and demands destroyed copies on breach" [Code-verified, High]); franken_node ("the license rider bars OpenAI/Anthropic and anyone acting for them from use including benchmarking, testing, and analyzing" [Code-verified (license text), High]); franken_scipy ("a non-OSI MIT+OpenAI/Anthropic-rider license that denies all rights — including benchmarking and analysis — to OpenAI, Anthropic, their affiliates, and their agents" [Verified, High]); franken_whisper ("the MIT+OpenAI/Anthropic-rider license withholds all rights — including benchmarking and analysis — from the two leading AI labs"); franken_tts (same formulation); franken_manim (same); franken_lean ("a license rider that bars the AI labs most able to validate it from even benchmarking it"); franken_surveillance_system ("the MIT license carries the FrankenSuite rider naming OpenAI and Anthropic as forbidden parties for use including *analysis*" [Code-verified (license text), High]); frankenfs ("the MIT+OpenAI/Anthropic-rider license withholds *all* rights — including benchmarking and analysis — from OpenAI, Anthropic, their affiliates, and anyone acting for them" [Verified verbatim, High]); franken_engine ("its license names OpenAI and Anthropic — the builders of the agent systems it targets — as forbidden parties barred from even analyzing the code" [Code-verified (license text), High]). The packet-sharing evidence is strongest where the rider sabotages the project's own mission: frankensympy (evidence project forbidding evidence-gatherers), franken_whisper ("legally bars the frontier labs from even *analyzing* it — the exact audience an agent-era speech stack needs"), franken_search ("AGENTS.md explicitly addresses Claude Code *and* Codex/GPT-5.2 agents (Codex is OpenAI's)" while the license bars them — "naming the agent operators as forbidden parties is self-sabotage"), frankenjax ("the license rider bars OpenAI/Anthropic and their agents from even *analyzing* the code — the same labs whose models co-authored 53% of recent commits"). The frankenpandas packet shows the rider's intent is deliberate suite-wide: it "survived review in bead `frankenpandas-dio8`" [Inference, Medium].

**Pattern B — Red CI at the pin, often with the README claiming otherwise.** Frankensympy: build-and-test gate failed 786 consecutive runs; README claims "green workspace tests" (stale) [Code-verified + CI-observed, High]. Shared by: frankenredis ("CI is red at the pin — conformance run #7679 failed at the `cargo fmt` gate (all substantive gates skipped) and the Tcl lane's scheduled run #284 failed at the verdict-verification step" [CI-observed, High] — notably also failing at a *fmt* gate); franken_numpy ("CI is **red at the pin** — the latest run (head = pin) fails at the G1 `cargo clippy -- -D warnings` gate with G2–G9 all skipped, established 2026-09-22 via the Actions API" [CI-observed, High], "while the README badge still reads 'G1 green'"); franken_node ("CI is red at the pin — all 8 push-triggered runs observed against the pin's head SHA concluded failure" [CI-observed, High]); franken_lean ("CI red at the pin, so the project's own gates cannot currently certify its own claims"); frankenpandas ("the live-oracle conformance job and `--all-features` lint are red" [CI-observed, High]); frankenfs ("the main CI workflow is **red at HEAD**" [CI-observed, High]); franken_ocr ("the CI workflows were deleted from the tree at HEAD and the last runs (2026-09-03) failed"); franken_code_browser ("No product exists that a third party can run: no CI workflows (the `.github` directory contains only issue templates [Verified, High])"); frankenjax ("zero GitHub Actions workflows exist, so every 'green' claim executes on the maintainer's local RCH fleet and is attested only by checked-in artifact JSON" [External + Inference, High]); franken_whisper ("the tree contains **no CI configuration at all**"); franken_surveillance_system ("CI history across all 622 recorded runs shows zero successes on the current 'portable qualification specification' workflow" [CI-observed, High]).

**Pattern C — README-vs-code drift / stale present-tense claims.** Frankensympy: README "green workspace tests" vs 786 red runs [Code-verified + CI-observed, High]; also "the workspace grew past its own documentation in 33 days" (plan said 25 crates/~66k lines; pin has 29 crates/97k lines) [Inference, Medium]. Shared by: franken_networkx ("The README is a drift source (13-variant tie-break → 12 counted; 33 fuzz targets → 34 counted; delegation-ledger table 61/71/1113/92/338 → generated 31/150/1112/114/346…" [Counted, High]); frankenredis ("the README cannot keep its own crate count straight"); frankenjax ("The README contradicts itself and the code on the most basic facts (118 vs 162 primitives, 15 vs 17 crates, 162,733 vs 459,703 lines)" [Counted, High]); franken_surveillance_system ("The README badge claims 'runtime-Asupersync'; asupersync is absent from `Cargo.lock` and the tree contains zero `async fn`" [Code-verified, High]); franken_scipy ("the badge implies an async role the dependency does not have" — asupersync appears only in fsci-conformance RaptorQ encoding [Verified, High]); franken_manim (README "written in present tense as if the 1.0 design were fully realized; it is a spec of the target, not a description of the present" — self-disclaimed [Verified, High]); franken_whisper ("the GitHub repo description still advertises 'zero unsafe code' [External snippet, Medium] while HEAD's `src/` carries 33 annotated `unsafe` code lines"); frankenfs (changelog scope window 4 weeks stale; CI red while Artifact Gates pass).

**Pattern D — Bus factor 1, usually with outside contributions refused.** Frankensympy: "One human maintainer (Jeffrey Emanuel)… No succession plan, no second committer, no foundation, no release to fork from" [Inference, Medium]. Shared by: frankenredis ("there is one human maintainer and no bus factor"); franken_node ("bus factor 1" [External, High]); franken_numpy ("bus factor 1"); frankenpandas (README "explicitly refuses outside contributions" [Code-verified, High]); franken_scipy ("bus factor 1 (6,395 commits by a `claude` contributor, 158 by the human owner)"); franken_whisper ("bus factor 1"); franken_tts ("the single-maintainer / no-outside-contributions governance"); franken_manim ("bus factor 1 with outside contributions explicitly refused"); frankenfs ("bus factor 1 with an explicit no-contributions policy"); frankenjax ("One human maintainer with an explicit no-outside-contributions policy" [Maintainer claim, High]); franken_lean ("one maintainer who refuses outside contributions" — and calls the three ceiling items "one mechanism, not three problems"); franken_code_browser ("Single human maintainer (Jeffrey Emanuel) with named agent personas"); franken_search ("120/120 recent commits by one human… the operational knowledge lives in a 48 KB AGENTS.md, MCP agent mail, beads, cass, and RCH — an agent-swarm apparatus that a human successor cannot casually inherit" [Git-observed + Code-verified, High]).

**Pattern E — Fresh clone does not build / sibling path-dependency constellation.** Frankensympy: path deps on `../asupersync`, `../franken_numpy-pin`, `../frankenscipy-pin` are absent — "a cold reader cannot `cargo check` the workspace" [Code-verified, High]. Shared by: franken_node ("default builds require the sibling `franken_engine` checkout, so a bare clone does not compile" [Code-verified, High] — `path = "../../../franken_engine/crates/franken-engine"`); franken_whisper ("a Linux build *fails* without" sibling FrankenSuite checkouts — FrankenTorch kernels load-bearing); franken_code_browser ("franken_markdown is a local-path dev dep (`../../../franken_markdown`)" [Code-verified, High]). The franken_engine packet documents the doctrine's failure mode: the root `Cargo.toml` "carries a remarkable scar: the `[patch.crates-io]` block was removed 2026-07-25 (bead `bd-h5cl7`) after it silently rewired a sibling's `fsqlite` from 0.1.18 to a breaking 0.1.19 and took the default build red."

**Pattern F — "No upstream fallback" resting on status flags while upstream code runs in the product path.** Frankensympy: four `import sympy` delegation sites in the product dev shell (`series`/`integrate`/`limit` on base `Expr`, relation pretty-printing); the "never a hidden production fallback" posture "currently rests on a status flag, not on the code" [Code-verified, High]. Shared by: franken_numpy — the PyO3 extension "reaches 100% of `numpy.__all__` — partly via native Rust fast-paths, partly by identity-equal re-export of the live NumPy" and "the wheel declares `numpy>=2.3` as a runtime dependency" [Code-verified, High]; franken_whisper began as "a unified 10-stage pipeline (Bayesian backend routing…)" wrapping whisper.cpp/insanely-fast-whisper before replacing the backends in-process; frankenjax's `jit` is "semantically transparent (a verified no-op) — the 'compiler' is an interpreter" [Code-verified, High] — the advertised surface resting on a thinner mechanism.

**Pattern G — Impersonating "OpenAI File Downloader" user-agent wiring.** Frankensympy: its own `AGENTS.md` (§22) "instructs agents making web requests to set their user-agent string to `'OpenAI File Downloader, XaiImageApiFetch/1.0'` — an impersonating UA baked into binding repo guidance" [Code-verified, High]. Shared by franken_search: "the repo's installer sets an 'OpenAI File Downloader' user agent" [Code-verified, High] — in a repo whose license bars OpenAI while AGENTS.md courts Codex/GPT-5.2 agents.

**Pattern H — Construction-only / name-census parity numbers.** Frankensympy: 230/230 construction admission, but 229/230 differ on the exact-surface (MRO/pickle) comparator [Code-verified, High]. Shared by: franken_scipy ("'parity' is a name census, not assertion coverage — the maintainer's own README says a per-routine list of what each `diff_*` file asserts 'does not exist yet' and that 'referenced is weaker than compared'" [Verified, High]); frankenpandas ("the 119/119 and 1484/1484 Python-parity numerals are README-only" [Maintainer claim, Medium]).
## 3. CI/EVIDENCE BAR — what frankensympy demonstrates that others lack

**a) Paired-benchmark admission discipline (semantic admission *before* timing).** `tools/perf/paired_bench.py` implements: semantic admission before timing (divergent cases excluded from scoring but *kept in the outcome mix* — the 15-case report reads 11 admitted, 3 divergences, 1 error), per-side coefficient of variation ≤5% eligibility, an A/A null control on every run (1.027 verified in the Sep-6 report), and raw paired samples retained [Code-verified, High]. The packet judges: "the method is the strongest in the suite; the outcome does not support PERF-001's 'materially outperforms'" [Code-verified, High], and "FrankenSymPy's `tools/perf` harness is stricter than FrankenRedis's bench discipline" [Inference]. Who lacks it: frankenredis has A/A nulls (0.979–0.999), core pinning, and ELF SHA-256 pinning [Maintainer claim, Medium] but *no* semantic-admission-before-timing with divergences reported in the mix; franken_whisper has the campaign-win doctrine (incumbent binary side-by-side, A/A nulls in [0.98, 1.02], WER gating) but no admission/exclusion protocol; franken_tts has CV≤5% gates; franken_libc has dlmopen-protected incumbents, A/A nulls, bootstrap CIs but no semantic-admission stage; frankenpandas has certified lanes with same-invocation live incumbents and per-arm A/A nulls but no explicit divergence-admission protocol; franken_manim has "zero qualified performance evidence" outright. No other packet documents a two-stage admit-then-time contract with divergences excluded from scoring and retained in the report.

**b) Two-level conformance comparator (construction admission vs exact surface).** The 230/230 construction corpus is paired with an exact-surface diagnostic (MRO/pickle bytes) that found 229/230 differing [Code-verified, High] — a demonstrated way to keep a headline parity number honest by publishing the level at which it breaks. Who lacks it: frankenredis (5,041 differential probes), franken_numpy (differential conformance against a live NumPy oracle), frankenpandas (1,387 conformance packets), franken_scipy (795 conformance integration-test files), franken_networkx (1,092-file parity suite), frankenjax (861 JAX-oracle fixtures) — all have one comparator level; none documents an admission-vs-exactness split that downgrades its own headline.

**c) Zero-unsafe *including* the PyO3 bridge.** Zero `unsafe` blocks in the entire tree; 26 of 29 crates carry crate-root `#![forbid(unsafe_code)]`, the other three at module level [Counted, High] — with the Python bridge (`fsym-python`, PyO3 0.27) inside the zero census. Who lacks it: franken_numpy (642 unsafe sites, "all in `fnp-python`" [Counted, High] — layout-checked but real); franken_networkx (exactly 2 unsafe blocks, both in `fnx-python`'s `InstanceDictGc` CPython GC dict-slot surgery [Counted, High]); frankenredis (61 independently re-counted sites [Counted, High]); franken_whisper (33 annotated unsafe lines). Shared with: frankenpandas ("a genuine zero-`unsafe` census — all 15 crate roots `forbid(unsafe_code)`" [Counted, High]) and franken_scipy ("zero `unsafe` blocks" [Verified, High]) — so the mechanism is "also done by pandas/scipy," not unique; what distinguishes frankensympy is that it holds the zero line *while* carrying a PyO3 bridge and 29 crates, whereas numpy/networkx/redis/whisper allow unsafe at their FFI boundary.

**d) Machine-readable claims registry with agent-binding and an explicit forbidden-claims list.** `registries/claims.toml` (27 claims, 0 validated, 0 certified [Counted, High]) plus repo `AGENTS.md` binding contributing agents to the registry — "present-tense prose must agree with it," with an explicit forbidden-claims list (no drop-in compatibility, no live engine from file names, no proof-producing algorithms without a live verifier and mutation gate, no performance wins without live-incumbent evidence) [Code-verified, High]. Who lacks the agent-binding half: franken_engine has a 28-claim JSON ledger that "fails closed when a sentence's wording outruns its evidence" [Code-verified, High] but no documented agent-facing binding; franken_node has `docs/honesty_manifest.json` with an executable checker (9 checks ok at pin [Verified, High]) but no forbidden-claims list for agents; franken_overlap has `fo-claim-gate` (preregistered paired-bootstrap verdicts) but it is claim-gating, not prose-binding. **Honest caveat:** frankengit's registry is *more* advanced on automation — auto-demoting verified rows on artifact-digest change via `tools/registry-check` plus the `<!-- franken-claims-status -->` README marker block the checker "refuses to leave stale" [Verified, High] — while frankensympy's is static TOML with manual status edits and has no demotion wiring. Frankensympy leads on the *strictness posture* (zero certified claims + agent-bound forbidden list); frankengit leads on *enforcement automation*.

**e) Adversarial self-audit that caught and remediated its own fails-open harness.** The Sep-8 revised reality-check audit documents a P0: a crashed child emitting valid-but-empty JSON made the corpus wrapper exit 0 (fails open); at the pin `validate_report` enforces exact field-set match, child exit-status agreement, and corpus-partition completeness [Code-verified, High] — audit-to-remediation in ≤14 days. Who lacks it: frankenredis's reality-checks found crate-count/safety drift and disavowed its own tables, but no harness-fails-open finding-and-fix cycle is documented; franken_networkx's CLAIM_COVERAGE_AUDIT found 579/591 KEEP rows carry no vs-incumbent ratio [Code-verified, High] but did not publish a remediated gate; frankenfs's ledger admits 166 unattributed benchmark rows and installs a ratchet ("that count of unattributed rows may only fall") [Maintainer claim, Medium] — a remediation policy, but not a fixed fail-closed gate with code evidence. Frankensympy's F1 is the only documented case of a *harness integrity* P0 found by self-audit and fixed in-tree, with the fix verifiable at the pin.

## 4. SHARED HURDLES

- **Bus factor 1 with no succession document** — see Pattern D above; frankensympy's packet states it plainly: "If the maintainer stops, the project freezes at whatever commit the stop happens on" [Inference, High]. The evidence: 33 days of single-author history, 3 stars, 1 fork, 0 issues [External, High]; authorship opacity acknowledged ("the 33-day velocity (29 crates, 97k lines, 24 workstreams) suggests substantial agent assistance, but unlike FrankenRedis there are no co-authorship trailers to count" [Inference, Medium] — contrast frankenredis: "162 of 449 recent commits agent-co-authored"; franken_scipy: "6,395 commits by a `claude` contributor, 158 by the human owner"; frankenpandas: "overwhelmingly by Claude-family models from Anthropic, a named Restricted Party").
- **The license rider as strategy-incoherence** — see Pattern A. Frankensympy's version is the starkest: an evidence-and-verifiability project that "forbids evidence-gathering by the best-equipped evaluators" [Inference, High], and whose "analyzing" prong "arguably makes this packet itself a licensed activity" (§4.8). The frankenredis packet's remedy applies suite-wide: "Treat the license rider as a supply-chain screen: any FrankenSuite dependency candidate with a named-party restriction fails intake automatically" [Inference, High] — which would also disqualify frankensympy's asupersync/franken_numpy/franken_scipy sibling deps as program dependencies.
- **README drift from velocity-vs-review-depth imbalance** — see Pattern C. Frankensympy's specific shape: 25 crates/~66k lines → 29 crates/97k lines in 33 days while "the plan's architecture index is a living document chasing a moving target — drift is structural, not incidental" [Inference, Medium]. The franken_lean packet names the dynamic: "a machine-checked claim matrix… is a genuine methodological contribution" precisely because hand-written prose cannot survive velocity; the franken_surveillance_system packet shows prose disavowals failing to stop a present-tense badge from lying.
- **CI debt as the governance tripwire** — see Pattern B. Frankensympy's packet makes the point general: "A project whose pitch is auditability cannot keep its own front gate green for a month" — and its cheapest ring-moving action is "Running the formatter and confirming a green run #834+," which is "also the falsifiable test of whether 'green workspace tests' is a live claim or a memory" [Inference, High].
- **Constellation dependency fragility** — see Pattern E. Frankensympy's `registries/dependencies.toml` records the asupersync pin (`b3e08ac0b`) with "a prior full-workspace check/test artifact" [Code-verified, High], but a fresh clone still cannot build without three sibling checkouts. The franken_node packet shows the mature form of the same trap (optional path deps behind an `engine` feature, an asupersync-transport integration that "no CI workflow has ever compiled"), and franken_whisper shows the runtime cost ("a Linux build *fails* without" sibling checkouts).
- **Claim-about-outcomes resting on machinery that never executes** — frankensympy: 1,127 counted tests that have not executed in CI for 786 runs; the "55/55 proof-kernel tests, 41/41 polys tests, 16/16 directed-rounding mutants killed" WS closures are "Plausible but locally observed… treat as [Maintainer claim, Medium] until a green CI run re-executes them." Shared with: frankenjax ("every 'green' claim executes on the maintainer's local RCH fleet and is attested only by checked-in artifact JSON"), franken_search ("the real gate (`dsr quality`) was not observed executing — only its committed outputs were read"), franken_surveillance_system ("25 of the last 40 commits carry 'NOT RUN' disclaimers (~63% of the recent window)" [Git-observed, High]).

## 5. GENUINE UNIQUENESS — things no other packet does

(Each checked against all 43 sibling packets; "also does X" cases are labeled as such.)

1. **A claims registry that certifies nothing.** `registries/claims.toml`: 27 claims, 15 `implemented_uncertified`, 11 `planned`, **zero validated, zero certified** [Counted, High] — combined with AGENTS.md binding contributing agents to the registry and an explicit forbidden-claims list [Code-verified, High]. FrankenGit has a claims registry but carries 6 verified rows and auto-demotion [Verified, High]; franken_node's honesty manifest passes its own 9 checks at the pin [Verified, High]; franken_engine's 28-claim ledger fails closed but exists to enforce, not to refuse certification. The registry-as-permanent-non-certification stance — a ledger whose job is to refuse every claim indefinitely until gates pass — appears only here.
2. **An independent proof kernel for symbolic computation with mutation-tested certificate families.** `verify_derivation_independent` entry points, rule-application checkers, RealBall certificate checker, generator-kernel dependency direction enforced in manifests [Code-verified, High]; certificate families gated by mutation testing (16/16 directed-rounding mutants killed [Maintainer claim, Medium]). FrankenLean has a Lean 4.32.0 proof lane (four narrow authority theorems under three named boundary assumptions [Verified, High]) and FrankenGit has a Lean proof lane too — but neither is a CAS proof kernel with mutation-gated certificate families; franken_engine has a VEF proof generator/verifier with no documented mutation gates; no other packet's verifier is both independent-by-construction and mutation-gated.
3. **The dual-lane Python-shell → proof-carrying-native-kernel architecture.** A Python compatibility shell that preserves SymPy's actual object model (ordinary classes, metaclasses, `evaluate=False` held forms, `_eval_*` hooks, singleton atoms like `Half`, pickle round-trips) while eligible expression regions lower into a deterministic, proof-carrying native kernel (typed `TermId`s, BLAKE3 content-addressed interning, independent verifier promoting candidates to accepted results) [Code-verified, High]. Franken_numpy/frankenpandas/franken_networkx do PyO3 bindings with partial native fast-paths and partial re-export of the live incumbent [Code-verified, High] — a compatibility *bridge*, not a shell-plus-verified-kernel split. No other packet lowers a preserved dynamic object model into a proof-carrying kernel.
4. **Paired benchmarking with semantic admission before timing, divergences excluded from scoring but retained in the outcome mix.** The 15-case report (11 admitted, 3 divergences, 1 error) with per-side cv≤5% eligibility, A/A null 1.027, and raw paired samples retained [Code-verified, High] — including the most honest single line in the suite: `integer_arith_rational` failed on *both* engines for opposite stated resource bounds ("RecursionError: expression nesting depth 4097 exceeds the bridge bound 4096" vs the oracle's "Exceeds the limit (4300 digits) for integer string conversion") and the report kept the case unscored instead of scoring it. Franken_redis, franken_whisper, franken_libc, frankenpandas, and franken_tts all have A/A-null / CV-gate bench discipline [Maintainer claim, Medium–High], but none documents semantic-admission-before-timing with an explicit divergence-in-mix rule.
5. **A fails-open conformance harness found, documented, and fixed by the project's own adversarial audit.** The Sep-8 reality-check's F1 (crashed child emitting valid-but-empty JSON → wrapper exit 0) with a ≤14-day remediation verifiable at the pin via `validate_report`'s field-set match, exit-status agreement, and partition-completeness checks [Code-verified, High]. Self-audit *culture* is shared (frankenredis reality-checks, franken_networkx CLAIM_COVERAGE_AUDIT, frankenfs worker-scope re-scoping, franken_man IM's IMPLEMENTATION_STATUS disclaimers) — the fails-open-harness-found-and-fixed-in-tree mechanism is not documented anywhere else.
6. **The "no upstream fallback" posture enforced by registry status rather than code, with the delegation sites enumerated.** Four `import sympy` delegation sites in the product dev shell, explicitly distinguished from oracle usage and from the shim importing *as* sympy (drop-in environment working as designed) [Code-verified, High]. Franken_numpy's re-export-of-live-NumPy is analogous in effect but is presented as surface-completeness strategy, not as a status-gated posture with enumerated sites. The enumeration-plus-status-flag discipline is unique.
## 6. CROSS-POLLINATION IN — concepts from other packets frankensympy should adopt

**Concept 1: Auto-demoting claims registry with README enforcement.**
→ Origin: **frankengit** — a machine-checked claims registry (claims.tsv) whose verified rows "automatically demote… the moment a required artifact's digest no longer matches," wired through `tools/registry-check` plus a `<!-- franken-claims-status -->` README marker block that the checker "refuses to leave stale" [Verified, High]; 6 verified claim rows; 34-row negative-evidence registry with prose/TSV correspondence rules.
→ Why it fits: frankensympy already has the registry (`claims.toml`) and the AGENTS.md agent-binding, but status changes are manual edits and the README already drifted once (the "green workspace tests" stale line [Code-verified + CI-observed, High]). The `xtask` gate/receipt validators are the natural enforcement point.
→ Expected payoff: claim hygiene becomes machine-checked rather than authorial. The stale-README line that currently requires an analyst to catch would demote automatically the moment the gate bundle's digest changes — the registry biting its author, the behavior frankensympy's packet already praises as its core governance mechanism.

**Concept 2: Anchor-bound upstream contracts with drift-failing CI.**
→ Origin: **franken_lean** — "kernel rules anchored to upstream source lines with `expect=` tokens that fail CI on drift" [Verified, High]; ABI/olean censuses "mechanically extracted from the pinned Reference."
→ Why it fits: frankensympy's entire conformance story rests on a pinned SymPy 1.14.0 oracle (source pin `16fa8553…` [Maintainer claim, Medium]); the 230-fixture admission corpus and the MRO/pickle exact-surface diagnostic are asserted against that pin but nothing binds a conformance rule to the upstream source line it tests. When the oracle pin moves, drift is silent.
→ Expected payoff: oracle-version drift fails loudly instead of silently. The exact-surface diagnostic becomes a first-class drift sensor rather than a one-off audit artifact.

**Concept 3: A negative-evidence ledger of its own.**
→ Origin: **frankenredis** (26,485-line negative-evidence log — "every benchmark loss recorded with a verdict and artifact" [Counted/CI-observed, High]); **frankenpandas** (44,086-line ledger "with inline perf-claim retractions" [Counted, High], including the entry where "a claimed 1.20x win turned out to be build variance"); **franken_scipy** (44,078-line ledger plus a discrepancy catalog with IDs).
→ Why it fits: frankensympy currently has no negative-evidence ledger of its own — its packet only cross-references FrankenRedis's as a model ("the strongest claim-hygiene machinery seen in the suite after FrankenRedis's negative-evidence ledger"). It has un-ledgered losses: the paired report's geometric mean favors the oracle 0.616 [Code-verified, High] against PERF-001's "materially outperforms" status, 3 divergences, 1 error case, and 229/230 exact-surface differences.
→ Expected payoff: losses and retractions become searchable, dated, artifact-bound records instead of scattered audit findings — exactly the evidence posture the project sells.

**Concept 4: Decision-theoretic portfolio selection (CASP).**
→ Origin: **franken_scipy** — CASP: "runtime algorithm selection by Bayesian expected-loss minimization against calibrated loss matrices, with audit certificates and conformal-calibration fallback," wiring verified across five numerical domains (`solve_with_casp`, `spsolve_with_casp`, `minimize_with_casp_portfolio`, `solve_ivp_with_casp_portfolio`, `select_hypergeometric_branch_with_casp` each calling `select_action`; `ConformalCalibrator::new(0.05, 200)` in all five constructors) [Verified wiring, High; calibration quality Maintainer claim, Medium].
→ Why it fits: frankensympy's `fsym-runtime` (11,654 lines, the largest crate) already does portfolio execution — "factorization, Gröbner bases, integration, and solving race multiple strategies but only an independent verifier can promote a candidate to an accepted result" [Maintainer claim, Medium] — with audit certificates on both sides. CASP is the decision-theoretic upgrade of ad-hoc strategy racing.
→ Expected payoff: principled strategy selection with calibrated loss matrices and conformal fallback replacing unverified heuristics, while the existing kernel-verifier promotion gate stays the acceptance authority.

**Concept 5: Executable honesty checks in CI.**
→ Origin: **franken_node** — the repo's own `scripts/check_claims_manifest.py --check-honesty` executed against the pinned tree by the analyst: "9 checks ok, 0 drifted, with live recounts (3,756 → 4,106 integration tests; 21,621 → 22,421 inline; 146/146 fuzz targets; 437 → 438 validators; 0 unsafe blocks) inside the manifest's tolerance bands" [Verified, High].
→ Why it fits: frankensympy's claims machinery currently executes in the maintainer's process, not in CI — the `xtask` gate/receipt validators were "inspected, not run" [Not done], and the WS06/WS08/WS11 mutation-gate closures are "plausible but locally observed" [Maintainer claim, Medium]. CI is red at the fmt gate anyway.
→ Expected payoff: claims that verify themselves on every CI run. The 1,127 counted tests, the unsafe census, and the receipt-digest checks become CI-executed gates rather than audit-time artifacts — the falsifiable answer to "do the 1,127 tests actually pass at the pin?"

## 7. CROSS-POLLINATION OUT — frankensympy concepts to export to named projects

**Concept 1: `claims.toml` + AGENTS.md agent-binding with forbidden-claims list.**
→ Target: **franken_surveillance_system**, **franken_code_browser**, **franken_threed**.
→ Why it fits them: FSS has the suite's most developed prose honesty protocol (IMPORTANT-box disavowals, "no performance wins" ledgers, NOT RUN trailers) yet still shipped a present-tense README badge ("runtime-Asupersync") contradicted by zero `async fn` in the tree [Code-verified, High] — a machine-readable registry with agent-binding would have caught exactly that; its honesty is prose-deep, not machine-checked. franken_code_browser ships 206 commits in 5 days with no CI and claims nothing by construction; its AGENTS.md already names "demo hardcoding," "proof laundering," "refusal farming," and "commit pumping" as reopenable defects [Code-verified, High] — a claims registry would give those defect names a status machine. franken_threed is among the lightest-honesty-machinery Explore packets (TRL 3–4).
→ Expected payoff: present-tense claims become demotable data instead of README prose, directly answering the "README marketing dies here" problem at the projects most exposed to velocity-vs-prose drift.

**Concept 2: Paired-benchmark admission rules (semantic admission before timing; cv≤5% eligibility; A/A null; divergences excluded from scoring, retained in the mix; raw samples retained).**
→ Target: **franken_manim**, **frankenjax**.
→ Why it fits them: franken_manim's packet records "zero qualified performance evidence" and "performance at the promised scale is unproven," with 93 placeholder symbols among 2,036 reviewed API rows — it has no admission discipline at all, so any future benchmark would start from zero governance. frankenjax already keeps a negative-evidence perf ledger with criterion CIs [Code-verified, High] but lacks a documented admit-then-time contract with divergence handling.
→ Expected payoff: benchmark numbers that survive an independent rerun — the packet's own standard ("The numbers that would survive an independent rerun are the paired report's *method*"). For franken_manim this is the difference between future performance claims being evidence or marketing.

**Concept 3: Independent proof kernel with mutation-tested certificate families.**
→ Target: **franken_scipy** (CASP audit certificates / `SolveCertificate` types), **franken_engine** (VEF proof generator/verifier), **franken_lean** (independent kernel checker).
→ Why it fits them: franken_scipy's CASP already emits audit certificates with conformal fallback [Verified wiring, High] but nothing independently verifies them — the certificate producer and consumer are not constitutionally separated the way frankensympy's generator/kernel split is [Code-verified, High]. franken_engine's VEF generates proofs and verifies executions but has no documented mutation gates on the verifier side. franken_lean's dual-check (K1 + independent checker) is the closest existing analog and would gain the mutation-gate layer directly.
→ Expected payoff: certificates that mean something without trusting the producer — the verifier becomes the acceptance authority, and mutation testing proves the verifier actually bites. For CASP, this turns "audit certificate" from a format into a trust mechanism.

**Concept 4: The adversarial self-audit format (dated reality-check with P0–F4 findings, reproducers, and a revised superseding audit).**
→ Target: **franken_code_browser**, **franken_threed**, **franken_surveillance_system**.
→ Why it fits them: franken_code_browser's 206-commits-in-5-days velocity with no CI and locally-run "strict remote" suites is the highest-risk shape in the suite for self-deception, and its anti-fabrication doctrine is written but never exercised against a real finding. franken_threed (TRL 3–4) and FSS (437k lines, "NOT RUN" on 63% of recent commits [Git-observed, High]) both document incompleteness at high resolution without a structured adversarial format that names, reproduces, and re-checks findings — FSS's own ledger schema "demands revival conditions," which the reality-check format would operationalize.
→ Expected payoff: the audit trail becomes the project's falsifiability surface — a maintainer who publishes how their own harness failed open (frankensympy's F1) sets the cultural bar the high-velocity packets currently lack.

**Concept 5: Fail-closed wrapper validation (`validate_report`: exact field-set match, child exit-status agreement, corpus-partition completeness).**
→ Target: **franken_tts**, **franken_whisper**.
→ Why it fits them: franken_tts's seam-by-seam parity apparatus rests on a pinned truth pack (HF weights @ `5d83992`, upstream source @ `022e286`), a discrepancies ledger, and a negative-evidence ledger — and its own next-steps ask whether "the truth-pack cannot be aimed at a second model without a rewrite" (falsifiability of the harness as method). A fail-closed report validator is the harness-integrity layer that question presupposes. franken_whisper's 10-stage pipeline wraps multiple backends with Bayesian routing and NDJSON robot streaming but ships "no CI config in the tree" — its oracle/harness reports have no documented fail-closed validation, so a crashed backend child is exactly frankensympy's F1 shape.
→ Expected payoff: oracle/harness reports that cannot silently fail open — the validator makes "the harness disagreed" a detectable, typed event rather than a silent exit 0.

---

## Caveats

- All 44 packets share the same analyst-method ceiling: shallow clones, no compilation or test execution, no benchmark reproduction, and per-commit CI conclusions sampled rather than exhaustive. Frankensympy's packet is explicit: "the workspace was never compiled… no test was executed, no benchmark reproduced" [Not done].
- Comparative claims ("most honest," "strongest in the suite") are [Inference, Medium] judgments over the 44-packet corpus, not measured rankings — the same caveat every packet attaches.
- The "uniqueness" verdicts in §5 hold over the 44 assessed packets as of 2026-09-22; franken_threed, franken_tts, frankenfs, frankengit, frankenlibc, frankenscipy, frankenterm, and frankentui_website were ordered for launch the same day per memory — if any packet is revised, re-verify §§5–7 against the new text.
