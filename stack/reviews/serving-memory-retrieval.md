<!-- Review record, published as written. Review of 7 verdicts (author VerdictsServing) by ReviewServing, with re-checks until signing. Scratch paths refer to the reviewer's machine. -->

# Review: model-serving and memory/retrieval verdicts (commit d189043)

Reviewer: ReviewServing. Author under review: VerdictsServing. Read-only pass; no verdict file was edited.
Rules applied: stack/METHOD.md v2 (16deb8c), RULEBOOK.md §1.
Scope: stack/inference-engines.md, quantization.md, structured-output.md, embedding-serving.md, agent-memory.md, rag-frameworks.md, vector-dbs.md.

## Summary

- Mechanical check: **7/7 files pass** — 224 citations, 0 failures (no missing file, no out-of-range line, no quote under 20 chars, no quote missing from its line). No citation-shaped text escaped the parser. `git diff d189043 -- stack/ ecosystem/ packets/ synthesis/` is empty, so the check matches the working tree.
- Review verdict: **0/7 files pass as-is; 36 findings** (4 P1, 17 P2, 15 P3), counting M6 from the rule-9 addendum at the end. P1 means a builder would act on something the evidence contradicts, or a FrankenSuite project is misrepresented. P2 means a tier or accuracy error, or a citation that does not carry its bullet. P3 means wording or a tag.
- Verdict order: all seven "Adopt and wrap" verdicts rest on a real, cited gap, so none is a reflex and none should change. But four files name individual incumbents whose evidence is existence, activity and file names only. Under METHOD step 1 those incumbents belong at Watch level (agent-memory M2, rag-frameworks R2, vector-dbs V3, and inference-engines I1 in part).
- Confidence (rule 3): all seven grades are consistent with the rule. No file claims High. The three Low grades (quantization, agent-memory, rag-frameworks) each cite the pack saying its evidence is thin or unread.
- Caveats carried (rule 6): **7/7 pass.** Every pack caveat that concerns a named incumbent or practice appears in "What we cannot say" with a citation to its line.
- Withdrawn claims (rule 7): no violation. rag CLAIM-13 (WITHDRAWN) is echoed at rag-frameworks:40 only as a limit carried from _evidence/rag-frameworks.md:35, not as a claim. inference CLAIM-14 is not used.

### The three most important findings

1. **quantization.md:26** credits torchao `benchmarks/benchmark_gptq.py` with "quantize, report quality delta, report speedup". My fresh read shows it only times `gptq_quantize` on random CUDA matrices; it prints no perplexity, eval or speedup. The cited quote is the pack's "Copy:" recommendation, not a description of torchao, and the pack describes the file as "times GPTQ quantize + profiles". Related: :25 lists torchao's `1xH100_tests.yml` as a "GPU numerics nightly" example, but the author's own fresh read at :17 (and mine) shows it runs on `push` and `pull_request`.
2. **rag-frameworks.md:23** presents LightRAG's `offline_retrieval_check.py` as "a frozen retrieval oracle checked offline". The script's docstring says it "uses a small deterministic lexical ranker. It does not start LightRAG, call the API server, compute embeddings, or call LLM/RAGAS services." A builder will think LightRAG's retrieval is scored against the oracle; it is not.
3. **structured-output.md:22** says "Run one conformance suite against every backend", backed by a fresh read of outlines `tests/backends/test_backends.py`. That file only asserts `isinstance(processor, <Backend>LogitsProcessor)`, which is a dispatch test. The per-backend files make shallow checks: regex match, output starting with `{`, and a `name` key. None validates output against the schema, and they are three parallel files, not one suite. The verdict's own wrap rests on "no project verifies mask against schema", so overstating outlines weakens that.

Next in weight: **agent-memory.md:32** calls franken_agent_detection "the nearest" FrankenSuite project. frankengraphdb's packet names agent memory as its target workload and FalkorDB/Kùzu, which are Graphiti's own backends, as its rivals (M1).

## Fresh checks (item 2)

All reads ran on 2026-09-23 (local; 2026-09-24T02:54Z) with
`curl -sS -L -o <scratch>/review-serving-fetches/<name>.txt -w '%{http_code} %{size_download}' <URL>`.
The scratch copies sit in `review-serving-fetches/` beside this file.

| # | URL | HTTP | Bullet | Result |
|---|---|---|---|---|
| 1 | raw…/sgl-project/sglang/main/.github/workflows/nightly-test-nvidia.yml | 200 | inference-engines:16 | Confirmed: `cron: '0 14 */2 * *'` (L12); `runs-on: 4-gpu-h100` (L191). Note: `*/2` runs every second day, not nightly (see I3). |
| 2 | raw…/vllm-project/vllm/main/tests/basic_correctness/test_basic_correctness.py | 200 | inference-engines:24 | Confirmed: `hf_runner(model)`, `VllmRunner(...)`, `check_outputs_equal(... name_0="hf", name_1="vllm")` (L145–173). The HF side is not pinned in the file. |
| 3 | raw…/bitsandbytes-foundation/bitsandbytes/main/.github/workflows/tests-nightly.yml | 200 | quantization:16 | Confirmed: `cron: "15 2 * * *"` (L7); `gpu_type: [T4, A10, L40S]` (L68). |
| 4 | raw…/pytorch/ao/main/.github/workflows/1xH100_tests.yml | 200 | quantization:17 | Confirmed: `on: push` (main, gh/**) and `pull_request` (L3–11); runner `mt-l-x86iamx-22-225-h100`. Contradicts :25's use of it as a nightly example (Q2). |
| 5 | raw…/ggml-org/llama.cpp/master/tools/perplexity/README.md | 200 | quantization:24 | Confirmed: L8 "used primarily to judge the quality loss from e.g. quantized models vs. FP16"; KL divergence via `--kl-divergence-base` (L20–25). |
| 6 | raw…/dottxt-ai/outlines/main/tests/backends/test_backends.py | 200 | structured-output:22 | Partly supports: it imports the outlines_core, llguidance and xgrammar backends, but its asserts are `isinstance` checks only (L72–120). Not conformance (S1). |
| 6b | …/tests/backends/test_xgrammar.py, test_llguidance.py, test_outlines_core.py | 200 ×3 | structured-output:22 | Each backend generates text and asserts `re.match(regex, …)`, `response[0] == "{"` and `"name" in response`. These are shallow semantic checks, with no schema validation. |
| 7 | raw…/mem0ai/mem0/main/.github/workflows/ci.yml | 200 | agent-memory:25 | Confirmed: job `changelog_check` (L11); `::error file=pyproject.toml::… docs/changelog/sdk.mdx was not updated` then `exit 1` (L46–47). |
| 8 | raw…/run-llama/llama_index/main/llama-index-core/llama_index/core/evaluation/faithfulness.py | 200 | rag-frameworks:15 | Confirmed: `class FaithfulnessEvaluator(BaseEvaluator)` (L98), `async def aevaluate(` (L159). |
| 9 | raw…/langchain-ai/langchain/master/.github/workflows/_test_vcr.yml | 200 | rag-frameworks:22 | Confirmed: L1 "Runs VCR cassette-backed integration tests in playback-only mode", L3 "No API keys needed", `OPENAI_API_KEY: sk-fake`. |
| 10 | raw…/HKUDS/LightRAG/main/lightrag/evaluation/offline_retrieval_check.py | 200 | rag-frameworks:23 | recall@k (L67) and reciprocal rank (L71) against `sample_retrieval_oracle.json` confirmed; **but the docstring (L4–5) says it scores a lexical ranker and "does not start LightRAG"** (R1). |
| 11 | raw…/deepset-ai/haystack/main/.github/workflows/tests.yml | 200 | rag-frameworks:28 | Confirmed: `integration-tests-linux:` (L219) with `needs: unit-tests` (L221). |
| 12 | api.github.com/repos/facebookresearch/faiss/contents/.github/workflows | 200 | vector-dbs:15 | Confirmed: listing includes `build-pull-request.yml`, `nightly.yml`, `index-io-backward-compatibility.yml`. |
| 13 | raw…/pgvector/pgvector/master/.github/workflows/build.yml | 200 | vector-dbs:16 | Confirmed: matrix `postgres: 20` down to `postgres: 13` (L11–25). |
| 14 | raw…/chroma-core/chroma/main/chromadb/test/property/invariants.py | 200 | vector-dbs:26 | Confirmed: `def count` (L109), `ids_match` (L173), `embeddings_match` (L196), `no_duplicates` (L202), `_exact_distances` (L207). |
| 15 | raw…/qdrant/qdrant/master/tests/openapi_consistency_check.sh | 200 | vector-dbs:28 | Confirmed: L7 "This tests makes sure the generated OpenAPI files are consistent with its sources". |
| 16 | raw…/erikbern/ann-benchmarks/main/README.md | 200 | vector-dbs:12,42 | Confirmed: L4 "ann-benchmarks is no longer actively maintained … such as VIBE". |
| 17 | raw…/pytorch/ao/main/benchmarks/benchmark_gptq.py | 200 | quantization:26 | **Contradicts the bullet**: 76 lines; `run()` times `gptq_quantize` on `torch.randn` tensors and prints `gptq_quantize avg time`; no quality or speedup output (Q1). |

Result: the author cited 15 URLs as fresh reads, and I re-fetched all 15. Thirteen support their bullets as written; row 1 carries a wording note (I3). Two support their bullets only in part (outlines #6, LightRAG #10). Rows 6b, 16 and 17 are my own extra reads. Row 17 contradicts a bullet (quantization:26) that had no fresh read behind it.

---

## stack/inference-engines.md — 9 findings

Checks: support mostly sound. Verdict order PASS: the gap is the unaudited shared benchmark (pickup-inference-engines.md:86-87) plus "no engine run"; vLLM (test body read) and SGLang (workflow read) have process evidence, so not Watch; Adopt and wrap is correct. Confidence Medium PASS. Caveats 34–38 all carried: PASS. Fairness: see I4–I7.

1. **[P2] :17 llama.cpp — tier and a contested claim.** "its perplexity and bench tools are the baseline the serving engines measure against" is tagged [Verified], but the quoted line is the pack author's characterization, not an inspected fact. The pack's only differential test for a serving engine runs against HF transformers, not llama.cpp (_evidence:22), and the companion rates the related claim CONTESTED (pickup-inference-engines.md:127). In this pack llama.cpp's own process evidence is file names only.
   Fix: "…; ships in-repo perplexity and llama-bench tools, which the pack author calls the baseline serving engines measure against (the companion rates that claim contested). [Verified] [Inference] (ecosystem/pickup/_evidence/inference-engines.md:26 "Benchmark harness shipped inside the library, not just as scripts."; ecosystem/pickup/_evidence/inference-engines.md:12 "define the correctness/perf baseline the serving engines measure against"; ecosystem/pickup/pickup-inference-engines.md:127 "A field reference engine can carry the category's perf baseline without any enabled automated perf gate")". For process evidence, optionally add ecosystem/pickup/_evidence/structured-output.md:17 "asserts byte-exact expected grammar text for given JSON schemas".
2. **[P3] :15 vLLM "whose benchmark script other projects clone" [Verified].** "Other projects clone" is the pack author's characterization; the companion calls it a convention (pickup:86-87). Fix: tag the clone clause [Inference], or say "the benchmark script SGLang, LMDeploy and TGI mirror with their own serving benchmarks" and add ecosystem/pickup/_evidence/inference-engines.md:18 "Standardized serving benchmark harness".
3. **[P3] :16 SGLang "nightly GPU CI per vendor".** The author's own fresh read, `cron: '0 14 */2 * *'`, fires every second day. Fix: "scheduled GPU CI per vendor (the NVIDIA workflow named nightly runs every second day, `cron: '0 14 */2 * *'`)".
4. **[P2] :34 franken_ocr — one quote carries nothing in the bullet, and the bullet's key facts are uncited.** "tagged binary releases for six platforms" supports no part of the bullet. The "3B OCR vision-language model plus a five-model zoo" has no quote, and "the CI workflows were deleted from the tre" is truncated mid-word. Fix: replace the six-platform quote with packets/franken_ocr-assessment.md:9 "a DeepSeek-OCR-derived 3B MoE VLM" and packets/franken_ocr-assessment.md:9 "a five-model zoo (Unlimited-OCR, GOT-OCR2, SmolVLM2, OneChart, Polyphonic-TrOMR)"; complete the quote to "the CI workflows were deleted from the tree at HEAD".
5. **[P2] :38 "All four are single-model runtimes" contradicts :34.** franken_ocr runs a five-model zoo (packet:9), and franken_whisper adds a Sortformer diarization port (packets/franken_whisper-assessment.md:9 "plus a Rust-native Sortformer 4-lane speaker diarization port"). Fix: "All four are fixed-model runtimes (each hard-codes the models it runs), not general multi-tenant serving engines; …".
6. **[P2 fairness] frankentorch is omitted.** It is the CPU kernel substrate named at :36, and franken_ocr depends on it by path. Add: "- **frankentorch**: Rust reimplementation of PyTorch CPU eager-mode semantics whose kernels franken_ocr, franken_whisper and frankensearch's reranker consume; TRL 4, NODUS ring Explore; CI red at the pin. [Verified] [Inference] (packets/frankentorch-assessment.md:20 "A from-scratch Rust reimplementation of PyTorch's CPU eager-mode semantics"; packets/franken_ocr-assessment.md:72 "the sibling frankentorch kernel crate (path dep)"; packets/frankentorch-assessment.md:23 "NODUS ring: Explore [Inference, Medium] (TRL 4"; packets/frankentorch-assessment.md:231 "CI red at the pin — the G1..G8 gate topology failed")".
7. **[P3] :36 franken_whisper "replaced the speech backends it once wrapped".** The packet says it wraps them *and* ships the in-process engine as the default. Fix: "ships an in-process Rust engine on FrankenTorch CPU kernels as the default in place of the backends it wraps" and add packets/franken_whisper-assessment.md:9 "that ships as the default".
8. **[P3] :26 "Starter kit: A2".** A2 is "Incumbent oracle pinned before implementation". Pinning a vendored dependency version is dependency hygiene, not an oracle pin. Fix: "Starter kit: none".
9. **[P3] :27 the quote is a bare filename, and "per quantized format" is uncited.** Fix: cite (ecosystem/pickup/_evidence/inference-engines.md:25 "Recorded precision/quantization baselines."; ecosystem/pickup/pickup-inference-engines.md:121 "per-format reference values, determinism tests").

## stack/quantization.md — 6 findings

Checks: Verdict order PASS with a note. The gap is real and cited: no numbers were verified (_evidence:32) and no negligibility threshold exists (pickup:377-378). Step 1 (Watch) is close, because no test body of a recommended incumbent was read, but the fresh reads of the bitsandbytes and torchao CI workflows are process evidence, so Adopt and wrap stands. Keep that basis explicit, as the author already does at :40. Confidence Low PASS: the pack says numbers were sampled and bodies not read. Caveats 31–35 all carried: PASS.

1. **[P1] :26 torchao "one command per method" is misattributed.** The quote is the pack's recommendation ("Copy: each method = script that (a) quantizes, (b) reports perplexity delta, (c) reports kernel speedup, all one command."), not a description of torchao; the pack describes the file as "times GPTQ quantize + profiles". My fresh read (table row 17) confirms it has no quality or speedup output. Fix: replace the bullet with "- **Pair every quantization script with a quality delta and a speedup**: the pack recommends one command that quantizes, reports the perplexity delta and reports kernel speedup; torchao's `benchmarks/benchmark_gptq.py` covers only the first step (a fresh read on 2026-09-23 of https://raw.githubusercontent.com/pytorch/ao/main/benchmarks/benchmark_gptq.py showed it timing `gptq_quantize` on random matrices, with no quality or speedup output). Starter kit: none. [Inference] [Verified] (ecosystem/pickup/_evidence/quantization.md:25 "Copy: each method = script that (a) quantizes, (b) reports perplexity delta, (c) reports kernel speedup, all one command."; ecosystem/pickup/_evidence/quantization.md:25 "times GPTQ quantize + profiles")".
2. **[P2] :25 "Build smoke on every PR, GPU numerics nightly" [Verified].** The quote is the pack's "Copy:" advice (the pack read no workflow bodies, :35), and torchao `1xH100_tests.yml` runs on `push`/`pull_request` per the author's own :17. Fix: tag [Inference] for the advice and keep bitsandbytes as the verified example ("bitsandbytes splits `tests-pr.yml` from `tests-nightly.yml`, whose nightly cron `15 2 * * *` runs on T4/A10/L40S"). Cite ecosystem/pickup/_evidence/quantization.md:24 "bitsandbytes: tests-pr.yml, tests-nightly.yml, test-runner.yml split by GPU availability". Drop torchao or name it as the counter-example that runs H100 tests per PR.
3. **[P2] :27 (and :30) — a contested claim is presented as [Verified].** "the companion found no quantization repo doing this" repeats CLAIM-12, which the companion rates T3, Low, CONTESTED, and the absence rests on tree listings. Fix: at :27 tag [Verified] for franken_ocr and [Inference] for the absence, and add "(the companion marks this as imported from FrankenSuite and contested)" with (ecosystem/pickup/pickup-quantization.md:119 "T3 | Low | CONTESTED"; ecosystem/pickup/pickup-quantization.md:123 "CONTESTED because no quantization repo in the evidence set demonstrates origin"). At :30 change "No quantization repo in the pack shows this" to "No quantization repo in the pack was shown doing this".
4. **[P3] :17 torchao "including a GPTQ implementation".** The pack places it under `prototype/`, and the verdict's own Revisit line depends on that. Fix: "including a prototype GPTQ implementation (`torchao/prototype/gptq/`)" with (ecosystem/pickup/_evidence/quantization.md:11 "torchao/prototype/gptq/ (GPTQ reference in framework)").
5. **[P2 fairness] frankentorch's int8 path is omitted.** It is the kernel crate franken_ocr uses, and it has its own dynamic-int8 exact-parity CI. Add: "- **frankentorch**: AVX-512 VNNI dynamic int8 CPU kernels with a dedicated int8 exact-parity CI workflow, which itself carries a literal GH_TOKEN hygiene finding; TRL 4, NODUS ring Explore. [Verified] [Inference] (packets/frankentorch-assessment.md:122 "dynamic_int8_x86.rs (8 sites: AVX-512 VNNI int8 paths, the pin's headline commit)"; packets/frankentorch-assessment.md:41 "Dynamic int8 exact-parity CI"; packets/frankentorch-assessment.md:174 "A literal GH_TOKEN in the CI workflow."; packets/frankentorch-assessment.md:23 "NODUS ring: Explore [Inference, Medium] (TRL 4")".
6. **[P3] :35 the int4 quote does not carry "gated and unvalidated".** Those words sit inside inner quotes the citation syntax cannot hold. The packet also records a released wasm-int4 artifact, which the bullet omits. Fix: cite (packets/franken_ocr-assessment.md:86 "int4 support | Aspirational (honestly labeled)") and add "(a wasm-int4 browser artifact is released, packets/franken_ocr-assessment.md:205 "wasm-int4 artifact released, playground committed")".

## stack/structured-output.md — 3 findings

Checks: Verdict order PASS. The gap (no differential fuzzer, _evidence:35) is real and cited, test bodies were read for xgrammar, llama.cpp and guidance, and Adopt and wrap is correct. Confidence Medium PASS. Caveats 35–39 carried (jsonformer is not named): PASS. Fairness: see S2.

1. **[P1] :22 outlines "one conformance suite against every backend".** The file freshly read is a dispatch test (table rows 6/6b). The per-backend files make shallow semantic checks with no schema validation. Fix: "- **Give every engine backend the same test shape**: outlines keeps one test file per backend (`tests/backends/test_xgrammar.py`, `test_llguidance.py`, `test_outlines_core.py`). Each generates under that engine and asserts regex matches and JSON-shaped output (first character `{`, a required key present); `test_backends.py` checks only that each backend name dispatches to its processor class (fresh read on 2026-09-23 of https://raw.githubusercontent.com/dottxt-ai/outlines/main/tests/backends/). None validates output against the schema; that suite is yours to write. Starter kit: none. [Verified] (ecosystem/pickup/_evidence/structured-output.md:16 "outlines (tests/backends/test_xgrammar.py, test_llguidance.py, test_outlines_core.py, test_backends.py)")".
2. **[P2] :32 "cannot link the C++/Python engines at all, as with franken_nlp".** franken_nlp excludes them by design, not by inability, and its own packet concedes the grammar half exists upstream. Fix: "- Build an engine only if your runtime forbids C/C++/Python dependencies by policy, as franken_nlp's pure-Rust single-model runtime does by design; its own packet notes llama.cpp already ships grammar-constrained generation, and franken_nlp's guarantee has not been observed. [Maintainer claim] [Inference] (packets/franken_nlp-assessment.md:59 "Why a rewrite, not a wrapper: the stated bet is specialization"; packets/franken_nlp-assessment.md:269 "llama.cpp already ships grammar-constrained generation (GBNF), so the grammar half of the appliance lane exists upstream"; packets/franken_nlp-assessment.md:91 "the construction guarantee is a design property, not an observed one")".
3. **[P3] :31 tagged [Verified] for a recommendation.** Fix: "[Verified] [Inference]" (the absence is verified within the sample; "build the harness" is inference), and add the fact-carrying quote (ecosystem/pickup/_evidence/structured-output.md:35 "No repo in this set runs a documented differential fuzzer").

## stack/embedding-serving.md — 4 findings

Checks: Verdict order PASS. The gaps (no revision pins, _evidence:42; no blocking quality gate, :40) are real and cited; raw files were read, so not Watch. Confidence Medium PASS. Caveats 40, 42, 43 and 45 are carried; 41 (FlagEmbedding) and 44 (ColBERT) concern items the verdict does not name: PASS. Fairness: frankensearch is portrayed accurately; see E3–E4.

1. **[P2] :12 the bottom line recommends infinity without its staleness.** Its last push was 2026-03-24 (_evidence:13), which the verdict itself flags at :42; the inference-engines verdict treats the equally stale TGI as a caveat, not a pick. Fix: drop infinity from the bottom-line list, or write "…or infinity (last pushed March 2026; confirm it is maintained first)".
2. **[P3] :27 the TEI and sentence-transformers practices are merged.** The pack says TEI commits toolchain and lockfiles and sentence-transformers pins actions to SHAs; neither is shown doing both. Fix: "TEI commits `rust-toolchain.toml`, `Cargo.lock` and `integration_tests/uv.lock`; sentence-transformers pins CI actions to commit SHAs." with (ecosystem/pickup/_evidence/embedding-serving.md:28 "TEI: rust-toolchain.toml, Cargo.lock, integration_tests/uv.lock"; ecosystem/pickup/_evidence/embedding-serving.md:28 "ST workflows pin actions to SHAs").
3. **[P3] :23 "as frankensearch's installer manifests do".** Line 141 describes pinned model manifests used for benchmark reproduction; the installer appears separately at packet:166. Fix: "as frankensearch's pinned model manifests do", or cite both lines (packets/frankensearch-assessment.md:141 "the 621 MB of pinned model artifacts"; packets/frankensearch-assessment.md:166 "a checksum-verifying installer that fails closed on missing models").
4. **[P3] :35 frankenterm tagged [Verified] with no execution caveat.** The packet rates it "Partially demonstrated (exists; not executed)". Fix: append "the code exists and was not executed by its assessor", as rag-frameworks:36 does, and add (packets/frankenterm-assessment.md:79 "Partially demonstrated (exists; not executed)").

## stack/agent-memory.md — 5 findings

Checks: Verdict order PASS for Mem0 and Graphiti. The gap (self-reported, managed-platform and ensemble scores, _evidence:36-37) is real and cited, and both have CI bodies read (mem0 ci.yml fresh; Graphiti unit_tests.yml command in the pack). Letta: see M2. Confidence Low PASS: the pack says adopter evidence is thin. Caveats 35–38 and 40 are carried; 39 (LangChain/AutoGen) concerns items not named: PASS.

1. **[P1 fairness] :32 "The nearest is franken_agent_detection" is wrong.** frankengraphdb's packet names agent memory as its target workload. It names FalkorDB and Kùzu forks as rivals, and those are the backends Graphiti's own CI switches (_evidence:22 "DISABLE_FALKORDB=1 DISABLE_KUZU=1"). Fix: keep "None of the FrankenSuite packets implements a memory layer" and add a bullet: "- **frankengraphdb** targets agent-memory and GraphRAG workloads as a graph database and names FalkorDB and Kùzu's forks, which are Graphiti backends, as rivals; what ships is an embedded store with a bounded read-only GQL slice, no server and no CLI; TRL 4, NODUS ring Explore. [Verified] [Maintainer claim] [Inference] (packets/frankengraphdb-assessment.md:59 "agent-memory and GraphRAG workloads needing transactional, time-travelable, branchable graph state"; packets/frankengraphdb-assessment.md:151 "FalkorDB (the agent-memory rival)"; packets/frankengraphdb-assessment.md:23 "there is no fgdbd server and no installable fgdb CLI as products"; packets/frankengraphdb-assessment.md:24 "NODUS ring: Explore [Inference, Medium] (TRL 4")". Optionally note frankensqlite's packet naming an "embedded agent-memory store" as a credible future (packets/frankensqlite-assessment.md:168 "the project's credible futures — embedded agent-memory store").
2. **[P2] :12/:17 Letta is recommended on existence and activity only.** The pack's Letta evidence is a README redirect plus workflow file names (_evidence:9-10); no Letta CI, test or code was read, so METHOD step 1 puts Letta at Watch level. Fix: drop "or Letta through letta-code" from the bottom line, and at :17 append "(existence, activity and CI file names only; its CI was not read)" with (ecosystem/pickup/_evidence/agent-memory.md:10 "CI includes nightly-update-smoke.yml, telegram-live-smoke.yml, per-agent-watcher workflows").
3. **[P2] :29 Build-only-if names a constraint no incumbent is shown to fail.** "Memory must stay on the machine" is not shown as an incumbent failure: Mem0's harness ships a local `ollama` provider config (ecosystem/pickup/_evidence/agent-memory.md:15 "configs/{azure-openai,ollama,openai}.yaml"). And "no incumbent in the pack is shown doing per-item provenance" is an absence the pack never examined. Fix: "- Every remembered item must carry verifiable origin, and your candidate incumbent cannot record it; the evidence pack did not examine incumbents for per-item provenance, so check yours first. The FrankenSuite substrate for it is unproven, and the companion's own starting point is small: …" (keep both existing citations; drop "stay on the machine" as the discriminator).
4. **[P3] :32 the CASS workload claim is the maintainer's.** packet:22 labels it "[Maintainer claim, Medium — the maintainer's own planning doc]". Fix: tags "[Verified] [Maintainer claim] [Inference]".
5. **[P3] :33 "the one FrankenSuite repo under plain MIT" has no citation carrying "the one".** Fix: add (synthesis/00-overview.md:15 "plain MIT, no rider 1/44 (franken_agent_detection)").

## stack/rag-frameworks.md — 2 findings

Checks: Verdict order PASS for LangChain, Haystack and LightRAG, which have process evidence read (fresh `_test_vcr.yml`, `tests.yml`, `offline_retrieval_check.py`, plus the LightRAG README thresholds read by the pack); see R2 for the rest. The gap (no judge freeze, pickup:362-363) is real and cited. Confidence Low PASS: the pack says it read by path, not source. Caveats 34–37 carried: PASS. Fairness: frankensearch, franken_nlp and frankenterm are portrayed accurately.

1. **[P1] :23 (and :18) LightRAG's offline check does not test LightRAG.** See fresh check row 10. Fix: at :23 append "The script scores a small built-in lexical ranker, not LightRAG itself (its docstring says it does not start LightRAG, call the API server, or compute embeddings); copy the frozen-oracle format and point the scorer at your own retriever." At :18 change "an offline retrieval check" to "an offline sanity check of its sample retrieval oracle".
2. **[P2] :12/:18/:19 GraphRAG and deepeval (and LlamaIndex's CI) are recommended on path evidence only.** The pack says file-level claims are "by path/name, not source-code review" (_evidence:36). No GraphRAG or deepeval CI, test or source was read, and LlamaIndex's evaluator source was read but not its CI or tests. Under step 1 those incumbents are Watch-level. Fix: keep the type verdict, and mark GraphRAG and deepeval "(existence and file layout only; CI and tests not read)" in their bullets and in the bottom line, citing (ecosystem/pickup/_evidence/rag-frameworks.md:36 "other file-level claims are by path/name, not source-code review").

## stack/vector-dbs.md — 6 findings

Checks: Verdict order PASS for pgvector, Qdrant, Chroma and faiss, which have fresh process reads plus the ann-benchmarks config read. The gap (harness sunset, filtered recall unverified) is real and cited. See V3 for the rest. Confidence Medium PASS but borderline: the pack's :43 marks the filter evidence partial. The wrap recommendation does not depend on vendors lacking a filter suite, so Medium holds. Caveats 41 and 43–47 are carried; 42 (bqann) is not used: PASS.

1. **[P2] :30 "Starter kit: A9" is the wrong item.** A9 is "Anti-reward-hacking law adopted" (starter-kit/CHECKLIST.md:84), and the site renders the title. The vendors' AGENTS.md/CLAUDE.md files are contributor playbooks, and nothing in the evidence shows anti-reward-hacking content. Fix: "Starter kit: none".
2. **[P2] :12 overstated absence and "everyone".** "a dedicated filtered-recall test suite was not found in any vendor repo" reads as the result of a search, but the pack looked at Qdrant only and says "did not verify". "the public benchmark harness everyone submitted to" is contradicted at :42 (Chroma and LanceDB never submitted). Fix: "…the public benchmark harness most server vendors submitted to is no longer maintained, and no dedicated filtered-recall suite was verified in any vendor repo (only Qdrant's filter-relevant tests were checked)". If a citation is wanted, use (ecosystem/pickup/_evidence/vector-dbs.md:43 "did not verify a dedicated filtered-recall correctness suite at file level"). Make the same "verified" wording change in Revisit :49.
3. **[P2] :12/:18/:20 Milvus, Weaviate, LanceDB and Vespa are recommended on metadata and file listings only.** None of the fresh reads covered them. Fix: mark them "(existence, activity and file layout only; CI and tests not read)" in the bottom line and their bullets; the type verdict stands on pgvector, Qdrant, Chroma and faiss.
4. **[P2] :33/:36 frankensearch-index is offered without its adoption cost.** The packet line already cited (289) continues with the toolchain restriction. Fix: at :36 append "it requires the pinned nightly toolchain, which excludes stable-Rust consumers, and carries the license rider" with (packets/frankensearch-assessment.md:289 "the pinned nightly-2026-08-31 toolchain (stable-Rust consumers are excluded)").
5. **[P3] :15 faiss "the ANN library other systems are measured against" [Verified].** This is the pack author's characterization. In the harness, faiss_hnsw is one of 55 participants. Fix: "the reference library the companion names as the exact-search ground-truth oracle" with (ecosystem/pickup/pickup-vector-dbs.md:76 "reference implementation / ground-truth"), or tag that clause [Inference].
6. **[P3 fairness, optional] frankengraphdb's retrieval layer is not mentioned.** It designs a hybrid ANN+BM25+graph operator that is not integrated. Add a one-liner under "Where FrankenSuite touches this": (packets/frankengraphdb-assessment.md:117 "the hybrid ANN+BM25+graph operator is not integrated").

---

## Reproduction notes

- Mechanical checker (inline Python, not committed). It extracts every `(<path>:<line>[-<line>] "<quote>")` from `git show d189043:stack/<f>.md`, reads the cited file at d189043, strips `*` and backticks from both sides, collapses whitespace, and requires the quote to be ≥20 chars and a substring of the cited line or range. Result: inference-engines 40, quantization 33, structured-output 26, embedding-serving 32, agent-memory 30, rag-frameworks 28, vector-dbs 35; 224 citations, 0 fail.
- The same checker validated all 57 quotes proposed in the fixes above against their cited lines: 57/57 present, each ≥20 chars.
- UNVERIFIED: whether the vLLM `test_correctness_mteb.py` gate runs per PR (the verdict already carries this as unknown); TEI's `integration-test.yaml` cron (not re-fetched).

## Evidence level of this review, and what I did not run

- Evidence level: [Verified] for every table row (live HTTP 200 reads at the stated time) and for the mechanical check (script output at d189043). Finding rationales that turn on analyst judgment are [Inference]: the verdict-order calls, whether a starter-kit mapping fits, and whether a FrankenSuite omission is unfair.
- Not run: no engine, library, benchmark or test suite named in any verdict was installed or executed; no repository was cloned; no CI run page was opened, so nothing here is [CI-observed]. The `tests/benchmarks/*` and `test_correctness_mteb.py` bodies in vLLM, TEI's workflows, Graphiti's `unit_tests.yml`, and letta-code's workflows were not re-read. After this report was written, `git diff --stat d189043 -- stack/` showed none of the seven verdict files changed; other stack files and METHOD.md had changed. No verdict file was edited; `reviewed_by` stays unset until the author's fixes are re-checked.
- Superseded by the addendum below. When first written, rule 9 was uncommitted and `stack/licenses.tsv` was absent. It has since landed at ce4186e, and the file is in the working tree at HEAD d24c92d.

## Addendum: METHOD rule 9 (licenses.tsv committed at ce4186e), expectations for the re-check

Cross-check run 2026-09-23 with inline Python. It parsed every `**owner/repo**` in the "Adopt, do not rebuild" sections of the seven files and compared them against `git show ce4186e:stack/licenses.tsv`. **Every adopted repository has a row, and names match exactly** [Verified]. The author must carry these non-permissive or status items, citing the row or a dated `gh api repos/<o>/<r> --jq .license` read, and say in "Build only if" whether each could justify building:

- inference-engines:18 and quantization:18, **NVIDIA/TensorRT-LLM**: `Apache-2.0 AND LicenseRef-LTX-2-Community`, permissive-with-conditions (the `ltx2/` path falls under the LTX-2 Community License).
- rag-frameworks:15, **run-llama/llama_index**: core MIT; several separately published integration packages are AGPL-3.0 or GPL.
- vector-dbs:20, **weaviate/weaviate**: `BSD-3-Clause AND LicenseRef-Weaviate`; the `wl/` directory is under the Weaviate license.
- agent-memory:18, **getzep/zep** (new accuracy finding, M6, P2): the licenses.tsv note records that the README now reads "Zep Cloud: Examples & Integrations" and "Zep Community Edition is no longer supported. Its code has been moved to the legacy/ folder". The bullet's "Zep's product repo" is therefore wrong. Fix: describe it as Zep Cloud's examples repository whose deprecated Community Edition sits in `legacy/`. Keep it only as the source of its LoCoMo and LongMemEval harnesses, not as an adoptable memory layer. With M6, agent-memory has 6 findings and the total is 36.

## Re-check against 3074f81 (2026-09-23)

**Mechanical check at 3074f81:** 7/7 pass [Verified]. This time the parser also covers `stack/licenses.tsv` citations: 307 citations, 0 failures, no unparsed path:line text. The working tree matched 3074f81 for all seven files before signing.

**Original 36 findings:** 36/36 resolved as specified [Verified by reading each line at 3074f81]. This includes the rule-9 license items (TensorRT-LLM LTX-2, llama_index copyleft integrations, Weaviate `wl/`) and M6 (Zep moved to Copy, with the Community Edition deprecation stated).

**Fresh license reads by the reviewer** (`gh api repos/<o>/<r> --jq '[.license.spdx_id, .archived] | @tsv'`, 2026-09-23, 14 repos) [Verified]:
- TensorRT-LLM and Weaviate return NOASSERTION; llama_index returns MIT; zep returns Apache-2.0. None of the four is archived.
- The adopted repos in structured-output and embedding-serving all return Apache-2.0 or MIT, matching their licenses.tsv rows.
- TensorRT-LLM's root LICENSE (L76–83) and its `ltx2/NOTICE` name the LTX-2 Community License for `tensorrt_llm/_torch/visual_gen/models/ltx2/`. Neither says "video".

**New frankentorch and frankengraphdb claims against their packets:**
- quantization:37 (frankentorch int8 kernels, exact-parity CI, GH_TOKEN, TRL 4 Explore) is correct and fully cited.
- inference-engines:39 is correct, but one of its facts is uncited in the bullet (R-I2).
- agent-memory:35 (frankengraphdb) is correct; two of its facts are uncited (R-M2).
- vector-dbs:40 (frankengraphdb hybrid operator not integrated) is correct and cited.
- R-I2 and R-M2 come from gaps in fix text I proposed myself.

**Signed (commit 785fee3):** stack/structured-output.md and stack/embedding-serving.md. The commit adds only the `reviewed_by: ReviewServing` / `review_date: 2026-09-23` lines (4 insertions).

**Returned unsigned, 9 residual items.** All are P3 (a citation gap or wording); none changes a verdict. Every proposed quote below was checked on its line at 3074f81.
1. **R-I1 inference-engines:31** and **R-Q1 quantization:18, :31**: "video-generation model directory" is not in the evidence. The path, the row and the fresh LICENSE/NOTICE reads support only visual generation. Fix: "a visual-generation model directory (`visual_gen/models/ltx2/`)".
2. **R-I2 inference-engines:39**: the frankensearch reranker's use of frankentorch is uncited here. Add (packets/frankensearch-assessment.md:119 "pure-Rust frankentorch int8 BERT by default"; packets/franken_whisper-assessment.md:9 "encoder/decoder transformer on FrankenTorch CPU kernels").
3. **R-Q2 quantization:31**: vLLM is adopted at :18, but its license row is uncited. Add (stack/licenses.tsv:99 "vllm-project/vllm Apache-2.0 permissive").
4. **R-M1 agent-memory:30**: "All adopted projects here are Apache-2.0 or MIT" rests only on a placeholder command (`repos/<owner>/<repo>`), not an exact one, and cites no row. Replace the parenthetical with (stack/licenses.tsv:52 "mem0ai/mem0 Apache-2.0 permissive"; stack/licenses.tsv:28 "getzep/graphiti Apache-2.0 permissive"; stack/licenses.tsv:47 "letta-ai/letta-code Apache-2.0 permissive"; stack/licenses.tsv:88 "SuperMemoryAI/supermemory MIT permissive"), and tag the bullet [Verified] [Inference].
5. **R-M2 agent-memory:35**: "Kùzu's forks" and "a bounded read-only GQL slice" are uncited. Add (packets/frankengraphdb-assessment.md:152 "Kùzu (archived) and its forks"; packets/frankengraphdb-assessment.md:23 "an embedded Rust composition crate with a bounded GQL read slice").
6. **R-R1 rag-frameworks:31**: "the other adopted frameworks are MIT or Apache-2.0" omits GraphRAG's and deepeval's rows. Add (stack/licenses.tsv:55 "microsoft/graphrag MIT permissive"; stack/licenses.tsv:17 "confident-ai/deepeval Apache-2.0 permissive").
7. **R-V1 vector-dbs:12**: LanceDB is recommended as an embedded library and called watch-level in the same sentence. Fix: "an embedded library (Chroma) for single-process apps".
8. **R-V2 vector-dbs:34**: the Milvus, LanceDB and Vespa licenses are uncited. Add (stack/licenses.tsv:60 "milvus-io/milvus Apache-2.0 permissive"; stack/licenses.tsv:43 "lancedb/lancedb Apache-2.0 permissive"; stack/licenses.tsv:98 "vespa-engine/vespa Apache-2.0 permissive").

Non-blocking note: structured-output:32 carries the same placeholder command as R-M1. That file cites all five rows, and my fresh reads match them, so I signed it.

Not run in the re-check: no engines or suites; no clones; no CI pages. The five unsigned files carry no `reviewed_by`.

## Final re-check against 35ca868 (2026-09-23)

- **Mechanical check at 35ca868:** 7/7 pass [Verified]. 321 citations, `stack/licenses.tsv` included, 0 failures, no unparsed path:line text. The two files signed in 785fee3 are unchanged since, and 785fee3 is an ancestor of HEAD.
- **Residual items:** 9/9 closed [Verified by reading each hunk]. The 3074f81→35ca868 diff touches only the lines named in R-I1, R-I2, R-Q1, R-Q2, R-M1, R-M2, R-R1, R-V1 and R-V2. The replacement quotes are the ones proposed above, and each is on its cited line.
- **Signed in 59fb66b:** inference-engines, quantization, agent-memory, rag-frameworks and vector-dbs. The commit adds only `reviewed_by: ReviewServing` / `review_date: 2026-09-23` to each (10 insertions, 0 deletions).
- **All seven verdicts are now signed:** 785fee3 covers structured-output and embedding-serving; 59fb66b covers the other five.
- Not run: no engines or suites; no clones; no CI pages. Gate K / K5 was not run by me; the site verify chain is the lead's job.
