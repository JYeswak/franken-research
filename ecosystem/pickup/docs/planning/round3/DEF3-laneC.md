# DEF3 — Lane C: steady-state mechanics + fabrication sweep (round 3)

*Lane scope: BEADS READY check mechanics (#1–#4), fabrication sweep, declaration-table style standard, ROUND_LOG grammar coverage. No reviewed files were edited.*

## Findings

DEF3-C-1 [P0] pickup-mcp.md:Gate profile / G6 entry — Invented mechanism: "Servers that execute tools/shell/filesystem operations route through the exorcist's classification operators regardless of language." (line 129). `_evidence/mcp.md` contains zero occurrences of "exorcist" (case-insensitive scan of the full 38-line evidence pack) and no classification-operator, quarantine-operator, or equivalent tool-execution routing mechanism anywhere in `_evidence/` (full-directory scan for "exorcist"/"classificat" returned no other hits). The mechanism is defined nowhere — not in the evidence pack, not elsewhere in pickup-mcp.md (single occurrence, line 129), not in the playbook. Round-2 lane B flagged it as an out-of-lane observation ("an unexplained, possibly invented mechanism — the same fabrication-risk class flagged in prior adversarial reviews", DEF2-laneB.md:215-216); it was never scored as a DEF and INTEGRATION2 contains no fix row for it, so the fabrication survived into the current corpus. This is the program's own P0 fabrication failure class. Fix: delete the clause "Servers that execute tools/shell/filesystem operations route through the exorcist's classification operators regardless of language." from the G6 entry (the entry is otherwise coherent as "advisory / N-A by language"); if a real tool-execution routing mechanism is intended, ground it in a cited evidence file before re-adding.

DEF3-C-2 [P2] pickup-agent-frameworks.md:198-202, pickup-mcp.md:157-158,162, pickup-multi-agent-protocols.md:148-149, pickup-workflow-orchestrators.md:188,192 — Shared-gate declaration tables use shorthand parentheticals instead of full shared-gates.md registry titles: 12 rows across these 4 tables (agent-frameworks: GATE-004 "(grammar conformance)", GATE-005 "(deterministic replay)", GATE-007 "(sandbox escape)", GATE-010 "(observability)", GATE-011 "(guardrails)"; mcp: GATE-004 "(grammar conformance)", GATE-006 "(protocol conformance)", GATE-017 "(tool-surface sync)"; multi-agent-protocols: GATE-006 "(protocol conformance)", GATE-017 "(tool-surface sync)"; workflow-orchestrators: GATE-005 "(deterministic replay)", GATE-017 "(tool-surface sync)"). Shorthand is the documented cause of the round-2 misnaming defects (DEF2-B-9..B-11, DEF2-C-2: e.g. a GATE-018 row carrying GATE-008's rationale — wrong gate ID wired to a load-bearing disposition — traceable to non-canonical names). These 4 are exactly the pre-round-2 declaration tables; INTEGRATION2's name-correction fixes (DEF2-B-9..B-11, C-2) were partial and left these 12 rows behind. NOTE on premise: the stated "10 new vs 11 older" split is not borne out mechanically — all 10 closer-added tables and all 7 round-2-added tables already use full titles; only these 4 (of the 11 pre-closer tables) carry shorthand, so the fix set is 4 tables / 12 rows, not 11 tables. Recommended standard: full shared-gates.md titles (case-insensitive match to the registry headers). Fix: normalize the 12 rows to full titles — GATE-004 → "(Structured-output grammar conformance + fuzz)", GATE-005 → "(Agent deterministic replay / trajectory)", GATE-006 → "(Protocol conformance + interop matrix)", GATE-007 → "(Sandbox escape + resource accounting)", GATE-010 → "(Trace completeness / privacy)", GATE-011 → "(Guardrail false-positive / false-negative)", GATE-017 → "(Spec / schema surface drift)".

DEF3-C-3 [P1] docs/planning/ROUND_LOG.md:whole file — The log does not conform to the playbook's machine-readable ROUND_LOG grammar. All 17 lines use an ad-hoc prose format ("S4 ROUND1 COMPLETE date=2026-09-23 findings=92 ...", "S5 REGISTRIES COMPLETE ..."); zero lines match any grammar line type (`round=`, `attest`, `unk`, `audit`, `stage-artifact`, `cert`, `def=`, `skip`, `reopen`, `kill` — ` | `-separated, lowercase keys). The BEADS READY certificate requires "the log parses against the grammar above (schema-valid)" as its first checkable attestation; with the current file that attestation cannot be satisfied, and none of the machine-checkable records the S5 checks reference (def records for the zero-open-P0/P1 check, unk records for the BLOCKS_PLAN check, audit result=pass, cert line) exist in the log. (Separate from the grammar-coverage question in (d) below: every attestation HAS a line representation in the grammar — the defect is that the actual log uses none of them.) Fix: rewrite ROUND_LOG.md as grammar-conformant records — backfill `round` lines for S4 rounds 1–3 with verdicts, `def` lines for all 173 DEF/DEF2 records with final statuses, `unk` lines for the UNK ledger (incl. the 25 BLOCKS_PLAN dispositions), at least one `audit` line, plus `stage-artifact`/`skip` records for S0–S3; the data sources already exist (id-index.tsv, blocks-plan-ledger.md, INTEGRATION.md, INTEGRATION2.md). No cert line may validate until this is done.

## Check verdicts

(a) Check #1 — zero open P0/P1: **PASS.** `id-index.tsv` parses to 0 DEF rows with severity P0/P1 and ledger_disposition OPEN/DEFERRED (DEF census: P0 FIXED 13, P1 FIXED 217, P2 FIXED 164, P2 DEFERRED/WONTFIX 8 — the two non-qualifying carry-forwards are P2-only as open-p0p1.md asserts). The 5 FIXED-but-flagged-as-new-defect P1s were re-verified against the current files and none describes a live defect: DEF2-A-1 — shared-gates.md:7 now reads "the set changes only by constitution amendment" (contradiction gone); DEF2-B-3 — pickup-structured-output.md:356-361 has UNK-2 at TARGETED with the S4-confirmation promotion predicate (not RESOLVED); DEF2-C-1 — pickup-workflow-orchestrators.md:304-305 registers "New gates — GATE-DW-2..3" with GATE-DW-1/4 marked RETIRED into GATE-005; DEF2-C-5 — pickup-computer-use.md:138 golden schema cites "shared GATE-008 fail-protocol" and "shared GATE-008 benchmark-pin" parameters; DEF2-C-6 — the companion's kit references register under GATE-008's benchmark-pin parameter (pickup-computer-use.md:151), and a corpus-wide scan finds no active (non-retired) GATE-CUA-03/04 citations.

(a) Check #2 — diff class: **SUBSTANTIVE confirmed.** The classification is a property of the recorded INTEGRATION2 diff, which changed claim tiers/rows (DEF2-C-4, C-13, D-5..D-9), UNK dispositions (B-14, C-8, B-3), gate definitions and retirements (D-1, C-7, D-11, C-1, A-2), and requirements/thresholds (D-3/D-4, C-12, C-5/C-6) — every category the playbook's POLISH/SUBSTANTIVE rule names as substantive. No subsequent edit can retroactively make that recorded diff POLISH (a reverting diff would itself be substantive). The five re-verified INTEGRATION2 fixes above also confirm the diff was applied, not rolled back.

(a) Check #3 — last diff POLISH: **does NOT hold.** The last integration diff is SUBSTANTIVE (see #2); there has been no complete S4 review round since that produced zero P0/P1 findings and changed only copy/format.

(a) Check #4 — no UNK-* with disposition BLOCKS_PLAN: **FAILS.** The ledger holds 25 BLOCKS_PLAN rows across 14 companions (blocks-plan-ledger.md); the check requires zero. Spot-verified live in current files (e.g. pickup-mcp.md:334 UNK-MCP-01, pickup-embedding-serving.md:341/351/355, pickup-voice-agents.md:413/423/433). No relabeling attempted. (Raw companion marker count is 30 vs the ledger's 25 — the S5 id-index dedupes to 25; either count is > 0.)

(b) Fabrication verdict: **INVENTED → P0 (DEF3-C-1).** "The exorcist's classification operators" has no support in `_evidence/mcp.md` (0 hits) or anywhere in `_evidence/`; no definition exists in the companion or playbook. It survived round 2 because the lane-B flag was out-of-lane and unscored, and INTEGRATION2 never touched it.

(d) ROUND_LOG grammar coverage of BEADS READY attestations: **grammar covers all — no P1 for missing representation.** Every certificate attestation maps to a line type: (1) schema-validity → the grammar itself; (2) zero OPEN/DEFERRED P0/P1 → `def` lines (severity/status fields); (3) no BLOCKS_PLAN unk → `unk` lines (disposition field); (4) latest audit result=pass → `audit` lines (result field); (5) S0–S3 stage-artifact or skip → `stage-artifact` / `skip` lines; (6) polish=yes → `cert` line polish field + `round` line verdict/polish fields. Signers, artifact+pin, round number, and pre-review attestations map to `cert.signers`, `cert` artifact/pin/round fields, and `attest` lines. The P1 filed is instead DEF3-C-3: the actual ROUND_LOG.md conforms to none of these line types.

## Counts

- P0: 1 (DEF3-C-1)
- P1: 1 (DEF3-C-3)
- P2: 1 (DEF3-C-2)
- Total: 3

No reviewed files were edited. All identifiers (gate IDs, DEF IDs, line numbers, disposition values) were copied from the cited files.
