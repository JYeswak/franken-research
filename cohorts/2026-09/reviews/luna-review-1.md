Reviewer: GPT-6-Luna via the control-plane pane, a different model family from the packet authors (Claude Opus 5.5). Review date: 2026-09-25.
Packet commits reviewed: f273e97 (readme_smoke_wt-pages screening), e353f1b (beads_bend), 7922e87 (annus-mirabilis.com). The review below is copied verbatim; the corrections it asks for were applied in the commit that added this file.

# Independent review — 2026-09 cohort packets

Review date: 2026-09-25. Target repo: `/Users/josh/Developer/franken-research`. This review is read-only against the three named packet commits and their public upstream pins. I did not edit, stage, commit, or push the target repo. No build, test, formatter, browser, or Bend proof run was performed.

## Verdicts

| Packet | Verdict | Basis |
|---|---|---|
| `cohorts/2026-09/readme_smoke_wt-pages-screening.md` | **ACCEPT-WITH-CORRECTIONS** | The screening failure is supported: predicates 2 and 4 fail. Correct the unsupported “meaning beads_viewer” provenance attribution in a `[Verified, High]` sentence. |
| `cohorts/2026-09/beads_bend-assessment.md` | **ACCEPT-WITH-CORRECTIONS** | Strong pin-level source/API work; correct the predicate-2 false pass, the `00:0x` timestamp placeholder, unlabelled judgment scores, and rider scope wording. |
| `cohorts/2026-09/annus-mirabilis.com-assessment.md` | **ACCEPT-WITH-CORRECTIONS** | Pin, CI, release, authorship, and six facsimile hashes corroborate. A website brief does not amend screening predicate 2; clarify that the ML-use restriction applies to Restricted Parties. |

## Re-derived claims

### 1. `readme_smoke_wt-pages`

- Pin `b45e54bafdcd7db48ad081ab14d25cb4dfc75743` exists; GitHub reports author/committer time `2026-09-03T04:32:23Z`, owner `Dicklesworthstone`. Repo metadata reports public, created `2026-09-03T04:32:20Z`, pushed `2026-09-03T04:32:25Z`. This supports predicate 3 at the packet's September 24 check date.
- GitHub languages at the pin: HTML 302,931 bytes, JavaScript 271,755, CSS 53,274; no Rust. The pinned tree has static assets, a Pages workflow, and vendored WASM/JS, but no `.rs` source or `Cargo.toml`. Predicate 2 fails; a copied compiled dependency does not make the assessed static site a Rust project.
- The pin has one commit, “Deploy static site via bv --pages”, by Dicklesworthstone, with no coauthor trailer. The pinned README says it was generated by `bv`; no root `AGENTS.md`/`CLAUDE.md` or README statement that coding agents built the repo. Predicate 4 fails.
- Both Actions runs at the pin completed successfully; they are Pages deployment workflows, not test CI. This is consistent with deploy-only behavior, not a product-test result.
- GitHub reports `license: null`; the pinned tree has no license file. The manifest's “MIT (repository license)” is not an operative license in this snapshot.
- The memo discloses predicate 7 as a cohort scope exception, not a literal pass; it is not the reason for its screening failure. Its “probable purpose” paragraph is explicitly `[Inference, Medium]` and says no direct evidence was found; do not elevate it to maintainer intent.

### 2. `beads_bend`

- Pin `a97165dbb0ec2165a6539def8d44c9c56b0db363` exists; GitHub author/committer time is `2026-09-24T23:15:09Z`, matching the packet's `19:15:09 -0400`. Repository metadata reports public, Shell as primary language, `NOASSERTION` license metadata, and push at `2026-09-24T23:15:11Z`.
- At the pin, all 129 commits are authored as Dicklesworthstone. Independent trailer count: 122 commits have `Co-Authored-By`; name counts are Claude Opus 5.5 (63), Claude Fable 5.1 (46), Claude Opus 5 (13). Root `AGENTS.md` independently satisfies predicate 4.
- Pinned `port/LAWS.bend`: `laws=73 closed=72 quantified=1`. Thus 72 are closed equality examples and only one has a `for` binder. This supports the packet's criticism of the “law-proved” framing; it does **not** establish that `PROOF.bend` type-checks, because the packet says it was not run.
- Pinned `goldens/cases.tsv`: 1,094 unique cases. Pinned `goldens/MANIFEST.txt`: 3,282 digest rows across 1,094 cases, three rows per case. These are inventory counts, not my independent replay of all digest values or outputs.
- `.github/` contains `CODEOWNERS` only; Actions runs at the pin are zero; releases and tags are zero. C4 and R1 follow the corpus definitions. The rider is present in the pinned `LICENSE`; bus factor 1 is supported by the 129 same-author commits.
- Predicate 2 is not established as written. The assessed project is implemented in Bend; the Rust `br` binary is its oracle, not the implementation. The candidate rule requires Rust primary language or that the project itself be a Rust port/tool. Cohort predicate-7 scope does not waive predicate 2.
- Rulebook §4.9 supports Explore for substantive-but-unproven work and permits `-with-a-ceiling`; it does not supply objective TRL stage anchors or definitions for each NODUS subscore. The ring is plausible. Numeric TRL/NODUS values are analyst judgments and should carry the Rulebook's inference labels.

### 3. `annus-mirabilis.com`

- Pin `d33916fac50b509d132c1145b6284a2c6102f2ca` exists; author/committer time is `2026-09-24T22:45:47Z`, matching the packet. GitHub reports TypeScript primary language and 48,207 Rust bytes.
- All 4,663 commits at the pin are authored by Jeff Emanuel / Dicklesworthstone. This supports bus factor 1; I did not independently recount every model coauthor trailer.
- Three workflow runs at the pin (Quality Gates, Browser Acceptance, Brownian laboratory preview) completed with failure. Quality Gates has 5 successes among 1,049 runs through the packet's cutoff; releases and tags are both zero. C2 and R1 are supported.
- Independently hashed each of the six pinned public PDFs and compared with its pinned provenance receipt: `ap-17-132`, `ap-17-549`, `ap-17-891`, `ap-18-639`, `ap-19-289`, and `ap-34-591` all match their receipt SHA-256 values.
- The Rust crate is a small `fs-annus-wasm` browser transport boundary (`publish = false`, `cdylib`/`rlib`) that compiles FrankenSim sources by reference; the pinned `src/lib.rs` describes it as transport-only. The site itself is TypeScript/Next.js. The packet's website scope can justify reviewing it as a website, but the published screening rule has no website exception to predicate 2. Rulebook §4.9's Monitor rule for websites is a ring rule, not a screening waiver.
- Monitor is the Rulebook's explicit ring for websites. TRL 7 and the NODUS subscores remain analyst judgments; the Rulebook provides rating ranges/ring rules but no numeric TRL stage rubric. The packet labels these judgments as inference. I did not independently test all deployed routes.
- The pinned license restricts rights to defined Restricted Parties: OpenAI, Anthropic, affiliates, and persons/entities acting for, benefiting, or acting under their direction. The license's “use” definition includes training/evaluation corpus or pipeline use, but that does not turn it into a blanket ban on all ML use by non-restricted parties. A person merely using one of those companies' models is not thereby barred. The packet's line 189 correctly states the model-user distinction and assigns no motive.

## Exact corrections

Each “current” excerpt is from the named local packet commit. Suggested text preserves the underlying evidence and avoids upgrading inference to fact.

1. `cohorts/2026-09/readme_smoke_wt-pages-screening.md:27` — current: “which `vendor/MANIFEST.json` attributes to ‘bv-graph-wasm (this repository)’, meaning beads_viewer” -> should: “which `vendor/MANIFEST.json` attributes to ‘bv-graph-wasm (this repository)’; this snapshot does not establish the source repository. Keep the predicate-2 failure based on the pinned tree having no Rust source or `Cargo.toml`.” (evidence: `gh api 'repos/Dicklesworthstone/readme_smoke_wt-pages/contents/vendor/MANIFEST.json?ref=b45e54bafdcd7db48ad081ab14d25cb4dfc75743' --jq '.content' | base64 -d | jq '.. | objects | select(has("upstream")) | {upstream,source_url,build_command}'` -> upstream `bv-graph-wasm (this repository)`, source `bv-graph-wasm/src`; `gh api 'repos/Dicklesworthstone/readme_smoke_wt-pages/git/trees/b45e54bafdcd7db48ad081ab14d25cb4dfc75743?recursive=1' --jq '[.tree[].path | select(test("(^|/)bv-graph-wasm|\\.rs$|Cargo\\.toml$"))] | if length == 0 then "none" else .[] end'` -> `none`.)

2. `cohorts/2026-09/beads_bend-assessment.md:3` — current: “GitHub API, 2026-09-25 00:0x UTC” -> should: “Remove the placeholder; use a contemporaneous captured timestamp if one exists, otherwise say the API observation time was not preserved.” (evidence: `gh api repos/Dicklesworthstone/beads_bend/commits/a97165dbb0ec2165a6539def8d44c9c56b0db363 --jq '{sha,author:.commit.author.date,committer:.commit.committer.date}'` -> pin dates are `2026-09-24T23:15:09Z`; this does not recover the later observation time.)

3. `cohorts/2026-09/beads_bend-assessment.md:9` — current: “Rust: pass with a note ... the ‘Rust port or Rust tool’ clause read broadly” -> should: “Rust: fail/not established under the published rule. This repository implements the port in Bend; the Rust release binary is the oracle. Reclassify only if the rule is explicitly broadened or a separate scope exception is recorded.” (evidence: `candidates/README.md:12-16` -> every predicate must be true and predicate 2 says the project is Rust-primary, a Rust port, or a Rust tool; `gh api repos/Dicklesworthstone/beads_bend/languages` -> no Rust; pinned tree and `port/LAWS.bend` -> Bend implementation.)

4. `cohorts/2026-09/beads_bend-assessment.md:127-132` — current: untagged “TRL 4” and NODUS scores `2/5, 3/5, 3/5, 1/5, 1/5` -> should: add `[Inference, Medium]` (or another evidence-backed confidence) to each analyst rating and state that TRL/subscore values are judgments, not mechanically defined stages. (evidence: `RULEBOOK.md:91-106` -> score ranges and ring rules, but no numeric stage anchors; `RULEBOOK.md:164-168` -> inferences must be labeled inline.)

5. `cohorts/2026-09/beads_bend-assessment.md:172` — current: “bars incorporating the software into ML training or evaluation pipelines” -> should: “withholds rights from Restricted Parties, including their training/evaluation uses; it does not impose a blanket ML-use ban on non-restricted users or bar a person merely for using those companies' models.” Also tag the statement about cost versus the project's purpose as `[Inference, Medium]`. (evidence: pinned `LICENSE` -> no rights to defined Restricted Parties; the definition of “use” includes training/evaluation; `RULEBOOK.md:164-168` -> inference-label requirement.)

6. `cohorts/2026-09/annus-mirabilis.com-assessment.md:30` — current: “The brief ... directs that the repository be assessed as a website ... so the predicate is set aside here by instruction” -> should: “Predicate 2 fails under the published all-seven rule. A website-focused packet is a separate scope exception, not a screening pass; record it as such.” (evidence: `candidates/README.md:12-16` -> every predicate must be true and no website exception; `RULEBOOK.md:101-106` -> website-to-Monitor mapping is a ring rule only.)

7. `cohorts/2026-09/annus-mirabilis.com-assessment.md:262` — current: “The rider excludes ... and machine-learning training or evaluation use, from the code and the new prose” -> should: “The rider withholds rights from Restricted Parties (OpenAI/Anthropic, affiliates, and their agents), including their use of the software or new prose in ML training/evaluation; it does not prohibit all non-restricted ML uses or merely model use.” (evidence: pinned `LICENSE` -> no rights to Restricted Parties and a training/evaluation-inclusive definition of “use”; `annus-mirabilis.com-assessment.md:189` already correctly says people using those labs' models are not barred.)

## Unverified claims and limits

- I did not recheck the screening memo's historical Pages HTTP 200 observation from 2026-09-24, or its account-wide code-search results. Current pin/tree/API evidence suffices for predicates 2 and 4; these historical/live claims remain packet evidence, not my reproduction.
- Beads `PROOF.bend` was not executed or built. The 72/73 source-shape count is verified; checker success is not. I also did not replay the claimed 804/813 `br` oracle cases, mutate laws, or compare every manifest digest to its file. The packet discloses the proof/lane limits; retain those as unverified, not independent reproduction.
- Annus's all-route live availability, all content/review counts beyond the six PDF hashes above, and the historical 0-fork snapshot were not re-derived here. Current metadata/API can move after the packet's timestamp; I do not call the fork count a packet error from that alone.
- The three packets' inference labels are generally clear. Smoke's “probable purpose” is explicitly inference; Annus line 189 explicitly disclaims motive attribution. Beads line 172 is the exception: the purpose-impact statement is a reviewer judgment, not evidence of maintainer motive.
- No build/test/formatter was run. No target-repo changes were made. The review file is the only requested deliverable written.
